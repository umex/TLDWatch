---
phase: 04-job-orchestrator-persistent-queue-websocket-progress
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - app/jobs/queue.py
  - app/jobs/interrupt.py
  - app/main.py
  - tests/test_orchestrator.py
  - tests/test_cancel.py
  - tests/conftest.py
autonomous: true
requirements: [JOB-04, JOB-05]
tags: [queue, sqlite, restart-resume, cancel, watchdog, asyncio, jobs]

must_haves:
  truths:
    - "Queued jobs re-join FIFO on backend restart (SC-2, D-02)"
    - "Jobs interrupted by backend restart are marked failed, not silently abandoned (D-03)"
    - "Worker=1 strict serial execution; no two jobs run concurrently (D-10)"
    - "Queued-job cancel is instant and idempotent (D-06)"
    - "Running-job cancel sets cancel_flag, stops after current chunk, discards partial (D-06)"
    - "Cancelling a terminal job is a no-op returning the current row (D-06)"
    - "Stale-sweep watchdog marks stale jobs every 60s (D-11)"
    - "Lifespan wires sweep + worker + watchdog; teardown cancels worker+watchdog before engine.dispose"
  artifacts:
    - path: "app/jobs/queue.py"
      provides: "SQLite-backed FIFO queue + single-worker loop + cancel + watchdog"
      exports: ["run_worker", "enqueue", "pull_next", "cancel", "run_watchdog"]
    - path: "app/jobs/interrupt.py"
      provides: "Boot interrupted-job sweep"
      exports: ["mark_interrupted_failed"]
    - path: "app/main.py"
      provides: "Lifespan wiring of sweep/worker/watchdog with ordered teardown"
      contains: "mark_interrupted_failed"
    - path: "tests/test_orchestrator.py"
      provides: "restart_rejoin, boot_interrupted_sweep, serial_no_concurrency, watchdog_stale"
    - path: "tests/test_cancel.py"
      provides: "queued/running/terminal cancel tests"
    - path: "tests/conftest.py"
      provides: "fake_stt, run_worker_off, non-auto-start TestClient fixtures"
  key_links:
    - from: "app/main.py lifespan"
      to: "app/jobs/interrupt.py::mark_interrupted_failed"
      via: "await sweep after reconcile_all, before worker"
      pattern: "mark_interrupted_failed"
    - from: "app/main.py lifespan"
      to: "app/jobs/queue.py::run_worker"
      via: "asyncio.create_task(worker)"
      pattern: "run_worker"
    - from: "app/jobs/queue.py::run_worker"
      to: "app/jobs/orchestrator.py (04-01)"
      via: "await orchestrator.run_job per pulled job"
      pattern: "run_job"
    - from: "app/jobs/queue.py::cancel"
      to: "app/jobs/cleanup.py::cancel_job"
      via: "DB-first cancel + rmtree"
      pattern: "cancel_job"
    - from: "app/jobs/queue.py::cancel (running path)"
      to: "app/jobs/orchestrator.py::_running (04-01)"
      via: "import _running; _running[job_id].set()"
      pattern: "_running"
---

<objective>
04-02 builds the persistent SQLite-backed FIFO queue, restart-resume sweep, cooperative cancel, and stale-sweep watchdog on top of 04-01's orchestrator state-machine driver (run_job, JobCancelled, _running registry), progress EventBus, and STTAdapter progress_cb/cancel_flag extension.

Purpose: Deliver SC-2 (restart re-join) and SC-4 (idempotent cancel + deterministic cleanup) so jobs survive backend restarts and cancel leaves no partial artifacts. Per D-02 no mid-transcription checkpointing; per D-10 worker=1 strict FIFO serial; per D-03 boot sweep marks only ingesting/transcribing (not queued); per D-06 cancel is idempotent with terminal no-op; per D-11 watchdog 60s cadence.

Output: app/jobs/queue.py (worker loop + enqueue/pull_next + cancel + run_watchdog), app/jobs/interrupt.py (boot sweep), app/main.py lifespan wiring, test suite (test_orchestrator.py, test_cancel.py, conftest.py). 04-01 already provides Settings.run_worker — 04-02 consumes it, does NOT re-add it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-job-orchestrator-persistent-queue-websocket-progress/04-01-PLAN.md
@.planning/phases/04-job-orchestrator-persistent-queue-websocket-progress/04-CONTEXT.md
@.planning/phases/04-job-orchestrator-persistent-queue-websocket-progress/04-RESEARCH.md
@.planning/phases/04-job-orchestrator-persistent-queue-websocket-progress/04-PATTERNS.md
@app/jobs/cleanup.py
@app/jobs/reconcile.py
@app/jobs/manifest.py
@app/jobs/orchestrator.py
@app/main.py
@app/models/settings.py
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Wave 0 test scaffolding for restart-resume, serial, watchdog, cancel</name>
  <read_first>
    - tests/conftest.py (existing fixtures — extend, do not duplicate; 04-01 already added run_worker=False to tmp_data_dir and extended mock_stt_adapter)
    - app/jobs/cleanup.py (cancel_job(session, settings, job_id)->bool, mark_failed(session, job_id, error)->bool, is_stale(settings, job_id, threshold_s=600)->bool, mark_stale(session, settings, job_id, threshold_s=600)->tuple[bool,bool], _TERMINAL_STATUSES=frozenset({"done","failed","cancelled"}))
    - app/jobs/reconcile.py (reconcile_all(settings, session_factory) signature + ordering)
    - .planning/phases/04-job-orchestrator-persistent-queue-websocket-progress/04-01-PLAN.md (run_job(settings, session_factory, job_id, bus, adapter), JobCancelled, _running: dict[str, threading.Event] registry, FakeAdapter honors progress_cb/cancel_flag)
    - app/models/settings.py (Settings.run_worker: bool = True — ALREADY added by 04-01; 04-02 consumes only)
    - tests/_stt_fake.py (04-01 extended FakeAdapter.transcribe with progress_cb/cancel_flag — reuse, do not re-extend)
  </read_first>
  <files>tests/conftest.py, tests/test_orchestrator.py, tests/test_cancel.py</files>
  <behavior>
    - test_cancel_queued: queued job cancel -> row status='cancelled', source dir rm'd, returns row
    - test_cancel_running: running job cancel -> cancel_flag set, chunker stops after current chunk, final status='cancelled', no partial transcript.json
    - test_cancel_terminal: done/failed/cancelled job cancel -> no-op, returns current row unchanged
    - test_restart_rejoin: queued jobs survive restart, re-join FIFO on boot
    - test_boot_interrupted_sweep: jobs in status ingesting/transcribing at boot -> failed, error="interrupted (backend restarted)"; queued jobs NOT swept
    - test_serial_no_concurrency: enqueue 3 jobs -> worker=1 executes strictly serial, max 1 concurrent
    - test_watchdog_stale: job last_heartbeat > 600s -> mark_stale fires; terminal job -> short-circuit skip
  </behavior>
  <action>
    Extend tests/conftest.py (04-01 already set run_worker=False in tmp_data_dir and extended mock_stt_adapter — verify, do NOT duplicate). Add: (1) `fake_stt` fixture returning tests/_stt_fake.py::FakeAdapter (04-01 made it honor progress_cb/cancel_flag) configured for N fake chunks; (2) `run_worker_off` fixture that asserts settings.run_worker is False (already set by tmp_data_dir — this fixture is a sanity alias, auto-use where needed); (3) async TestClient fixture that does NOT auto-start worker (relies on run_worker=False from tmp_data_dir). Guard `mark_interrupted_failed`/`run_worker`/`enqueue` imports in try/except so collection succeeds before Task 2/3 land.

    Create tests/test_cancel.py with 3 parametrized tests (queued/running/terminal) — use pytest.mark.parametrize over status. Body asserts against cleanup.cancel_job + queue.cancel behavior; mark xfail until Task 3 lands.

    Create tests/test_orchestrator.py with 4 NEW tests (04-01 already added test_state_machine + test_restart_rejoin — ADD to the file, do NOT clobber): restart_rejoin_boot (insert queued rows, call mark_interrupted_failed + run_worker, assert re-join and completion), boot_interrupted_sweep (insert ingesting+transcribing+queued rows, call mark_interrupted_failed, assert only ingesting/transcribing -> failed, queued untouched), serial_no_concurrency (3 enqueued jobs, semaphore-based FakeAdapter that asserts >1 concurrent would fail), watchdog_stale (fake old last_stage_mtime, call run_watchdog one tick, assert mark_stale fires; terminal job -> short-circuit). All xfail until Task 2/3/4 land.

    RED state expected: new tests fail on missing imports (queue, interrupt). Commit as `test(04-02): add wave-0 test scaffolding for queue/cancel/sweep`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_cancel.py tests/test_orchestrator.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py contains `fake_stt` fixture reusing tests/_stt_fake.py::FakeAdapter and async TestClient that does not start worker (run_worker=False already set by 04-01's tmp_data_dir)
    - tests/test_cancel.py contains test_cancel_queued, test_cancel_running, test_cancel_terminal
    - tests/test_orchestrator.py contains test_restart_rejoin_boot, test_boot_interrupted_sweep, test_serial_no_concurrency, test_watchdog_stale (ADDED to existing 04-01 tests, not replacing)
    - `python -m pytest tests/test_cancel.py tests/test_orchestrator.py --collect-only -q` exits 0 (collection succeeds)
    - New tests xfail/skip until Tasks 2-4 land
  </acceptance_criteria>
  <done>Test scaffolding collects cleanly; xfail markers applied; conftest fixtures reuse 04-01's FakeAdapter and run_worker=False setting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SQLite-backed FIFO queue (app/jobs/queue.py) + boot interrupted sweep (app/jobs/interrupt.py)</name>
  <read_first>
    - app/jobs/cleanup.py (mark_failed(session, job_id, error)->bool — NOTE: no settings param; _TERMINAL_STATUSES; cancel_job(session, settings, job_id)->bool)
    - app/jobs/reconcile.py (reconcile_all(settings, session_factory) — queue + sweep run AFTER this)
    - app/jobs/manifest.py (update_stage signature + stage-first/DB-last ordering)
    - app/db/models.py or app/db/schema.py (Job columns: id, status, created_at, updated_at, error, source_name)
    - app/jobs/orchestrator.py (04-01: run_job(settings, session_factory, job_id, bus=None, adapter=None)->None, _running: dict[str, threading.Event], JobCancelled)
    - app/models/settings.py (Settings.run_worker: bool = True — PROVIDED BY 04-01, consume only, do NOT re-add)
    - app/storage/fs.py (job_dir(settings, job_id) for rmtree target)
  </read_first>
  <files>app/jobs/queue.py, app/jobs/interrupt.py</files>
  <behavior>
    - enqueue(job_id, session): UPDATE row status='queued' (direct DB UPDATE — enqueue is pre-stage, manifest.update_stage NOT used here), then _work_signal.set()
    - pull_next(session): SELECT id FROM jobs WHERE status='queued' ORDER BY created_at LIMIT 1; return id or None
    - run_worker(settings, session_factory, bus): single asyncio task; loop: pull_next -> if None await _work_signal.wait()+clear -> else await orchestrator.run_job(settings, session_factory, job_id, bus=bus) -> loop
    - mark_interrupted_failed(session) -> int: SELECT ids WHERE status IN ('ingesting','transcribing') (NOT 'queued'); for each: await mark_failed(session, id, "interrupted (backend restarted)") — mark_failed keeps the folder (no rmtree — source data may be intact); return count
    - settings.run_worker=False -> run_worker returns immediately (tests drive manually)
  </behavior>
  <action>
    app/jobs/queue.py: async module. Use AsyncSession (session-per-operation, await DB directly — no thread offload for DB; the project already uses SQLAlchemy async + aiosqlite per Phase 1). Maintain module-level `asyncio.Event` `_work_signal`. `enqueue(job_id, session)`: `await session.execute(text("UPDATE jobs SET status='queued', updated_at=:now WHERE id=:id"), {...}); await session.commit(); _work_signal.set()`. `pull_next(session)`: `SELECT id FROM jobs WHERE status='queued' ORDER BY created_at LIMIT 1`; return id or None. `run_worker(settings, session_factory, bus=None)`: guard `if not settings.run_worker: return`; loop: `async with session_factory() as session: job_id = await pull_next(session)`; if None: `await _work_signal.wait(); _work_signal.clear(); continue`; else `await orchestrator.run_job(settings, session_factory, job_id, bus=bus)` (04-01 contract — `run_job(settings, session_factory, job_id, bus, adapter)` per 04-01-PLAN.md). Worker=1 strict serial (D-10): single asyncio task, NO asyncio.gather of multiple jobs. Import `from app.jobs.orchestrator import run_job` and `from app.models.settings import Settings` (Settings.run_worker already exists from 04-01).

    app/jobs/interrupt.py: `async def mark_interrupted_failed(session) -> int`: `SELECT id FROM jobs WHERE status IN ('ingesting','transcribing')` (NOT 'queued' — D-03: queued re-join FIFO, active interrupted -> failed, no auto-resume). For each id: `await mark_failed(session, id, "interrupted (backend restarted)")` (reuse cleanup.mark_failed(session, job_id, error) — signature is (session, job_id, error), NO settings arg; mark_failed keeps the folder — do NOT rmtree source, source data may be intact). Return count. MUST be called AFTER reconcile_all and BEFORE run_worker (enforced in Task 4 lifespan wiring). Import `from app.jobs.cleanup import mark_failed`.

    Do NOT add run_worker to Settings — 04-01 already added it. Import Settings.run_worker for the guard.

    Commit: `feat(04-02): add SQLite FIFO worker + boot interrupted sweep`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_orchestrator.py::test_restart_rejoin_boot tests/test_orchestrator.py::test_boot_interrupted_sweep tests/test_orchestrator.py::test_serial_no_concurrency -x</automated>
  </verify>
  <acceptance_criteria>
    - app/jobs/queue.py contains `async def run_worker(`, `async def enqueue(`, `async def pull_next(`
    - app/jobs/queue.py guards `if not settings.run_worker: return` at top of run_worker
    - app/jobs/queue.py uses asyncio.Event `_work_signal` signaled in enqueue, awaited in run_worker when pull_next is None
    - app/jobs/queue.py pulls `ORDER BY created_at` (strict FIFO, D-10)
    - app/jobs/queue.py does NOT use asyncio.gather for multiple jobs (grep `gather` in queue.py matches nothing or only teardown-cancellation in Task 3)
    - app/jobs/queue.py imports `from app.jobs.orchestrator import run_job` (04-01 contract, no scope overlap)
    - app/jobs/interrupt.py contains `async def mark_interrupted_failed(`
    - app/jobs/interrupt.py selects `status IN ('ingesting','transcribing')` — NO 'queued'
    - app/jobs/interrupt.py calls `mark_failed(session, id, "interrupted (backend restarted)")` with the correct (session, job_id, error) signature
    - app/jobs/interrupt.py does NOT call rmtree (source preserved)
    - pytest tests/test_orchestrator.py::test_restart_rejoin_boot, test_boot_interrupted_sweep, test_serial_no_concurrency exit 0
  </acceptance_criteria>
  <done>Worker loop pulls FIFO-ordered queued jobs strictly serially via 04-01 run_job; boot sweep marks only ingesting/transcribing jobs as failed with interrupted error (source preserved); run_worker flag (from 04-01) gates auto-start.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cooperative cancel (queued/running/terminal) + stale-sweep watchdog</name>
  <read_first>
    - app/jobs/cleanup.py (cancel_job(session, settings, job_id)->bool DB-first+rmtree; mark_stale(session, settings, job_id, threshold_s=600)->tuple[bool,bool] status-aware gate; is_stale(settings, job_id, threshold_s=600)->bool; _TERMINAL_STATUSES=frozenset({"done","failed","cancelled"}))
    - app/jobs/queue.py (Task 2 — _work_signal, run_worker, enqueue)
    - app/jobs/orchestrator.py (04-01: `_running: dict[str, threading.Event]` module-level registry; JobCancelled; run_job sets _running[job_id]=cancel_flag in try/finally pop)
    - app/jobs/manifest.py + app/storage/atomic.py (atomic_write_json fires ONLY after transcribe returns — confirm no partial transcript on cancel)
    - app/storage/fs.py (job_dir(settings, job_id) for rmtree; transcript_path for no-partial check)
  </read_first>
  <files>app/jobs/queue.py, tests/test_cancel.py</files>
  <behavior>
    - cancel(job_id, session, settings) -> dict: SELECT row; if status in _TERMINAL_STATUSES -> return row unchanged (no-op, D-06 idempotent)
    - cancel queued: await cancel_job(session, settings, job_id) (DB-first + rmtree via cleanup) + _work_signal.set() (in case worker just pulled it)
    - cancel running (ingesting/transcribing): import _running from orchestrator; flag = _running.get(job_id); if flag: flag.set() (threading.Event); orchestrator chunker stops after current chunk via 04-01's cancel_flag check; orchestrator's JobCancelled path calls cancel_job itself (04-01 contract) — do NOT double-rmtree; if flag is None (race: job just finished), treat as terminal no-op. Return final row.
    - watchdog tick: iterate non-terminal jobs; call is_stale(settings, job_id) -> if True await mark_stale(session, settings, job_id) (reuse cleanup; status-aware short-circuits terminal per D-11)
    - run_watchdog: asyncio task; loop asyncio.sleep(60) then tick; guard with settings.run_worker
  </behavior>
  <action>
    Extend app/jobs/queue.py with `async def cancel(job_id, session, settings) -> dict`: (1) `SELECT status FROM jobs WHERE id=:id` -> row; if None return {} (caller maps 404); (2) if `status in _TERMINAL_STATUSES` (import `from app.jobs.cleanup import cancel_job, mark_stale, is_stale, _TERMINAL_STATUSES`) -> return row, no-op (D-06); (3) if `status == 'queued'`: `await cancel_job(session, settings, job_id)` (DB-first + rmtree via cleanup) + `_work_signal.set()` (in case worker just pulled it); (4) if `status in ('ingesting','transcribing')`: `from app.jobs.orchestrator import _running; flag = _running.get(job_id)`; if flag is not None: `flag.set()` (threading.Event — 04-01 chunker checks at loop top and raises JobCancelled; 04-01's run_job JobCancelled path calls cancel_job + rmtree itself — do NOT call cancel_job here, that would double-rmtree); if flag is None (race: job finished between SELECT and now) -> re-SELECT; if now terminal, return row no-op; else fall through. Return final row via re-SELECT.

    Add `async def run_watchdog(settings, session_factory) -> None`: guard `if not settings.run_worker: return`; loop: `await asyncio.sleep(60)`; `async with session_factory() as session:` iterate job ids (SELECT id FROM jobs WHERE status NOT IN ('done','failed','cancelled')); for each `if is_stale(settings, job_id): await mark_stale(session, settings, job_id)` (reuse cleanup; mark_stale has its own status-aware gate — double safety). Single asyncio task. Cancellable via asyncio (Task 4 cancels on teardown).

    Commit: `feat(04-02): add cooperative cancel + stale-sweep watchdog`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_cancel.py tests/test_orchestrator.py::test_watchdog_stale -x</automated>
  </verify>
  <acceptance_criteria>
    - app/jobs/queue.py contains `async def cancel(` and `async def run_watchdog(`
    - cancel checks `_TERMINAL_STATUSES` first and returns row unchanged for terminal jobs
    - cancel for 'queued' calls cancel_job(session, settings, job_id) + _work_signal.set()
    - cancel for running imports `from app.jobs.orchestrator import _running` and calls `_running.get(job_id)` then `.set()` (threading.Event); does NOT double-call cancel_job (04-01 run_job JobCancelled path handles that)
    - cancel handles the race where _running has no flag (job just finished) by re-selecting and treating terminal as no-op
    - run_watchdog sleeps 60s between ticks, calls is_stale + mark_stale (reused from cleanup), guards with settings.run_worker
    - tests/test_cancel.py all 3 (queued/running/terminal) pass
    - tests/test_orchestrator.py::test_watchdog_stale passes
  </acceptance_criteria>
  <done>Cancel is idempotent across queued/running/terminal; running cancel sets 04-01 threading.Event cancel_flag and lets orchestrator's JobCancelled path do the cleanup (no double-rmtree); watchdog marks stale non-terminal jobs every 60s; no partial transcript left on cancel (atomic_write only at end).</done>
</task>

<task type="auto">
  <name>Task 4: Lifespan wiring — sweep + worker + watchdog with teardown ordering</name>
  <read_first>
    - app/main.py (lifespan: reconcile_all at ~main.py:202-214, print("ready") ~main.py:219, engine.dispose() ~main.py:238; session_factory in scope)
    - app/jobs/interrupt.py (Task 2 mark_interrupted_failed(session)->int)
    - app/jobs/queue.py (Task 2+3 run_worker(settings, session_factory, bus), run_watchdog(settings, session_factory))
    - app/jobs/reconcile.py (reconcile_all(settings, session_factory) — sweep runs AFTER this)
    - app/jobs/progress.py (04-01 EventBus — worker needs a bus instance to pass to run_job)
  </read_first>
  <files>app/main.py</files>
  <action>
    In app/main.py lifespan, AFTER `await reconcile_module.reconcile_all(settings, session_factory)` (~main.py:202-214) and BEFORE `print(f"TranscriptionAndNotes backend ready..."` (~main.py:219): (1) `from app.jobs.interrupt import mark_interrupted_failed` then `async with session_factory() as session: swept = await mark_interrupted_failed(session)` and log `swept` count (D-03 boot sweep — runs AFTER reconcile_all, BEFORE worker); (2) `from app.jobs.queue import run_worker, run_watchdog` and `from app.jobs.progress import EventBus`; create a module-level/app-state EventBus instance `bus = EventBus()`; (3) `worker_task = asyncio.create_task(run_worker(settings, session_factory, bus=bus))` if `settings.run_worker`; (4) `watchdog_task = asyncio.create_task(run_watchdog(settings, session_factory))` if `settings.run_worker`. Store worker_task + watchdog_task on app state (or module-level refs) for teardown.

    In lifespan teardown (the `finally:` block at ~main.py:223-238): cancel worker_task + watchdog_task BEFORE `await engine.dispose()` (~main.py:238). Insert, before the existing model unload block: `worker_task.cancel(); await asyncio.gather(worker_task, return_exceptions=True)`; `watchdog_task.cancel(); await asyncio.gather(watchdog_task, return_exceptions=True)` (guard with hasattr/None checks for the case where lifespan failed before creating them). THEN proceed to the existing model unload + `await engine.dispose()`. This guarantees no DB access after engine closed.

    Do NOT change reconcile_all ordering. Insert strictly between reconcile_all and ready. Per D-03: sweep runs AFTER reconcile_all (which fixes DB/source divergence) and BEFORE worker (so worker doesn't pick up jobs that should be failed). Guard task creation with `if settings.run_worker:` so tests with run_worker=False skip auto-start.

    Commit: `feat(04-02): wire sweep+worker+watchdog into lifespan with ordered teardown`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_orchestrator.py -x && python -c "import ast; ast.parse(open('app/main.py').read()); print('main.py parses')"</automated>
  </verify>
  <acceptance_criteria>
    - app/main.py lifespan calls `mark_interrupted_failed` after `reconcile_all` and before `print("ready")`
    - app/main.py creates worker_task and watchdog_task via asyncio.create_task guarded by `if settings.run_worker:`
    - app/main.py teardown cancels worker_task and watchdog_task (with return_exceptions=True) BEFORE `await engine.dispose()`
    - app/main.py passes a shared EventBus instance to run_worker (so WS subscribers in 04-03 see events)
    - `python -c "import ast; ast.parse(open('app/main.py').read())"` exits 0
    - tests/test_orchestrator.py full suite passes
  </acceptance_criteria>
  <done>Lifespan wires reconcile_all -> mark_interrupted_failed -> run_worker -> run_watchdog in correct order with deterministic teardown that cancels tasks (return_exceptions=True) before engine disposal.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- app/jobs/queue.py — run_worker (asyncio single-worker loop), enqueue, pull_next, cancel, run_watchdog
- app/jobs/interrupt.py — mark_interrupted_failed (boot sweep)
- app/main.py — lifespan wiring (sweep + worker + watchdog with ordered teardown)
- tests/conftest.py — fake_stt, run_worker_off, non-auto-start TestClient (extends 04-01's fixtures)
- tests/test_orchestrator.py — restart_rejoin_boot, boot_interrupted_sweep, serial_no_concurrency, watchdog_stale (added to 04-01's existing tests)
- tests/test_cancel.py — queued/running/terminal cancel

CONSUMED FROM 04-01 (NOT re-created): app/jobs/orchestrator.py (run_job, JobCancelled, _running registry), app/jobs/progress.py (EventBus), app/models/stt/protocol.py + chunker.py (progress_cb/cancel_flag), app/models/settings.py (Settings.run_worker — 04-02 imports and consumes, does NOT add), tests/_stt_fake.py (FakeAdapter already honors progress_cb/cancel_flag).

EXCLUDED (04-01 scope, do NOT duplicate): orchestrator.py state-machine driver, progress.py EventBus, STTAdapter progress_cb/cancel_flag extension, Settings.run_worker field.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| boot → DB | restart sweep reads/writes job rows based on status; wrong scope corrupts state |
| worker → DB | single asyncio worker pulls + runs; concurrency violation breaks D-10 |
| cancel (asyncio) → running job (thread) | cancel_flag threading.Event crosses asyncio/thread boundary; race with terminal commit |
| watchdog → DB | periodic mark_stale; mis-scope marks terminal jobs stale |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering/Race | cancel + terminal commit | mitigate | DB-first cancel_job; check `_TERMINAL_STATUSES` first; cancel terminal = no-op returning current row; running cancel sets 04-01 threading.Event and lets run_job's JobCancelled path do cancel_job (no double-rmtree) (D-06) |
| T-04-05 | Spoofing/Tampering | boot sweep marks wrong jobs | mitigate | "active" = status IN ('ingesting','transcribing') only (NOT 'queued'); runs AFTER reconcile_all, BEFORE worker (D-03) |
| T-04-06 | Information Disclosure | cancel leaves partial transcript | mitigate | atomic_write_json fires only after transcribe returns (04-01 Pitfall 4) -> nothing partial on cancel; 04-01 run_job JobCancelled path does NOT write transcript.json; rmtree on cancel; verify no transcript.json left |
| T-04-07 | DoS | watchdog marks terminal jobs stale | mitigate | reuse is_stale/mark_stale — mark_stale has status-aware gate short-circuiting terminal (D-11); worker SELECT also filters out terminal |
| T-04-08 | Tampering | worker concurrency violation (D-10) | mitigate | single asyncio task, no asyncio.gather of multiple jobs, strict FIFO ORDER BY created_at; one run_job at a time |
| T-04-09 | Race | cancel sets _running flag but job just finished | mitigate | if _running.get(job_id) is None, re-SELECT status; if now terminal, return row no-op; else fall through |
| T-04-SC | Tampering | pip installs (aiosqlite already in project per Phase 1) | accept | aiosqlite is an existing project dependency (Phase 1 stack); no new package install in 04-02 |
</threat_model>

<verification>
- `python -m pytest tests/test_cancel.py tests/test_orchestrator.py -x` — all green
- `python -c "import ast; ast.parse(open('app/main.py').read())"` — main.py parses
- Boot sequence: reconcile_all -> mark_interrupted_failed -> run_worker -> run_watchdog (verifiable via lifespan order in app/main.py)
- Teardown: worker_task.cancel() + watchdog_task.cancel() (return_exceptions=True) BEFORE engine.dispose()
- `grep -n "run_worker" app/models/settings.py` shows Settings.run_worker flag (provided by 04-01, consumed by 04-02)
- Worker=1: `grep -n "gather" app/jobs/queue.py` matches only teardown-cancellation (run_watchdog/teardown), NOT concurrent job execution
- No scope overlap: `grep -n "class EventBus\|def run_job\|progress_cb" app/jobs/queue.py app/jobs/interrupt.py` matches nothing (04-01 owns those)
- No double-rmtree: `grep -n "cancel_job" app/jobs/queue.py` matches only the queued-cancel path (running path sets _running flag, does NOT call cancel_job)
</verification>

<success_criteria>
- SC-2 restart re-join: queued jobs re-join FIFO on boot (test_restart_rejoin_boot green)
- SC-4 idempotent cancel + deterministic cleanup: queued/running/terminal cancel all behave correctly (test_cancel.py green); no partial transcript
- D-02 no mid-transcription resume: reuse infer_resume_point; no new checkpointing
- D-03 boot sweep: ingesting/transcribing -> failed with "interrupted (backend restarted)"; queued NOT swept (source preserved)
- D-06 cancel idempotent; terminal = no-op returning row; running = set threading.Event, let 04-01 JobCancelled path clean up
- D-10 worker=1 strict serial (test_serial_no_concurrency green)
- D-11 watchdog 60s cadence, status-aware (test_watchdog_stale green)
- Lifespan wiring correct with ordered teardown (worker+watchdog cancelled before engine.dispose)
- Zero scope overlap with 04-01 (no re-creation of orchestrator/EventBus/STTAdapter/run_worker field)
</success_criteria>

<output>
Create `.planning/phases/04-job-orchestrator-persistent-queue-websocket-progress/04-02-SUMMARY.md` when done
</output>