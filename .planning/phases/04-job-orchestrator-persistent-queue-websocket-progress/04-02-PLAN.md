---
phase: 04-job-orchestrator-persistent-queue-websocket-progress
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - app/jobs/queue.py
  - app/jobs/interrupt.py
  - app/main.py
  - tests/test_orchestrator.py
  - tests/test_cancel.py
  - tests/conftest.py
autonomous: true
requirements: [JOB-04, JOB-05]
tags: [queue, sqlite, restart-resume, cancel, watchdog, asyncio, jobs]

must_haves:
  truths:
    - "Queued jobs re-join FIFO on backend restart (SC-2, D-02)"
    - "Jobs interrupted by backend restart are marked failed in BOTH DB and manifest, not silently abandoned (D-03 + Codex MEDIUM — manifest stays consistent so reconcile_all does not revert)"
    - "Worker=1 strict serial execution; no two jobs run concurrently (D-10)"
    - "pull_next atomically claims a queued job via conditional UPDATE ... WHERE status='queued' so a duplicate worker cannot run the same job twice (Fix 6 — Codex HIGH)"
    - "run_worker uses hybrid Event + poll wakeup (asyncio.wait_for(_work_signal.wait(), timeout=2.0)) so a missed signal does not stall the queue (Fix 1 — both reviewers)"
    - "Queued-job cancel is instant and idempotent (D-06)"
    - "Running-job cancel sets cancel_flag, stops after current chunk, discards partial (D-06)"
    - "Cancelling a terminal job is a no-op returning the current row (D-06)"
    - "Stale-sweep watchdog excludes queued jobs (they are not active — Codex MEDIUM) and marks stale active jobs every 60s (D-11); active transcribing jobs are kept fresh by 04-01's heartbeat (Fix 2)"
    - "enqueue guards against re-queueing terminal or active jobs (Codex MEDIUM — status-aware)"
    - "Lifespan wires sweep + worker + watchdog; teardown cancels worker+watchdog BEFORE engine.dispose; in-flight transcription exits cooperatively via 04-01's graceful shutdown"
  artifacts:
    - path: "app/jobs/queue.py"
      provides: "SQLite-backed FIFO queue with atomic claiming + single-worker hybrid-wakeup loop + cancel + watchdog"
      exports: ["run_worker", "enqueue", "pull_next", "cancel", "run_watchdog"]
    - path: "app/jobs/interrupt.py"
      provides: "Boot interrupted-job sweep — updates DB AND manifest so reconcile_all does not revert"
      exports: ["mark_interrupted_failed"]
    - path: "app/main.py"
      provides: "Lifespan wiring of sweep/worker/watchdog with ordered teardown + app.state.bus/settings/session_factory"
      contains: "mark_interrupted_failed"
    - path: "tests/test_orchestrator.py"
      provides: "restart_rejoin, boot_interrupted_sweep, serial_no_concurrency, watchdog_stale, watchdog_excludes_queued"
    - path: "tests/test_cancel.py"
      provides: "queued/running/terminal cancel tests"
    - path: "tests/conftest.py"
      provides: "fake_stt, run_worker_off, non-auto-start TestClient fixtures"
  key_links:
    - from: "app/main.py lifespan"
      to: "app/jobs/interrupt.py::mark_interrupted_failed"
      via: "await sweep after reconcile_all, before worker"
      pattern: "mark_interrupted_failed"
    - from: "app/main.py lifespan"
      to: "app/jobs/queue.py::run_worker"
      via: "asyncio.create_task(worker)"
      pattern: "run_worker"
    - from: "app/jobs/queue.py::run_worker"
      to: "app/jobs/orchestrator.py (04-01)"
      via: "await orchestrator.run_job per pulled job"
      pattern: "run_job"
    - from: "app/jobs/queue.py::pull_next"
      to: "atomic claim via UPDATE jobs SET status='starting' WHERE id=:id AND status='queued'"
      via: "conditional UPDATE — only run if one row changed (Fix 6)"
      pattern: "status='queued'"
    - from: "app/jobs/queue.py::run_worker"
      to: "asyncio.wait_for(_work_signal.wait(), timeout=2.0)"
      via: "hybrid Event + poll wakeup (Fix 1)"
      pattern: "wait_for"
    - from: "app/jobs/queue.py::cancel"
      to: "app/jobs/cleanup.py::cancel_job"
      via: "DB-first cancel + rmtree"
      pattern: "cancel_job"
    - from: "app/jobs/queue.py::cancel (running path)"
      to: "app/jobs/orchestrator.py::_running (04-01)"
      via: "import _running; _running[job_id].set()"
      pattern: "_running"
---

<objective>
04-02 builds the persistent SQLite-backed FIFO queue, restart-resume sweep, cooperative cancel, and stale-sweep watchdog on top of 04-01's orchestrator state-machine driver (run_job, JobCancelled, _running registry, heartbeat, progress.json), progress EventBus, and STTAdapter progress_cb/cancel_flag extension.

Purpose: Deliver SC-2 (restart re-join) and SC-4 (idempotent cancel + deterministic cleanup) so jobs survive backend restarts and cancel leaves no partial artifacts. The review fixes folded in here (Fix 1 — hybrid Event+poll wakeup; Fix 2-partial — watchdog excludes queued + 04-01 heartbeat keeps active jobs fresh; Fix 6 — atomic job claiming) close operational correctness gaps surfaced by Codex + Ollama: no missed-signal queue stalls, no duplicate execution, no false-stale kills on long transcriptions, no orphaned active jobs on restart. Per D-02 no mid-transcription checkpointing; per D-10 worker=1 strict FIFO serial; per D-03 boot sweep marks only ingesting/transcribing (not queued) and updates BOTH DB and manifest (Codex MEDIUM); per D-06 cancel is idempotent with terminal no-op; per D-11 watchdog 60s cadence excluding queued.

Output: app/jobs/queue.py (worker loop with hybrid wakeup + atomic claim + enqueue/pull_next + cancel + run_watchdog), app/jobs/interrupt.py (boot sweep updating DB + manifest), app/main.py lifespan wiring (sweep + worker + watchdog + teardown + app.state), test suite (test_orchestrator.py, test_cancel.py, conftest.py). 04-01 already provides Settings.run_worker — 04-02 consumes it, does NOT re-add it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-job-orchestrator-persistent-queue-websocket-progress/04-01-PLAN.md
@.planning/phases/04-job-orchestrator-persistent-queue-websocket-progress/04-CONTEXT.md
@.planning/phases/04-job-orchestrator-persistent-queue-websocket-progress/04-RESEARCH.md
@.planning/phases/04-job-orchestrator-persistent-queue-websocket-progress/04-PATTERNS.md
@.planning/phases/04-job-orchestrator-persistent-queue-websocket-progress/04-REVIEWS.md
@app/jobs/cleanup.py
@app/jobs/reconcile.py
@app/jobs/manifest.py
@app/jobs/orchestrator.py
@app/jobs/progress.py
@app/main.py
@app/models/settings.py
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Wave 0 test scaffolding for restart-resume, serial, watchdog, cancel, atomic claim, hybrid wakeup</name>
  <read_first>
    - tests/conftest.py (existing fixtures — extend, do not duplicate; 04-01 already added run_worker=False to tmp_data_dir and extended mock_stt_adapter)
    - app/jobs/cleanup.py (cancel_job(session, settings, job_id)->bool, mark_failed(session, job_id, error)->bool, is_stale(settings, job_id, threshold_s=600)->bool, mark_stale(session, settings, job_id, threshold_s=600)->tuple[bool,bool], _TERMINAL_STATUSES=frozenset({"done","failed","cancelled"}))
    - app/jobs/reconcile.py (reconcile_all(settings, session_factory) signature + ordering)
    - .planning/phases/04-job-orchestrator-persistent-queue-websocket-progress/04-01-PLAN.md (run_job(settings, session_factory, job_id, bus, adapter), JobCancelled in app/jobs/errors.py, _running: dict[str, threading.Event] registry, FakeAdapter honors progress_cb/cancel_flag, heartbeat + progress.json writes)
    - app/models/settings.py (Settings.run_worker: bool = True — ALREADY added by 04-01; 04-02 consumes only)
    - tests/_stt_fake.py (04-01 extended FakeAdapter.transcribe with progress_cb/cancel_flag — reuse, do not re-extend)
    - 04-REVIEWS.md Fix 1 (hybrid wakeup), Fix 6 (atomic claim), MEDIUM concerns (mark_interrupted_failed manifest update, exclude queued from watchdog, enqueue status guards)
  </read_first>
  <files>tests/conftest.py, tests/test_orchestrator.py, tests/test_cancel.py</files>
  <behavior>
    - test_cancel_queued: queued job cancel -> row status='cancelled', source dir rm'd, returns row
    - test_cancel_running: running job cancel -> cancel_flag set, chunker stops after current chunk, final status='cancelled', no partial transcript.json
    - test_cancel_terminal: done/failed/cancelled job cancel -> no-op, returns current row unchanged
    - test_restart_rejoin: queued jobs survive restart, re-join FIFO on boot
    - test_boot_interrupted_sweep: jobs in status ingesting/transcribing at boot -> failed in DB AND manifest (error="interrupted (backend restarted)"); queued jobs NOT swept
    - test_serial_no_concurrency: enqueue 3 jobs -> worker=1 executes strictly serial, max 1 concurrent
    - test_watchdog_stale: active job last_heartbeat > 600s -> mark_stale fires; terminal job -> short-circuit skip; QUEUED job -> NOT swept (Codex MEDIUM — queued excluded)
    - test_atomic_claim: simulate two concurrent pull_next calls on the same queued job -> only one returns the job_id, the other returns None (Fix 6)
    - test_hybrid_wakeup_no_signal: enqueue a job AFTER the worker is already waiting on _work_signal.wait() -> the poll timeout (2s) catches it even if the signal was missed (Fix 1)
  </behavior>
  <action>
    Extend tests/conftest.py (04-01 already set run_worker=False in tmp_data_dir and extended mock_stt_adapter — verify, do NOT duplicate). Add: (1) `fake_stt` fixture returning tests/_stt_fake.py::FakeAdapter (04-01 made it honor progress_cb/cancel_flag) configured for N fake chunks; (2) `run_worker_off` fixture that asserts settings.run_worker is False (already set by tmp_data_dir — this fixture is a sanity alias, auto-use where needed); (3) async TestClient fixture that does NOT auto-start worker (relies on run_worker=False from tmp_data_dir). Guard `mark_interrupted_failed`/`run_worker`/`enqueue` imports in try/except so collection succeeds before Task 2/3 land.

    Create tests/test_cancel.py with 3 parametrized tests (queued/running/terminal) — use pytest.mark.parametrize over status. Body asserts against cleanup.cancel_job + queue.cancel behavior; mark xfail until Task 3 lands.

    Create tests/test_orchestrator.py with NEW tests (04-01 already added test_state_machine + test_restart_rejoin + test_heartbeat_during_transcribing + test_progress_snapshot_persisted — ADD to the file, do NOT clobber): restart_rejoin_boot (insert queued rows, call mark_interrupted_failed + run_worker, assert re-join and completion), boot_interrupted_sweep (insert ingesting+transcribing+queued rows, call mark_interrupted_failed, assert only ingesting/transcribing -> failed in BOTH DB and manifest, queued untouched), serial_no_concurrency (3 enqueued jobs, semaphore-based FakeAdapter that asserts >1 concurrent would fail), watchdog_stale (fake old last_heartbeat, call run_watchdog one tick, assert mark_stale fires; terminal job -> short-circuit; QUEUED job -> NOT swept), atomic_claim_two_workers (Fix 6 — two concurrent pull_next on the same queued job, only one wins), hybrid_wakeup_no_signal (Fix 1 — enqueue while worker is waiting, assert poll timeout catches it). All xfail until Task 2/3/4 land.

    RED state expected: new tests fail on missing imports (queue, interrupt). Commit as `test(04-02): add wave-0 test scaffolding for queue/cancel/sweep/atomic-claim/hybrid-wakeup`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_cancel.py tests/test_orchestrator.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py contains `fake_stt` fixture reusing tests/_stt_fake.py::FakeAdapter and async TestClient that does not start worker (run_worker=False already set by 04-01's tmp_data_dir)
    - tests/test_cancel.py contains test_cancel_queued, test_cancel_running, test_cancel_terminal
    - tests/test_orchestrator.py contains test_restart_rejoin_boot, test_boot_interrupted_sweep, test_serial_no_concurrency, test_watchdog_stale, test_atomic_claim_two_workers (Fix 6), test_hybrid_wakeup_no_signal (Fix 1) (ADDED to existing 04-01 tests, not replacing)
    - `python -m pytest tests/test_cancel.py tests/test_orchestrator.py --collect-only -q` exits 0 (collection succeeds)
    - New tests xfail/skip until Tasks 2-4 land
  </acceptance_criteria>
  <done>Test scaffolding collects cleanly; xfail markers applied; conftest fixtures reuse 04-01's FakeAdapter and run_worker=False setting; atomic-claim and hybrid-wakeup tests added (Fix 1 + Fix 6).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SQLite-backed FIFO queue (app/jobs/queue.py) with atomic claim (Fix 6) + hybrid Event+poll wakeup (Fix 1) + boot interrupted sweep (app/jobs/interrupt.py) updating DB + manifest</name>
  <read_first>
    - app/jobs/cleanup.py (mark_failed(session, job_id, error)->bool — NOTE: no settings param; _TERMINAL_STATUSES; cancel_job(session, settings, job_id)->bool)
    - app/jobs/reconcile.py (reconcile_all(settings, session_factory) — queue + sweep run AFTER this)
    - app/jobs/manifest.py (read update_stage signature, STAGE_ORDER, and stage_to_status; update_stage is the PRIMARY path for mark_interrupted_failed's manifest update — confirm whether 'failed' is an accepted stage name; also read_manifest for the existing manifest shape)
    - app/db/models.py or app/db/schema.py (Job columns: id, status, created_at, updated_at, error, source_name)
    - app/jobs/orchestrator.py (04-01: run_job(settings, session_factory, job_id, bus=None, adapter=None)->None, _running: dict[str, threading.Event], JobCancelled in app/jobs/errors.py, heartbeat + progress.json writes)
    - app/models/settings.py (Settings.run_worker: bool = True — PROVIDED BY 04-01, consume only, do NOT re-add)
    - app/storage/fs.py (job_dir(settings, job_id) for rmtree target)
    - 04-REVIEWS.md Fix 1 (hybrid Event + poll), Fix 6 (atomic claim), MEDIUM (mark_interrupted_failed manifest update, enqueue status guards, exclude queued from watchdog)
    - 04-RESEARCH.md § Pattern 4 (hybrid Event + poll worker loop)
  </read_first>
  <files>app/jobs/queue.py, app/jobs/interrupt.py</files>
  <behavior>
    - enqueue(job_id, session): status-aware guard — only UPDATE rows WHERE status IN ('created','queued') (Codex MEDIUM — do NOT re-queue terminal or active jobs); then _work_signal.set()
    - pull_next(session) -> job_id | None: ATOMIC CLAIM (Fix 6) — `UPDATE jobs SET status='starting', updated_at=:now WHERE id=:id AND status='queued'` then check rowcount; if rowcount==1 return the id, else (another worker claimed it or status changed) return None and re-poll. Use `SELECT id FROM jobs WHERE status='queued' ORDER BY created_at LIMIT 1` first to find a candidate, then the conditional UPDATE to claim it.
    - run_worker(settings, session_factory, bus): single asyncio task; guard `if not settings.run_worker: return`; loop: pull_next -> if None: HYBRID WAKEUP (Fix 1) `await asyncio.wait_for(_work_signal.wait(), timeout=2.0)` then `_work_signal.clear()` (catch asyncio.TimeoutError and continue — the poll fallback); else `await orchestrator.run_job(settings, session_factory, job_id, bus=bus)` -> loop. The 2s poll ensures a missed signal (enqueue fired before the worker awaited) does not stall the queue.
    - mark_interrupted_failed(session, settings, session_factory) -> int: SELECT ids WHERE status IN ('ingesting','transcribing') (NOT 'queued'); for each: call mark_failed(session, id, "interrupted (backend restarted)") AND update the manifest to 'failed' stage with the same error (Codex MEDIUM — so reconcile_all on the next boot does not revert). mark_failed keeps the folder (no rmtree — source data may be intact); return count.
    - settings.run_worker=False -> run_worker returns immediately (tests drive manually)
  </behavior>
  <action>
    app/jobs/queue.py: async module. Use AsyncSession (session-per-operation, await DB directly — no thread offload for DB; the project already uses SQLAlchemy async + aiosqlite per Phase 1). Maintain module-level `asyncio.Event` `_work_signal`. 

    `enqueue(job_id, session)`: status-aware guard (Codex MEDIUM): `await session.execute(text("UPDATE jobs SET status='queued', updated_at=:now WHERE id=:id AND status IN ('created','queued')"), {...})` — only re-queues rows in a valid pre-active state; rows in terminal or active states are untouched. `await session.commit(); _work_signal.set()`.

    `pull_next(session) -> str | None` (Fix 6 — atomic claim): FIRST `SELECT id FROM jobs WHERE status='queued' ORDER BY created_at LIMIT 1` to find a candidate. If no candidate, return None. THEN claim it conditionally: `result = await session.execute(text("UPDATE jobs SET status='starting', updated_at=:now WHERE id=:id AND status='queued'"), {"id": candidate_id, "now": iso_now})` and check `result.rowcount`. If `rowcount == 1`, commit and return the candidate_id (this worker won the claim). If `rowcount == 0`, another worker (or a cancel) changed the status between SELECT and UPDATE — return None and let the loop re-poll. This prevents two workers running the same job (Fix 6 — Codex HIGH). The 'starting' status is a transient claim state; 04-01's run_job will immediately transition it via update_stage to the real ingesting/transcribing stage.

    `run_worker(settings, session_factory, bus=None)`: guard `if not settings.run_worker: return`; loop: `async with session_factory() as session: job_id = await pull_next(session)`; if None: HYBRID WAKEUP (Fix 1): `try: await asyncio.wait_for(_work_signal.wait(), timeout=2.0); except asyncio.TimeoutError: pass; _work_signal.clear(); continue`; else `await orchestrator.run_job(settings, session_factory, job_id, bus=bus)` (04-01 contract — `run_job(settings, session_factory, job_id, bus, adapter)` per 04-01-PLAN.md). Worker=1 strict serial (D-10): single asyncio task, NO asyncio.gather of multiple jobs. Import `from app.jobs.orchestrator import run_job` and `from app.models.settings import Settings` (Settings.run_worker already exists from 04-01).

    app/jobs/interrupt.py: `async def mark_interrupted_failed(session, settings, session_factory) -> int`: `SELECT id FROM jobs WHERE status IN ('ingesting','transcribing')` (NOT 'queued' — D-03: queued re-join FIFO, active interrupted -> failed, no auto-resume). For each id: (1) `await mark_failed(session, id, "interrupted (backend restarted)")` (reuse cleanup.mark_failed(session, job_id, error) — signature is (session, job_id, error), NO settings arg); (2) update the manifest to failed stage so reconcile_all does not revert (Codex MEDIUM). Commit to a SINGLE deterministic primary path: `from app.jobs.manifest import update_stage; from app.models.job import ManifestPatch; await update_stage(settings, session, id, "failed", ManifestPatch(error="interrupted (backend restarted)"))`. The executor MUST first read `app/jobs/manifest.py` (in read_first above) and verify `update_stage`, `STAGE_ORDER`, and `stage_to_status` accept `'failed'` as a stage name. If — and only if — `update_stage` rejects `'failed'` (raises or maps it incorrectly per `stage_to_status`), fall back to `atomic_write_json(manifest_path, {'stage':'failed','error':'interrupted (backend restarted)', ...existing manifest fields...})` directly, reading `app/jobs/manifest.py` + `app/storage/atomic.py` first. Do NOT leave this as a vague either/or — commit to the `update_stage` path as primary and use the atomic_write_json fallback only when the read-first verification proves `update_stage` rejects `'failed'`. mark_failed keeps the folder — do NOT rmtree source, source data may be intact. Return count. MUST be called AFTER reconcile_all and BEFORE run_worker (enforced in Task 4 lifespan wiring). Import `from app.jobs.cleanup import mark_failed` and `from app.jobs.manifest import update_stage`.

    Do NOT add run_worker to Settings — 04-01 already added it. Import Settings.run_worker for the guard.

    Commit: `feat(04-02): add SQLite FIFO worker with atomic claim + hybrid wakeup + boot interrupted sweep (DB+manifest)`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_orchestrator.py::test_restart_rejoin_boot tests/test_orchestrator.py::test_boot_interrupted_sweep tests/test_orchestrator.py::test_serial_no_concurrency tests/test_orchestrator.py::test_atomic_claim_two_workers tests/test_orchestrator.py::test_hybrid_wakeup_no_signal -x</automated>
  </verify>
  <acceptance_criteria>
    - app/jobs/queue.py contains `async def run_worker(`, `async def enqueue(`, `async def pull_next(`
    - app/jobs/queue.py pull_next uses a conditional UPDATE with `WHERE id=:id AND status='queued'` and checks `result.rowcount` (Fix 6 — atomic claim)
    - app/jobs/queue.py run_worker uses `asyncio.wait_for(_work_signal.wait(), timeout=2.0)` with `asyncio.TimeoutError` handling (Fix 1 — hybrid Event + poll)
    - app/jobs/queue.py guards `if not settings.run_worker: return` at top of run_worker
    - app/jobs/queue.py uses asyncio.Event `_work_signal` signaled in enqueue, awaited in run_worker when pull_next is None
    - app/jobs/queue.py pulls `ORDER BY created_at` (strict FIFO, D-10)
    - app/jobs/queue.py enqueue guards status with `status IN ('created','queued')` (Codex MEDIUM — no re-queueing terminal/active)
    - app/jobs/queue.py does NOT use asyncio.gather for multiple jobs (grep `gather` in queue.py matches nothing or only teardown-cancellation in Task 3)
    - app/jobs/queue.py imports `from app.jobs.orchestrator import run_job` (04-01 contract, no scope overlap)
    - app/jobs/interrupt.py contains `async def mark_interrupted_failed(`
    - app/jobs/interrupt.py selects `status IN ('ingesting','transcribing')` — NO 'queued'
    - app/jobs/interrupt.py calls `mark_failed(session, id, "interrupted (backend restarted)")` AND updates the manifest to failed stage (Codex MEDIUM — DB + manifest)
    - DB status is `failed` AND manifest stage is `failed` for interrupted jobs (test_boot_interrupted_sweep asserts both — Codex MEDIUM closure)
    - app/jobs/interrupt.py does NOT call rmtree (source preserved)
    - pytest tests/test_orchestrator.py::test_restart_rejoin_boot, test_boot_interrupted_sweep, test_serial_no_concurrency, test_atomic_claim_two_workers, test_hybrid_wakeup_no_signal exit 0
  </acceptance_criteria>
  <done>Worker loop pulls FIFO-ordered queued jobs strictly serially via 04-01 run_job with atomic claiming (Fix 6) and hybrid Event+poll wakeup (Fix 1); boot sweep marks only ingesting/transcribing jobs as failed with interrupted error in BOTH DB and manifest (Codex MEDIUM) preserving source; enqueue guards against re-queueing terminal/active rows; run_worker flag (from 04-01) gates auto-start.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cooperative cancel (queued/running/terminal) + stale-sweep watchdog excluding queued</name>
  <read_first>
    - app/jobs/cleanup.py (cancel_job(session, settings, job_id)->bool DB-first+rmtree; mark_stale(session, settings, job_id, threshold_s=600)->tuple[bool,bool] status-aware gate; is_stale(settings, job_id, threshold_s=600)->bool; _TERMINAL_STATUSES=frozenset({"done","failed","cancelled"}))
    - app/jobs/queue.py (Task 2 — _work_signal, run_worker, enqueue, pull_next with atomic claim)
    - app/jobs/orchestrator.py (04-01: `_running: dict[str, threading.Event]` module-level registry; JobCancelled in app/jobs/errors.py; run_job sets _running[job_id]=cancel_flag in try/finally pop; graceful in-flight shutdown via asyncio.wait_for(future, timeout=30.0))
    - app/jobs/manifest.py + app/storage/atomic.py (atomic_write_json fires ONLY after transcribe returns — confirm no partial transcript on cancel)
    - app/storage/fs.py (job_dir(settings, job_id) for rmtree; transcript_path for no-partial check)
    - 04-REVIEWS.md MEDIUM (exclude queued from stale watchdog — Codex)
  </read_first>
  <files>app/jobs/queue.py, tests/test_cancel.py</files>
  <behavior>
    - cancel(job_id, session, settings) -> dict: SELECT row; if status in _TERMINAL_STATUSES -> return row unchanged (no-op, D-06 idempotent)
    - cancel queued: await cancel_job(session, settings, job_id) (DB-first + rmtree via cleanup) + _work_signal.set() (in case worker just pulled it); the atomic claim in pull_next prevents a race where the worker already claimed it (the conditional UPDATE would have set status='starting', so the queued-cancel path is safe)
    - cancel running (ingesting/transcribing): import _running from orchestrator; flag = _running.get(job_id); if flag: flag.set() (threading.Event); orchestrator chunker stops after current chunk via 04-01's cancel_flag check; orchestrator's JobCancelled path calls cancel_job itself (04-01 contract) — do NOT double-rmtree; if flag is None (race: job just finished), treat as terminal no-op. Return final row.
    - watchdog tick: iterate jobs WHERE status IN ('ingesting','transcribing') (NOT 'queued' — Codex MEDIUM); call is_stale(settings, job_id) -> if True await mark_stale(session, settings, job_id) (reuse cleanup; status-aware short-circuits terminal per D-11). 04-01's heartbeat keeps active transcribing jobs fresh (Fix 2).
    - run_watchdog: asyncio task; loop asyncio.sleep(60) then tick; guard with settings.run_worker
  </behavior>
  <action>
    Extend app/jobs/queue.py with `async def cancel(job_id, session, settings) -> dict`: (1) `SELECT status FROM jobs WHERE id=:id` -> row; if None return {} (caller maps 404); (2) if `status in _TERMINAL_STATUSES` (import `from app.jobs.cleanup import cancel_job, mark_stale, is_stale, _TERMINAL_STATUSES`) -> return row, no-op (D-06); (3) if `status == 'queued'`: `await cancel_job(session, settings, job_id)` (DB-first + rmtree via cleanup) + `_work_signal.set()` (in case worker just pulled it — the atomic claim in pull_next means if the worker already claimed it, the status would be 'starting' not 'queued', so this path is safe); (4) if `status in ('starting','ingesting','transcribing')`: `from app.jobs.orchestrator import _running; flag = _running.get(job_id)`; if flag is not None: `flag.set()` (threading.Event — 04-01 chunker checks at loop top and raises JobCancelled; 04-01's run_job JobCancelled path calls cancel_job + rmtree itself — do NOT call cancel_job here, that would double-rmtree); if flag is None (race: job finished between SELECT and now) -> re-SELECT; if now terminal, return row no-op; else fall through. Return final row via re-SELECT.

    Add `async def run_watchdog(settings, session_factory) -> None`: guard `if not settings.run_worker: return`; loop: `await asyncio.sleep(60)`; `async with session_factory() as session:` iterate job ids `SELECT id FROM jobs WHERE status IN ('ingesting','transcribing')` (NOT 'queued' — Codex MEDIUM; queued jobs are not active and 04-01's heartbeat only fires for active transcribing jobs, so excluding queued avoids false-stale on legitimately-waiting jobs); for each `if is_stale(settings, job_id): await mark_stale(session, settings, job_id)` (reuse cleanup; mark_stale has its own status-aware gate — double safety). Single asyncio task. Cancellable via asyncio (Task 4 cancels on teardown).

    Commit: `feat(04-02): add cooperative cancel + stale-sweep watchdog (excludes queued)`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_cancel.py tests/test_orchestrator.py::test_watchdog_stale -x</automated>
  </verify>
  <acceptance_criteria>
    - app/jobs/queue.py contains `async def cancel(` and `async def run_watchdog(`
    - cancel checks `_TERMINAL_STATUSES` first and returns row unchanged for terminal jobs
    - cancel for 'queued' calls cancel_job(session, settings, job_id) + _work_signal.set()
    - cancel for running imports `from app.jobs.orchestrator import _running` and calls `_running.get(job_id)` then `.set()` (threading.Event); does NOT double-call cancel_job (04-01 run_job JobCancelled path handles that)
    - cancel handles the race where _running has no flag (job just finished) by re-selecting and treating terminal as no-op
    - run_watchdog sleeps 60s between ticks
    - run_watchdog SELECT uses `status IN ('ingesting','transcribing')` — NO 'queued' (Codex MEDIUM — Fix verified by grep)
    - run_watchdog calls is_stale + mark_stale (reused from cleanup), guards with settings.run_worker
    - tests/test_cancel.py all 3 (queued/running/terminal) pass
    - tests/test_orchestrator.py::test_watchdog_stale passes
  </acceptance_criteria>
  <done>Cancel is idempotent across queued/running/terminal; running cancel sets 04-01 threading.Event cancel_flag and lets orchestrator's JobCancelled path do the cleanup (no double-rmtree); watchdog marks stale active (ingesting/transcribing) jobs every 60s, excludes queued (Codex MEDIUM); 04-01 heartbeat keeps active transcribing jobs fresh (Fix 2); no partial transcript left on cancel (atomic_write only at end).</done>
</task>

<task type="auto">
  <name>Task 4: Lifespan wiring — sweep + worker + watchdog with ordered teardown + app.state establishment</name>
  <read_first>
    - app/main.py (lifespan: reconcile_all at ~main.py:202-214, print("ready") ~main.py:219, engine.dispose() ~main.py:238; session_factory in scope)
    - app/jobs/interrupt.py (Task 2 mark_interrupted_failed(session, settings, session_factory)->int)
    - app/jobs/queue.py (Task 2+3 run_worker(settings, session_factory, bus), run_watchdog(settings, session_factory))
    - app/jobs/reconcile.py (reconcile_all(settings, session_factory) — sweep runs AFTER this)
    - app/jobs/progress.py (04-01 EventBus — worker needs a bus instance to pass to run_job; 04-03 WS handler accesses it via app.state.bus)
    - 04-REVIEWS.md Fix 3 (graceful in-flight shutdown — 04-01 handles the future await; 04-02 teardown just cancels the worker task which lets 04-01's finally block run), Fix 7-partial (app.state.settings + app.state.session_factory — 04-03 also needs these)
  </read_first>
  <files>app/main.py</files>
  <action>
    In app/main.py lifespan, AFTER `await reconcile_module.reconcile_all(settings, session_factory)` (~main.py:202-214) and BEFORE `print(f"TranscriptionAndNotes backend ready..."` (~main.py:219): (1) `from app.jobs.interrupt import mark_interrupted_failed` then `async with session_factory() as session: swept = await mark_interrupted_failed(session, settings, session_factory)` and log `swept` count (D-03 boot sweep — runs AFTER reconcile_all, BEFORE worker); (2) `from app.jobs.queue import run_worker, run_watchdog` and `from app.jobs.progress import EventBus`; create a module-level/app-state EventBus instance `bus = EventBus()`; (3) ESTABLISH app.state (Fix 7-partial — 04-03 WS handler also needs these): `app.state.bus = bus; app.state.settings = settings; app.state.session_factory = session_factory` (set these explicitly even if 04-03 also sets them — belt-and-suspenders; if they are already set by an earlier phase, verify and do not duplicate, but they are NOT currently established per Codex HIGH); (4) `worker_task = asyncio.create_task(run_worker(settings, session_factory, bus=bus))` if `settings.run_worker`; (5) `watchdog_task = asyncio.create_task(run_watchdog(settings, session_factory))` if `settings.run_worker`. Store worker_task + watchdog_task on app state (or module-level refs) for teardown.

    In lifespan teardown (the `finally:` block at ~main.py:223-238): cancel worker_task + watchdog_task BEFORE `await engine.dispose()` (~main.py:238). Insert, before the existing model unload block: `worker_task.cancel(); await asyncio.gather(worker_task, return_exceptions=True)`; `watchdog_task.cancel(); await asyncio.gather(watchdog_task, return_exceptions=True)` (guard with hasattr/None checks for the case where lifespan failed before creating them). The worker task cancellation causes run_worker to exit its loop; if a run_job is in-flight, 04-01's graceful shutdown path (asyncio.wait_for(future, timeout=30.0) + cancel_flag.set()) handles the sync thread exit before model unload — confirm by reading the 04-01 orchestrator finally block. THEN proceed to the existing model unload + `await engine.dispose()`. This guarantees no DB access after engine closed and no use-after-free on the model.

    Do NOT change reconcile_all ordering. Insert strictly between reconcile_all and ready. Per D-03: sweep runs AFTER reconcile_all (which fixes DB/source divergence) and BEFORE worker (so worker doesn't pick up jobs that should be failed). Guard task creation with `if settings.run_worker:` so tests with run_worker=False skip auto-start.

    Commit: `feat(04-02): wire sweep+worker+watchdog into lifespan with ordered teardown + app.state`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_orchestrator.py -x && python -c "import ast; ast.parse(open('app/main.py').read()); print('main.py parses')"</automated>
  </verify>
  <acceptance_criteria>
    - app/main.py lifespan calls `mark_interrupted_failed` after `reconcile_all` and before `print("ready")`
    - app/main.py creates worker_task and watchdog_task via asyncio.create_task guarded by `if settings.run_worker:`
    - app/main.py establishes `app.state.bus`, `app.state.settings`, `app.state.session_factory` (Fix 7-partial)
    - app/main.py teardown cancels worker_task and watchdog_task (with return_exceptions=True) BEFORE `await engine.dispose()`
    - app/main.py passes a shared EventBus instance to run_worker (so WS subscribers in 04-03 see events)
    - `python -c "import ast; ast.parse(open('app/main.py').read())"` exits 0
    - tests/test_orchestrator.py full suite passes
  </acceptance_criteria>
  <done>Lifespan wires reconcile_all -> mark_interrupted_failed -> run_worker -> run_watchdog in correct order with deterministic teardown that cancels tasks (return_exceptions=True) before engine disposal; app.state.bus/settings/session_factory established (Fix 7-partial); 04-01's graceful in-flight shutdown ensures the sync transcription thread exits before model unload.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- app/jobs/queue.py — run_worker (asyncio single-worker loop with hybrid Event+poll wakeup Fix 1), enqueue (status-aware guard), pull_next (atomic claim Fix 6), cancel, run_watchdog (excludes queued — Codex MEDIUM)
- app/jobs/interrupt.py — mark_interrupted_failed (boot sweep updating BOTH DB and manifest — Codex MEDIUM)
- app/main.py — lifespan wiring (sweep + worker + watchdog with ordered teardown) + app.state.bus/settings/session_factory (Fix 7-partial)
- tests/conftest.py — fake_stt, run_worker_off, non-auto-start TestClient (extends 04-01's fixtures)
- tests/test_orchestrator.py — restart_rejoin_boot, boot_interrupted_sweep, serial_no_concurrency, watchdog_stale, atomic_claim_two_workers (Fix 6), hybrid_wakeup_no_signal (Fix 1) (added to 04-01's existing tests)
- tests/test_cancel.py — queued/running/terminal cancel

CONSUMED FROM 04-01 (NOT re-created): app/jobs/errors.py (JobCancelled), app/jobs/orchestrator.py (run_job with infer_resume_point + functools.partial + graceful in-flight shutdown + heartbeat + progress.json, _running registry), app/jobs/progress.py (EventBus), app/models/stt/protocol.py + adapter.py + chunker.py (progress_cb/cancel_flag + FasterWhisperAdapter update), app/models/settings.py (Settings.run_worker — 04-02 imports and consumes, does NOT add), tests/_stt_fake.py (FakeAdapter already honors progress_cb/cancel_flag).

EXCLUDED (04-01 scope, do NOT duplicate): orchestrator.py state-machine driver, errors.py JobCancelled, progress.py EventBus, STTAdapter progress_cb/cancel_flag extension, FasterWhisperAdapter update, Settings.run_worker field, heartbeat + progress.json writes.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| boot → DB + manifest | restart sweep reads/writes job rows AND manifests based on status; wrong scope corrupts state |
| worker → DB | single asyncio worker pulls + runs; concurrency violation breaks D-10 |
| pull_next atomic claim → DB | conditional UPDATE prevents duplicate execution (Fix 6) |
| cancel (asyncio) → running job (thread) | cancel_flag threading.Event crosses asyncio/thread boundary; race with terminal commit |
| watchdog → DB | periodic mark_stale; mis-scope marks terminal or queued jobs stale |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering/Race | cancel + terminal commit | mitigate | DB-first cancel_job; check `_TERMINAL_STATUSES` first; cancel terminal = no-op returning current row; running cancel sets 04-01 threading.Event and lets run_job's JobCancelled path do cancel_job (no double-rmtree) (D-06) |
| T-04-05 | Spoofing/Tampering | boot sweep marks wrong jobs | mitigate | "active" = status IN ('ingesting','transcribing') only (NOT 'queued'); runs AFTER reconcile_all, BEFORE worker (D-03); updates BOTH DB and manifest so reconcile_all does not revert (Codex MEDIUM) |
| T-04-06 | Information Disclosure | cancel leaves partial transcript | mitigate | atomic_write_json fires only after transcribe returns (04-01 Pitfall 4) -> nothing partial on cancel; 04-01 run_job JobCancelled path does NOT write transcript.json; rmtree on cancel; verify no transcript.json left |
| T-04-07 | DoS | watchdog marks terminal/queued jobs stale | mitigate | reuse is_stale/mark_stale — mark_stale has status-aware gate short-circuiting terminal (D-11); watchdog SELECT filters to `status IN ('ingesting','transcribing')` — excludes queued (Codex MEDIUM); 04-01 heartbeat keeps active transcribing fresh (Fix 2) |
| T-04-08 | Tampering | worker concurrency violation (D-10) | mitigate | single asyncio task, no asyncio.gather of multiple jobs, strict FIFO ORDER BY created_at; one run_job at a time |
| T-04-09 | Race | cancel sets _running flag but job just finished | mitigate | if _running.get(job_id) is None, re-SELECT status; if now terminal, return row no-op; else fall through |
| T-04-10 | Tampering/Race | two workers pull the same queued job | mitigate | atomic claim via conditional UPDATE ... WHERE id=:id AND status='queued' with rowcount check (Fix 6 — Codex HIGH); only the worker with rowcount==1 proceeds |
| T-04-11 | Denial of Service | missed _work_signal stalls the queue | mitigate | hybrid Event + poll wakeup: asyncio.wait_for(_work_signal.wait(), timeout=2.0) with TimeoutError catch + continue (Fix 1 — both reviewers); a missed signal self-heals within 2s |
| T-04-12 | Tampering | enqueue re-queues terminal or active jobs | mitigate | status-aware guard in enqueue: UPDATE ... WHERE id=:id AND status IN ('created','queued') (Codex MEDIUM); rows in terminal/active states are untouched |
| T-04-SC | Tampering | pip installs (aiosqlite already in project per Phase 1) | accept | aiosqlite is an existing project dependency (Phase 1 stack); no new package install in 04-02 |
</threat_model>

<verification>
- `python -m pytest tests/test_cancel.py tests/test_orchestrator.py -x` — all green
- `python -c "import ast; ast.parse(open('app/main.py').read())"` — main.py parses
- Boot sequence: reconcile_all -> mark_interrupted_failed -> run_worker -> run_watchdog (verifiable via lifespan order in app/main.py)
- Teardown: worker_task.cancel() + watchdog_task.cancel() (return_exceptions=True) BEFORE engine.dispose()
- `grep -n "run_worker" app/models/settings.py` shows Settings.run_worker flag (provided by 04-01, consumed by 04-02)
- `grep -n "gather" app/jobs/queue.py` matches only teardown-cancellation (run_watchdog/teardown), NOT concurrent job execution
- `grep -n "status='queued'" app/jobs/queue.py` matches in pull_next atomic claim (Fix 6)
- `grep -n "wait_for.*_work_signal" app/jobs/queue.py` matches in run_worker (Fix 1)
- `grep -n "IN ('ingesting','transcribing')" app/jobs/interrupt.py app/jobs/queue.py` matches in both (sweep + watchdog)
- No scope overlap: `grep -n "class EventBus\|def run_job\|progress_cb\|class JobCancelled\|heartbeat\|progress.json" app/jobs/queue.py app/jobs/interrupt.py` matches nothing (04-01 owns those)
- No double-rmtree: `grep -n "cancel_job" app/jobs/queue.py` matches only the queued-cancel path (running path sets _running flag, does NOT call cancel_job)
- app.state established: `grep -n "app.state.bus\|app.state.settings\|app.state.session_factory" app/main.py` matches (Fix 7-partial)
</verification>

<success_criteria>
- SC-2 restart re-join: queued jobs re-join FIFO on boot (test_restart_rejoin_boot green)
- SC-4 idempotent cancel + deterministic cleanup: queued/running/terminal cancel all behave correctly (test_cancel.py green); no partial transcript
- D-02 no mid-transcription resume: reuse infer_resume_point; no new checkpointing
- D-03 boot sweep: ingesting/transcribing -> failed with "interrupted (backend restarted)" in BOTH DB and manifest (Codex MEDIUM); queued NOT swept (source preserved)
- D-06 cancel idempotent; terminal = no-op returning row; running = set threading.Event, let 04-01 JobCancelled path clean up
- D-10 worker=1 strict serial (test_serial_no_concurrency green)
- D-11 watchdog 60s cadence, status-aware, excludes queued (Codex MEDIUM); 04-01 heartbeat keeps active transcribing fresh (Fix 2)
- Fix 1 hybrid Event+poll wakeup prevents missed-signal stalls (test_hybrid_wakeup_no_signal green)
- Fix 6 atomic claim prevents duplicate execution (test_atomic_claim_two_workers green)
- Fix 7-partial app.state.bus/settings/session_factory established in lifespan (04-03 WS handler relies on these)
- Lifespan wiring correct with ordered teardown (worker+watchdog cancelled before engine.dispose; 04-01 graceful in-flight shutdown handles the sync thread)
- Zero scope overlap with 04-01 (no re-creation of orchestrator/EventBus/STTAdapter/JobCancelled/heartbeat/progress.json/run_worker field)
</success_criteria>

<output>
Create `.planning/phases/04-job-orchestrator-persistent-queue-websocket-progress/04-02-SUMMARY.md` when done
</output>