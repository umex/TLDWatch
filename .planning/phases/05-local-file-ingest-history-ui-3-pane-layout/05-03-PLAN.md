---
phase: 05-local-file-ingest-history-ui-3-pane-layout
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - web/src/hooks/useScrollSpy.ts
  - web/src/hooks/useScrollSpy.test.ts
  - web/src/components/TranscriptPane.tsx
  - web/src/components/ActiveJobCard.tsx
  - web/src/components/HistoryList.tsx
  - web/src/api/jobs.ts
autonomous: true
requirements: [UI-03, JOB-03, UI-01]
must_haves:
  truths:
    - "The transcript segment row nearest the viewport center is highlighted as the user scrolls (UI-03, D-09, local files only)"
    - "Clicking a completed history row opens /jobs/:id and loads that job's transcript via GET /jobs/{id}/transcript (JOB-03 re-open, D-06, D-14)"
    - "When an active job card receives a terminal WS event (done/failed/cancelled), the card is removed and the history list refetches so the job appears in the completed list (D-03, UI-SPEC §2 terminal transition)"
    - "The full back-end + front-end test suites are green end-to-end (the vertical slice is provable)"
  artifacts:
    - path: "web/src/hooks/useScrollSpy.ts"
      provides: "IntersectionObserver scroll-spy hook (rootMargin -49% 0px -49% 0px + pixel-offset fallback)"
      contains: "IntersectionObserver"
    - path: "web/src/hooks/useScrollSpy.test.ts"
      provides: "Unit test: mock IntersectionObserver, assert activeId updates"
    - path: "web/src/components/TranscriptPane.tsx"
      provides: "Transcript pane with scroll-spy wired (active row highlighted)"
  key_links:
    - from: "web/src/components/TranscriptPane.tsx"
      to: "web/src/hooks/useScrollSpy.ts"
      via: "useScrollSpy(containerRef, rowIds) -> activeId -> TranscriptRow active prop"
      pattern: "useScrollSpy"
    - from: "web/src/components/ActiveJobCard.tsx"
      to: "web/src/components/HistoryList.tsx"
      via: "terminal WS event -> queryClient.invalidateQueries(['jobs'])"
      pattern: "invalidateQueries"
    - from: "web/src/pages/DetailPage.tsx"
      to: "web/src/api/jobs.ts::useTranscript"
      via: "GET /jobs/{id}/transcript"
      pattern: "useTranscript"
---

<objective>
Wire the scroll-spy active-line highlight (UI-03), finalize the history-page terminal-transition flow (D-03) and the re-open-loads-transcript flow (JOB-03, D-06, D-14), and verify the full back-end + front-end vertical slice end-to-end. This plan depends on 05-01 (back-end upload + transcript endpoint) and 05-02 (FE scaffold + shell + components) and stitches them together.

Purpose: This is the integration + polish wave. Plan 05-01 built the back-end routes; plan 05-02 built the FE scaffold + shell against the documented contract. This plan wires the last FE behavior (scroll-spy), locks the terminal-transition refetch + re-open flow, and runs the full combined suite to prove the vertical slice (drop file -> upload -> active card -> history -> re-open -> transcript) is end-to-end functional.

Output: `useScrollSpy.ts` + test, the wired TranscriptPane, the finalized ActiveJobCard -> HistoryList terminal transition, and a green full back-end + FE suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-CONTEXT.md
@.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-RESEARCH.md
@.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-PATTERNS.md
@.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-UI-SPEC.md
@.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-VALIDATION.md
@.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-01-SUMMARY.md
@.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-02-SUMMARY.md
@web/src/components/TranscriptPane.tsx
@web/src/components/TranscriptRow.tsx
@web/src/components/ActiveJobCard.tsx
@web/src/components/HistoryList.tsx
@web/src/api/jobs.ts
@web/src/styles.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useScrollSpy hook + test + wire into TranscriptPane (UI-03)</name>
  <files>web/src/hooks/useScrollSpy.ts, web/src/hooks/useScrollSpy.test.ts, web/src/components/TranscriptPane.tsx</files>
  <read_first>
    - web/src/components/TranscriptPane.tsx (created in 05-02 — renders seg-{index} rows; this task wires the active highlight)
    - web/src/components/TranscriptRow.tsx (accepts an `active` boolean prop from 05-02)
    - web/src/styles.css (the .transcript-row.active class from 05-02: 4px left #2563EB border + rgba(37,99,235,0.05) tint)
    - web/src/test/setup.ts (the mock IntersectionObserver from 05-02 — the test triggers it)
    - .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-RESEARCH.md (Pattern 4 — full useScrollSpy implementation: IntersectionObserver with rootMargin "-49% 0px -49% 0px", threshold 0, pixel-offset fallback; Anti-Patterns — do NOT use threshold: 1.0; do NOT do manual scroll-event math)
    - .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-UI-SPEC.md (§3 Scroll-Spy — focal anchor 50% center, rootMargin "-49% 0px -49% 0px" 2% focal line, pixel-offset fallback, highlight styling 4px #2563EB border + rgba(37,99,235,0.05) tint)
    - .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-VALIDATION.md (Per-Task Verification Map row 05-03-02 — `cd web && npx vitest run src/hooks/useScrollSpy.test.ts` mock IntersectionObserver, assert activeId updates)
    - .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-PATTERNS.md (row: useScrollSpy.ts — RESEARCH Pattern 4 + UI-SPEC §3)
  </read_first>
  <behavior>
    - useScrollSpy(containerRef, rowIds) returns the activeId (the id of the row nearest the viewport center).
    - When one or more rows intersect the 2% center focal line (rootMargin -49%/-49%), the last (lowest in DOM) intersecting row's id is the activeId.
    - When no row intersects (fast scroll / short transcripts), the pixel-offset fallback picks the row closest to the viewport center by getBoundingClientRect distance.
    - The hook cleans up the observer on unmount or when rowIds change.
    - useScrollSpy.test.ts mocks IntersectionObserver (from setup.ts), triggers intersection on a sample row, and asserts activeId updates to that row's id; also tests the fallback path (no intersections -> nearest-by-pixel).
    - TranscriptPane passes the activeId to each TranscriptRow's `active` prop so the .active class applies the highlight.
  </behavior>
  <action>
    Create web/src/hooks/useScrollSpy.ts per RESEARCH Pattern 4 verbatim (the reference implementation is in the research): `useScrollSpy(containerRef: RefObject<HTMLDivElement>, rowIds: string[])` returns `activeId: string | null`. Implementation: a single IntersectionObserver with `{ root: containerRef.current, rootMargin: "-49% 0px -49% 0px", threshold: 0 }`. On entries: filter isIntersecting, if any -> setActiveId(last intersecting target.id); else -> pixel-offset fallback (loop rowIds, getElementById, compute abs(rect.top + rect.height/2 - window.innerHeight/2), pick min). Observe each row id's element. Cleanup: observer.disconnect() in the effect cleanup. Re-run the effect when rowIds changes (dependency). Do NOT use threshold: 1.0 (RESEARCH Anti-Pattern). Do NOT use manual scroll-event math.

    Create web/src/hooks/useScrollSpy.test.ts: use the mock IntersectionObserver from web/src/test/setup.ts. Render a container with 3 row divs (id seg-0, seg-1, seg-2) via Testing Library. Call useScrollSpy with the container ref + ["seg-0","seg-1","seg-2"]. Trigger the mock observer's callback with a fake entry for seg-1 isIntersecting=true; assert activeId === "seg-1". Trigger with no intersections; assert the fallback picks the nearest-by-pixel (mock getBoundingClientRect to place seg-1 closest to center). This covers VALIDATION.md row 05-03-02 (UI-03).

    Update web/src/components/TranscriptPane.tsx (from 05-02): add a containerRef (useRef<HTMLDivElement>), call `const activeId = useScrollSpy(containerRef, segmentIds)` where segmentIds = segments.map((_, i) => `seg-${i}`). Pass `active={activeId === `seg-${index}`}` to each TranscriptRow. Attach the ref to the scrollable container div. The .transcript-row.active class (defined in 05-02's styles.css) applies the 4px #2563EB left border + rgba(37,99,235,0.05) tint (UI-SPEC §3).

    Per D-09, UI-03, UI-SPEC §3, RESEARCH Pattern 4.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/hooks/useScrollSpy.test.ts && cd web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "IntersectionObserver" web/src/hooks/useScrollSpy.ts` returns >= 1 (observer used).
    - `grep -c '"-49% 0px -49% 0px"' web/src/hooks/useScrollSpy.ts` returns >= 1 (UI-SPEC §3 focal-line rootMargin).
    - `grep -c "useScrollSpy" web/src/components/TranscriptPane.tsx` returns >= 1 (wired into the pane).
    - `grep -c "active=" web/src/components/TranscriptPane.tsx` returns >= 1 (active prop passed to rows).
    - `cd web && npx vitest run src/hooks/useScrollSpy.test.ts` exits 0 (UI-03 verified — mock IntersectionObserver, activeId updates).
    - `cd web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>useScrollSpy hook + test pass; TranscriptPane highlights the active row based on scroll position (UI-03, D-09, local files only).</done>
</task>

<task type="auto">
  <name>Task 2: Finalize terminal-transition refetch + re-open-loads-transcript flow + full back-end + FE suite green (JOB-03, UI-01)</name>
  <files>web/src/components/ActiveJobCard.tsx, web/src/components/HistoryList.tsx, web/src/api/jobs.ts</files>
  <read_first>
    - web/src/components/ActiveJobCard.tsx (from 05-02 — has the onTerminal callback placeholder; this task finalizes the invalidate)
    - web/src/components/HistoryList.tsx (from 05-02 — renders completed jobs; this task ensures the refetch-on-invalidate is wired)
    - web/src/api/jobs.ts (from 05-02 — useJobs/useTranscript + the invalidate helper; this task verifies the re-open flow)
    - web/src/pages/DetailPage.tsx (from 05-02 — calls useTranscript(id); this task confirms the 404 -> "Transcribing..." path)
    - .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-UI-SPEC.md (§2 terminal transition: fade-out + WebSocket close + prepend to history list; §5 click-through to /jobs/:id; §6 Transcript Not Ready 404/processing state)
    - .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-RESEARCH.md (Pattern 3 — invalidate ["jobs","done"] on terminal WS event)
    - .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-CONTEXT.md (D-03 terminal -> history; D-06 click -> load transcript; D-14 transcript endpoint)
    - .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-VALIDATION.md (Per-Task Verification Map rows 05-03-01 history list, 05-03-03 re-open; Sampling Rate — per wave merge run full back-end + FE suites)
    - .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-01-SUMMARY.md (the back-end endpoints now live: POST /jobs/upload, GET /jobs/{id}/transcript, the 'uploading' status)
    - .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-02-SUMMARY.md (the FE scaffold + shell + components now exist)
  </read_first>
  <action>
    Finalize web/src/components/ActiveJobCard.tsx (from 05-02): on a terminal WS event (type === "done" | "failed" | "cancelled"), call the onTerminal(jobId) callback that HistoryPage passes. HistoryPage's onTerminal handler calls `queryClient.invalidateQueries({ queryKey: ["jobs"] })` so the HistoryList refetches and the job appears in the completed list (D-03, UI-SPEC §2). Add a brief CSS fade-out transition (opacity 1 -> 0 over ~200ms) before the card unmounts — the .active-card class can carry the transition. Close the WebSocket (the useJobEvents cleanup already handles this on unmount). If the terminal state is "failed", show the soft red border + error copy from UI-SPEC §6 ("Failed to transcribe video. Please check your file format and try again.") before fade-out.

    Verify web/src/components/HistoryList.tsx (from 05-02): it consumes useJobs (terminal statuses), renders HistoryRow newest-first, and shows "No Transcripts Yet" + the UI-SPEC empty-state body when empty. Confirm clicking a HistoryRow navigates to /jobs/:id (React Router navigate). This is the JOB-03 history list + the re-open entry point (D-06).

    Verify web/src/api/jobs.ts (from 05-02): useTranscript(id) fetches GET /jobs/{id}/transcript; on 404 returns a "transcribing" sentinel (the DetailPage shows the "Transcribing..." loading animation per UI-SPEC §6); on 200 returns the parsed Transcript. Confirm the useJobs invalidation on terminal WS fires the refetch (TanStack Query background refetch on invalidate). This is the JOB-03 re-open flow (D-14).

    Run the full combined suite to prove the vertical slice end-to-end: back-end (`pytest` — all 42+ existing + 7 Phase 5 tests green) + front-end (`cd web && npx vitest run` — DetailPage.test, jobs.test, useScrollSpy.test all green) + type-check (`cd web && npx tsc --noEmit`) + the UI-02 lint gate (`grep -r "<video" web/src/` returns no matches) + the no-dangerouslySetInnerHTML gate.

    Per D-03, D-06, D-14, UI-SPEC §2/§5/§6, RESEARCH Pattern 3.
  </action>
  <verify>
    <automated>pytest && cd web && npx vitest run && cd web && npx tsc --noEmit && grep -r "<video" web/src/ || echo NO_VIDEO_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "invalidateQueries" web/src/components/ActiveJobCard.tsx` returns >= 1 OR the onTerminal callback in HistoryPage calls invalidate (terminal -> history refetch wired, D-03).
    - `grep -c "No Transcripts Yet" web/src/components/HistoryList.tsx` returns >= 1 (UI-SPEC empty state present).
    - `grep -c "navigate.*jobs\|Link.*jobs" web/src/components/HistoryRow.tsx` returns >= 1 (click-through to /jobs/:id, D-06).
    - `pytest` exits 0 (full back-end suite green — 42+ existing + 7 Phase 5 tests).
    - `cd web && npx vitest run` exits 0 (all FE tests green — DetailPage.test, jobs.test, useScrollSpy.test).
    - `cd web && npx tsc --noEmit` exits 0 (full FE type-check).
    - `grep -r "<video" web/src/` returns no matches (UI-02 preserved across the full FE).
    - `grep -r "dangerouslySetInnerHTML" web/src/` returns no matches (XSS mitigation T-05-07 preserved).
  </acceptance_criteria>
  <done>Terminal WS event -> history refetch wired; re-open loads transcript (404 -> "Transcribing..."); full back-end + FE suites green end-to-end; UI-02 no-video + no-dangerouslySetInnerHTML gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> GET /jobs, /ws/jobs/{id}/events, /jobs/{id}/transcript | FE reads + subscribes (localhost-only, inherited from 05-01/05-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering | Scroll-spy observer attached to a stale container | mitigate | useScrollSpy disconnects the observer in the effect cleanup; re-runs on rowIds change so a transcript swap (re-open a different job) re-observes the new rows. No stale-listener leak. |
| T-05-12 | Tampering | History refetch race on rapid terminal events | accept | TanStack Query deduplicates + invalidates idempotently; a double-invalidate just triggers one background refetch. Single-user local app; no abuse surface. |
| T-05-07 (inherited) | Tampering | XSS via transcript text | mitigate | Inherited from 05-02: React default escaping, no dangerouslySetInnerHTML. Re-asserted by the grep gate in this plan's verification. |

## Mitigation Traceability

- T-05-11 -> Task 1 action: "Cleanup: observer.disconnect() in the effect cleanup" + "Re-run the effect when rowIds changes".
- T-05-07 -> Task 2 acceptance criterion: "grep -r 'dangerouslySetInnerHTML' web/src/ returns no matches".
</threat_model>

<verification>
- `pytest` — full back-end suite green (42+ existing + 7 Phase 5 tests from 05-01).
- `cd web && npx vitest run` — all FE tests green (DetailPage.test, jobs.test, useScrollSpy.test from 05-02 + this plan).
- `cd web && npx tsc --noEmit` — full FE type-check clean.
- `cd web && npm run build` — Vite production build succeeds.
- `grep -r "<video" web/src/` returns no matches (UI-02 across the whole FE).
- `grep -r "dangerouslySetInnerHTML" web/src/` returns no matches (T-05-07).
- Manual (deferred to /gsd-verify-phase): drop a real file -> active card -> terminal -> history -> click row -> detail 2-pane + transcript + scroll-spy highlight; confirm the full vertical slice.
</verification>

<success_criteria>
- The active transcript line is highlighted based on scroll position (UI-03, D-09) — verified by useScrollSpy.test.
- Clicking a completed history row loads the transcript in the 2-pane detail view (JOB-03 re-open, D-06, D-14) — verified by the wired useTranscript + HistoryRow navigate.
- A terminal WS event moves the job from active cards to the history list (D-03, UI-SPEC §2) — verified by the invalidateQueries wiring.
- Full back-end + front-end suites green end-to-end (the vertical slice is provable) — verified by pytest + vitest + tsc + build.
- No <video> element anywhere (UI-02) — verified by the grep gate.
</success_criteria>

<output>
Create `.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Front-end symbols added/finalized by this plan (the plan-review-convergence source-grounding pass excludes these from drift verification):
- `web/src/hooks/useScrollSpy.ts` (`useScrollSpy(containerRef, rowIds) -> activeId`)
- `web/src/hooks/useScrollSpy.test.ts`
- Wired `web/src/components/TranscriptPane.tsx` (scroll-spy active highlight)
- Finalized `web/src/components/ActiveJobCard.tsx` (terminal WS event -> onTerminal -> invalidateQueries)
- Verified `web/src/components/HistoryList.tsx` + `web/src/components/HistoryRow.tsx` (click-through re-open)
- Verified `web/src/api/jobs.ts` (useTranscript 404 -> transcribing sentinel; useJobs invalidation)