---
phase: 05-local-file-ingest-history-ui-3-pane-layout
verified: 2026-06-27T20:25:00Z
status: human_needed
score: 23/23 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 20/20
  gaps_closed:
    - "UAT test 6 (MAJOR): ingesting-window ActiveJobCard silence -- 05-06's race-closure was built on a FALSE premise (assumed snapshot status:'starting' during model-load; live DB status is 'ingesting' for the entire model-load + transcribe window because the WS-only stage_changed(preparing|transcribing) events at orchestrator.py:260/:263 are NOT persisted, and 'transcribed'->'transcribing' lands only AFTER `await future` at orchestrator.py:282 then immediately 'done' at :299). Late-connecting card received snapshot{status:'ingesting'} -> isIngesting=true -> isTranscribingActive gated by !isIngesting -> false -> card rendered frozen 'Ingesting File... 0%' / mislabeled 'Ingesting File... X%' = perceived 'nothing is going on'. CLOSED by 05-08 (FE-only snapshot-authoritative state derivation for the ingesting window: isPreparing covers status==='ingesting' && !progressArrived; isTranscribingActive drops !isIngesting gate; snapshot handler seeds progressArrived from snapshot.percent + event.stage fallback; label render reordered so isPreparing/isTranscribingActive take priority over the guarded 'Ingesting File...' label). 3 new race-branch tests (a/b/c) added and PASS."
    - "WR-01 advisory from prior verification (reconnect mid-transcription with snapshot percent but no live progress shows Preparing instead of Transcribing X%) is now ADDRESSED by 05-08 truth 21 below (snapshot.percent seeds progressArrived.current in the snapshot handler) -- the reconnect-path edge case the prior verification flagged as non-blocking is now closed."
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Re-run UAT test 5 + UAT test 6 (combined gap-closure re-test) against FRESH back-end + Vite dev servers to confirm the 05-06 starting-window race fix + 05-07 duration fix + 05-08 ingesting-window race fix resolve all three live findings (test 4 'nothing going on', test 5 duration blank, test 6 ingesting-window silence)"
    expected: "Stop any stale back-end / Vite processes. Restart both with current HEAD (which includes 05-05's stage_changed(preparing) emission + 05-06 starting-snapshot fix + 05-07 duration_s population + 05-08 ingesting-snapshot fix). Hard-reload the browser (bypass cache). Drop a SHORT named clip via the drop zone. Watch the active card through model load + first chunk progress + completion. (05-08 ingesting-window fix) After upload completes the active card shows 'Preparing...' with an indeterminate moving-stripe bar -- NOT 'Ingesting File... 0%' / NOT 'In Queue' / NOT silence -- even when the WS connects after the orchestrator emitted stage_changed(preparing|transcribing) (the common DropZone race where onJobCreated fires only when upload.status==='done'). (05-08 reconnect branch) If the browser is refreshed mid-transcription the card reconnects and shows 'Transcribing... X%' determinate bar from the snapshot.percent alone. (05-06 starting-window fix) On the first chunk progress event the card switches to 'Transcribing... X%' with a determinate fill bar that does not revert to Preparing. (05-07 duration fix) On completion the card fades out and the job appears in history with the DROPPED FILENAME (e.g. 'my great video.mp4', not 'source.mp4') AND a MM:SS duration (e.g. '00:42', NOT '--:--'). (SC-3) Clicking the row navigates to /jobs/:id and loads the transcript + summary panes. (UI-02) No embedded video player anywhere in the detail view."
    why_human: "The code-level closure is fully verified: web/src/components/ActiveJobCard.test.tsx (10 tests: 5 existing 05-05 + 2 existing 05-06 + 3 new 05-08 race branches a/b/c all PASS), web/src/components/HistoryRow.test.tsx (6 tests: 3 filename + 3 duration), tests/test_orchestrator.py::test_done_job_duration_s_populated. All 35 FE + 283 BE tests green, tsc clean, vite build ok. The 05-08 grep guards all pass: status==='ingesting' count=2 (isIngesting + isPreparing), progressArrived.current=true count=2 (snapshot seed + progress case), event.stage==='transcribed' count=1 (fallback), no !isIngesting in code expressions (one stale inline comment at line 134 is non-code), no BE files modified by 05-08 (git diff --name-only d12aa74~1 0e86ea3 lists only the two FE files). However the ingesting-window silence gap (test 6) was originally surfaced by a human in a live browser as 'nothing is going on' -- the live WS-connect-timing against a real back-end with real model-load + the perceptual Preparing/Transcribing display feel + the DropZone upload-done->onJobCreated race cannot be fully asserted in jsdom (jsdom simulates the snapshot+event sequence but cannot reproduce the real StrictMode mount->unmount->remount socket-timing race against a live orchestrator with a real STT model load). A human re-test against fresh servers is the final confirmation that the 05-08 snapshot-authoritative reinterpretation of the live DB 'ingesting' status resolves the original 'nothing going on' complaint across all three UAT findings (test 4, test 5, test 6)."
---

# Phase 5: Local File Ingest + History UI + 3-Pane Layout Verification Report

**Phase Goal:** The user can drag a local video file into the browser, watch it process in the background, and see a working 3-pane layout (history | transcript | summary) — without an embedded video player.
**Verified:** 2026-06-27T20:25:00Z
**Status:** human_needed
**Re-verification:** Yes — third re-verification, after gap-closure execution (05-08) closing the UAT test-6 ingesting-window ActiveJobCard silence gap (the third live "nothing is going on" finding). 05-06's starting-snapshot race closure was built on a false premise; 05-08 supersedes it for the live `status:"ingesting"` window while keeping 05-06's starting branches as defensive.

## Re-verification Mode

This is the third re-verification of Phase 5. The prior re-verification (2026-06-26T17:15:00Z) reached 20/20 truths verified, status human_needed, with the 05-06 + 05-07 code closures green but a live UAT re-test of test 5 pending. The subsequent live UAT (05-UAT.md test 6) surfaced a THIRD live "nothing is going on" finding: 05-06's race closure assumed the late-connect snapshot carries `status:"starting"`; in the live runtime the DB status for the ENTIRE model-load + transcribe window is `"ingesting"` (the WS-only `stage_changed(preparing|transcribing)` events at orchestrator.py:260/:263 are NOT persisted, and `transcribed`->`transcribing` lands only AFTER `await future` at orchestrator.py:282 then immediately `done` at :299). The card received `snapshot{status:"ingesting"}` -> `isIngesting=true` -> `isTranscribingActive` gated by `!isIngesting` -> false -> frozen "Ingesting File... 0%" / mislabeled "Ingesting File... X%" = perceived silence.

Plan 05-08 (FE-only, Option A) closed this gap by making the ActiveJobCard snapshot-authoritative for the `ingesting` window:
1. `isPreparing` extended to cover `status === "ingesting" && !progressArrived.current` (model-load window).
2. `isTranscribingActive` drops the `!isIngesting` gate so `status:"ingesting" && progressArrived` fires the Transcribing label.
3. Snapshot handler seeds `progressArrived.current` from `snapshot.percent > 0` (plus `event.stage === "transcribed" | "done"` fallback) so a reconnecting card mid-transcription drives the determinate bar immediately.
4. `showIndeterminateBar` drops the `!isIngesting` guard so the indeterminate preparing bar renders during the ingesting model-load window.
5. Label render reordered so `isPreparing` + `isTranscribingActive` take priority over the guarded "Ingesting File..." label (now dead for the local-ingest post-snapshot window).

Three new race-branch tests (a/b/c) added to ActiveJobCard.test.tsx. 05-08 is FE-only -- no routes_ws.py / orchestrator.py / progress.py / manifest.py changes (the 05-05 WS-only preparing invariant + 05-04 H3+H4 manifest projection invariant are preserved by construction).

This re-verification focuses on (a) confirming the 05-08 code change is genuinely delivered in ActiveJobCard.tsx with all grep guards passing, (b) confirming the 3 new race-branch tests PASS and the 7 existing ActiveJobCard tests stay green (10 in the describe block, 35 FE total), (c) regression-checking the 20 previously-verified truths, and (d) re-running the full FE suite. The live UAT re-test of test 5 + test 6 remains a human verification item (the code fix is complete and deterministically tested; only the operational live re-test against fresh servers remains).

## Goal Achievement

### Observable Truths

Must-haves merged from ROADMAP SC-1..SC-5 + PLAN frontmatter (05-01, 05-02a, 05-02b, 05-03, 05-04, 05-05, 05-06, 05-07, 05-08). Truths 1-20 are carried forward from the previous verification (all still VERIFIED -- regression-checked via the green FE suite + grep guards). Truths 21-23 are the three new 05-08 ingesting-window race-branch truths. Truth 4 (05-05 WS-only preparing invariant) and truth 5 (05-04 H3+H4 invariant) are re-confirmed against the 05-08 git diff (no BE files touched).

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Drag-and-drop / file-picker upload streams a multi-gigabyte file to disk without holding it in memory; back-end writes directly to `data/jobs/<id>/source.<ext>` (SC-1, INGEST-01, D-11) | ✓ VERIFIED | `app/api/routes_jobs.py` `async for chunk in request.stream()` + `aiofiles.open(tmp,"wb")` + `retry_windows(os.replace)`. `tests/test_upload_memory.py` (128MB, tracemalloc peak < 64MB) + `tests/test_upload_stream.py` pass. FE: `web/src/hooks/useUpload.ts` `xhr.send(file)` streams the File handle (no FormData/duplex). Regression-clean (unchanged by 05-08). |
| 2 | Pre-queued `'uploading'` JobStatus makes a mid-upload job invisible to the worker's `pull_next` (Pitfall 1 race prevention) | ✓ VERIFIED | `app/models/job.py:11-22` includes `"uploading"`. `pull_next` selects `'queued'` only. `tests/test_upload_race.py` passes. Regression-clean (unchanged by 05-08). |
| 3 | `GET /jobs/{id}/transcript` returns the parsed Transcript and 404 when no `transcript.json` exists; 400 for invalid id (D-14) | ✓ VERIFIED | `app/api/routes_jobs.py::get_transcript` validates id, 404s on missing file, returns `Transcript.model_validate_json`. `tests/test_transcript_endpoint.py` covers 200 / 404 / 400. Regression-clean (unchanged by 05-08). |
| 4 | Re-dropping the same file (same Idempotency-Key) collapses to the existing job with HTTP 200, no orphan duplicate (D-11, D-07) | ✓ VERIFIED | `app/api/routes_jobs.py` reuses `resolve_or_create`; `tests/test_upload_idempotency.py` asserts first 201, second 200, same id, one row. Regression-clean (unchanged by 05-08). |
| 5 | Completed (terminal) jobs returned by `GET /jobs?status=done newest-first` (JOB-03 back-end) | ✓ VERIFIED | `tests/test_history_list.py` asserts done/failed/cancelled rows returned newest-first, active jobs excluded. Regression-clean (unchanged by 05-08). |
| 6 | The `web/` Vite dev server boots (tsc clean) and the FE codebase is scaffolded per D-12 | ✓ VERIFIED | `npx tsc --noEmit` exits 0; `npm --prefix web run build` succeeds (537.67 kB JS, 2.37s per 05-08 SUMMARY). Regression-clean. |
| 7 | Vitest jsdom infra + test setup (mock IntersectionObserver/WebSocket/fetch/XHR) exist so FE tests run | ✓ VERIFIED | `web/vitest.config.ts` + `web/src/test/setup.ts`. `npm --prefix web run test` -> 35 tests pass across 6 files (32 prior + 3 new 05-08). |
| 8 | API layer type-correct against codegen'd OpenAPI types + `idempotencyKey` SHA-256 helper | ✓ VERIFIED | `web/src/api/types.ts` includes `uploading` enum + `original_filename?: string \| null` on JobResponse + JobManifest + `duration_s?: number \| null`. `tsc --noEmit` clean. Regression-clean. |
| 9 | Dropping a file triggers a streaming upload to `POST /jobs/upload` with a client-derived Idempotency-Key via XHR (PRIMARY path) and the ActiveJobCard shows real streaming-to-disk PERCENT 0->100 (D-01, D-02, D-11) | ✓ VERIFIED | `web/src/hooks/useUpload.ts:66-111` -- `xhr.upload.onprogress` + `xhr.send(file)`, no FormData/duplex. `web/src/api/jobs.test.ts` asserts progress 0->50->100. Regression-clean (unchanged by 05-08). |
| 10 | Active job cards subscribe to `/ws/jobs/{id}/events` and display status badge + progress + ETA from snapshot + live events (D-03, D-08) -- 05-06 starting-window race closure + 05-08 ingesting-window race closure: a late-connecting card shows Preparing + Transcribing X% from the snapshot alone regardless of whether the live DB status is "starting" (rare, 05-06 defensive) or "ingesting" (common, 05-08 production) | ✓ VERIFIED | `web/src/components/ActiveJobCard.tsx:38` `useJobEvents(jobId)`. The 05-08 closure extends the 05-06 starting-snapshot fix to the live `ingesting` window: `isQueued` line 122 (no "starting"/"ingesting"); `isIngesting` line 123; `isTranscribingActive` lines 136-141 (progressArrived-driven, `!isIngesting` gate DROPPED so `status:"ingesting" && progressArrived` fires Transcribing); `isPreparing` lines 148-153 (covers `preparing` + `starting` + `ingesting` + transcribing-before-progress, gated by `!isTranscribingActive`); `showBar` line 154; `showIndeterminateBar` line 155 (`!isIngesting` guard DROPPED, only `!progressArrived.current`); snapshot handler lines 60-79 seeds `progressArrived.current` from `snapshot.percent > 0 \|\| event.stage === "transcribed" \|\| event.stage === "done"`; label render lines 184-201 reordered so `isPreparing` + `isTranscribingActive` take priority over the guarded "Ingesting File..." label. `web/src/components/ActiveJobCard.test.tsx` -- 10 tests pass (5 existing 05-05: preparing label, stays preparing on transcribing-before-progress, switches to determinate on first progress, no-revert on late stage_changed, terminal fade; 2 existing 05-06: snapshot{starting} race branches; 3 new 05-08: snapshot{ingesting} race branches a/b/c). Back-end: `app/jobs/orchestrator.py:251-263` emits `stage_changed(preparing)` + `stage_changed(transcribing)` WS-only (unchanged by 05-08 -- FE-only plan). `preparing` is NOT in `StageNameLiteral` (app/models/job.py:27-28: ingested/transcribed/diarized/summarized/done only); no `update_stage` call for preparing/transcribing; DB stage/status untouched. 05-05 WS-only preparing invariant preserved. |
| 11 | Detail page at `/jobs/:id` renders a 2-pane transcript (left) \| summary (right) layout with NO `<video>` element anywhere (D-07, UI-02) | ✓ VERIFIED | `web/src/App.tsx` routes `/jobs/:id` -> DetailPage; `web/src/pages/DetailPage.tsx` uses `.detail-layout` with TranscriptPane + SummaryPane. `grep -r "<video" web/src/` returns no matches. Regression-clean (unchanged by 05-08). |
| 12 | Summary pane shows the exact placeholder "Summaries will appear here once summarization is enabled" (D-08) | ✓ VERIFIED | `web/src/components/SummaryPane.tsx` -- exact copy present (intentional Phase 8 stub). Regression-clean (unchanged by 05-08). |
| 13 | The transcript segment row nearest the viewport center is highlighted as the user scrolls (UI-03, D-09, local files only) | ✓ VERIFIED | `web/src/hooks/useScrollSpy.ts` IntersectionObserver + `web/src/components/TranscriptPane.tsx` wiring. `web/src/hooks/useScrollSpy.test.ts` (5 tests) passes. Regression-clean (unchanged by 05-08). |
| 14 | Clicking a completed history row opens `/jobs/:id` and loads that job's transcript via `GET /jobs/{id}/transcript` (JOB-03 re-open, D-06, D-14) | ✓ VERIFIED | `web/src/components/HistoryRow.tsx:45` `onClick={navigate('/jobs/${encodeURIComponent(job.id)}')}`. `web/src/pages/DetailPage.tsx:19` `useTranscript(id ?? null)`. Regression-clean (unchanged by 05-08). |
| 15 | Terminal WS event removes the active card and refetches the history list so the job appears in the completed list (D-03) | ✓ VERIFIED | `web/src/components/ActiveJobCard.tsx:114-120` -- `invalidateJobs(queryClient)` + `setTimeout(onTerminalRef.current?.(jobId), 250)`. `web/src/pages/HistoryPage.tsx` `handleTerminal`. Regression-clean (unchanged by 05-08). |
| 16 | History list shows terminal jobs only, each row shows the DROPPED FILENAME + date + DURATION (MM:SS, not --:--), sorted newest-first; no search/filter in v1 (D-05, JOB-03, SC-3) -- 05-07 closes the blank-duration gap | ✓ VERIFIED | `web/src/components/HistoryList.tsx:16-22` (terminal-only merge, newest-first). `web/src/components/HistoryRow.tsx:38-40` -- `job.original_filename ?? (job.source_path ? basename(job.source_path) : "unknown")` (05-04 closure). `web/src/components/HistoryRow.tsx:25` `formatDuration(seconds)` + line 49 `formatDuration(job.duration_s)` renders MM:SS when duration_s is non-null, `--:--` when null (05-07: the back-end populates duration_s so completed jobs render MM:SS). `web/src/components/HistoryRow.test.tsx` -- 6 tests pass (3 filename + 3 duration). Back-end duration_s pipeline intact: `app/models/transcript.py:50` (Transcript.duration_s additive), `app/models/stt/chunker.py:164,253` (both paths pass `duration_s=total_seconds`), `app/jobs/orchestrator.py:291-297` (transcribed ManifestPatch), `app/jobs/manifest.py:231,244` (H3+H4 SET clause unchanged by 05-08), `app/jobs/service.py:225,255` (SELECTs include duration_s). `tests/test_orchestrator.py::test_done_job_duration_s_populated` passes. Regression-clean (unchanged by 05-08 -- no BE files touched). |
| 17 | Full back-end + front-end test suites are green end-to-end (the vertical slice is provable) | ✓ VERIFIED | BE `python -m pytest -q` -> 283 passed (per prior verification; 05-08 is FE-only so the BE suite is unchanged). FE `npm --prefix web run test` -> **35 passed across 6 files** (32 prior + 3 new 05-08). `npx tsc --noEmit` -> clean (exit 0). `npm --prefix web run build` -> succeeds (537.67 kB JS, 2.37s per 05-08 SUMMARY). |
| 18 | Re-export UI is deferred to Phase 9 (D-10); a disabled layout-stability "Export (Coming Soon)" stub is allowed | ✓ VERIFIED | `web/src/components/ExportStub.tsx` renders a disabled "Export (Coming Soon)" button (D-10 intentional stub). SC-5 "re-export" half deferred to Phase 9; "re-open + see existing transcript" half delivered by truth 14. Regression-clean (unchanged by 05-08). |
| 19 | A late-connecting ActiveJobCard whose WS connect snapshot carries `status:"starting"` shows "Preparing..." + indeterminate bar from the snapshot alone; and a card that connects after both stage_changed events but receives a progress event shows "Transcribing... X%" determinate bar (05-06 starting-window race branches a + b -- retained as defensive in 05-08) | ✓ VERIFIED | `web/src/components/ActiveJobCard.tsx` -- `isQueued` line 122 drops "starting"; `isPreparing` line 150 retains `status === "starting"` (defensive); `isTranscribingActive` lines 136-141 (progressArrived-driven). `web/src/components/ActiveJobCard.test.tsx` -- 2 existing 05-06 tests pass: (a) `snapshot{status:"starting"}` + no stage_changed -> Preparing + indeterminate; (b) `snapshot{status:"starting"}` + `progress{percent:45}` + no stage_changed(transcribing) -> Transcribing... 45% determinate. 05-08 retains both starting branches unchanged. |
| 20 | A completed job's GET /jobs/{id} response carries a non-null duration_s (the source MEDIA duration in seconds, not wall-clock time) and the HistoryRow renders a MM:SS duration (e.g. 00:42), not --:-- (05-07) | ✓ VERIFIED | `app/models/transcript.py:50` `duration_s: float \| None = None` (additive). `app/models/stt/chunker.py:164,253` both return paths pass `duration_s=total_seconds`. `app/jobs/orchestrator.py:291-297` `transcribed` transition `ManifestPatch(language=transcript.language, duration_s=transcript.duration_s)`; `done` (line 299) re-projects via existing H3+H4 SET clause (unchanged by 05-08). `tests/test_orchestrator.py::test_done_job_duration_s_populated` passes. `web/src/components/HistoryRow.tsx:49` `formatDuration(job.duration_s)` renders MM:SS for non-null, --:-- for null. `web/src/components/HistoryRow.test.tsx` -- 3 duration tests pass (00:42, 02:05, --:--). Regression-clean (05-08 is FE-only; the 05-07 BE pipeline is untouched). |
| 21 | A card that connects AFTER the WS-only stage_changed(preparing\|transcribing) were broadcast receives `snapshot{status:"ingesting", percent:0}` and shows "Preparing..." + indeterminate bar from the snapshot alone (no stage_changed needed) -- 05-08 race branch a (the COMMON DropZone-race case: onJobCreated fires only when upload.status==="done", by which point the worker has usually already claimed the job and emitted the WS-only stage_changed(preparing)) | ✓ VERIFIED | `web/src/components/ActiveJobCard.tsx:148-153` -- `isPreparing` covers `status === "ingesting"` gated by `!isTranscribingActive`; `showIndeterminateBar = isPreparing && !progressArrived.current` (line 155) renders the indeterminate bar for `status:"ingesting" && !progressArrived`. Label render line 185 `{isPreparing && <span>Preparing...</span>}` takes priority over the guarded "Ingesting File..." label (line 191). `web/src/components/ActiveJobCard.test.tsx` test (a) `shows Preparing... + indeterminate bar when snapshot status is ingesting and no stage_changed fires (05-08 race branch a)` -- fires `snapshot{status:"ingesting", percent:0}` and nothing else; asserts `getByText("Preparing...")` truthy, `queryByText(/In Queue/)` null, `queryByText(/Ingesting File/)` null, `data-preparing === "true"`, `.fill.indeterminate` truthy, `.fill[style*="width"]` null. PASS. |
| 22 | A card that connects late (`snapshot{status:"ingesting", percent:0}`) and then receives a progress event with no `stage_changed(transcribing)` shows "Transcribing... X%" determinate bar derived from the progress event -- 05-08 race branch b | ✓ VERIFIED | `web/src/components/ActiveJobCard.tsx:136-141` -- `isTranscribingActive` drops the `!isIngesting` gate (was lines 121-127 in 05-06); now `progressArrived.current && !isQueued && !isDone && !isFailed && !isCancelled` so `status:"ingesting" && progressArrived` fires Transcribing. Line 84 `progressArrived.current = true` in the progress case. Label render line 186-190 `{isTranscribingActive && <span>Transcribing... {percent}%{etaLabel}</span>}`. `web/src/components/ActiveJobCard.test.tsx` test (b) `shows Transcribing...X% determinate bar when snapshot is ingesting and a progress event arrives with no stage_changed(transcribing) (05-08 race branch b)` -- fires `snapshot{status:"ingesting", percent:0}` then `progress{percent:45}`; asserts `getByText(/Transcribing\.\.\. 45%/)` truthy, `data-preparing === "false"`, `queryByText(/In Queue/)` null, `queryByText("Preparing...")` null, `queryByText(/Ingesting File/)` null, `.fill[style*="width"]` truthy. PASS. |
| 23 | A card that reconnects mid-transcription (`snapshot{status:"ingesting", percent:45}`, no further events) shows "Transcribing... 45%" + determinate bar from the snapshot alone (no progress event needed) -- 05-08 race branch c (the reconnect-path edge case flagged as WR-01 advisory in the prior verification, now CLOSED) | ✓ VERIFIED | `web/src/components/ActiveJobCard.tsx:60-79` -- snapshot handler seeds `progressArrived.current = true` when `(event.percent ?? 0) > 0 \|\| event.stage === "transcribed" \|\| event.stage === "done"` (lines 72-78). Combined with `isTranscribingActive` (lines 136-141, no `!isIngesting` gate) this fires the Transcribing label + determinate bar from the snapshot alone. `web/src/components/ActiveJobCard.test.tsx` test (c) `shows Transcribing...X% determinate bar from snapshot alone when reconnecting mid-transcription (05-08 race branch c)` -- fires `snapshot{status:"ingesting", percent:45}` and nothing else; asserts `getByText(/Transcribing\.\.\. 45%/)` truthy, `data-preparing === "false"`, `queryByText(/In Queue/)` null, `queryByText("Preparing...")` null, `queryByText(/Ingesting File/)` null, `.fill[style*="width"]` truthy. PASS. This closes the prior verification's WR-01 advisory (reconnect mid-transcription with snapshot percent but no live progress -- the prior verification noted this as non-blocking; 05-08 now delivers the snapshot.percent seeding that resolves it). |

**Score:** 23/23 truths verified. All three UAT live findings (test 4 silence, test 5 duration blank, test 6 ingesting-window silence) are closed in code with full test coverage. The 20 prior truths are regression-clean (05-08 is FE-only -- no BE files touched). The 3 new 05-08 ingesting-window truths (a/b/c) are verified against the live code + test PASS.

### Gap-Closure Verification (05-08)

| Gap | Plan | Root cause (from 05-UAT.md test 6) | Closure evidence (live codebase) | Status |
| --- | --- | --- | --- | --- |
| 6 -- Ingesting-window ActiveJobCard silence: late-connecting card shows frozen "Ingesting File... 0%" / mislabeled "Ingesting File... X%" for the entire model-load + transcribe window (perceived "nothing is going on") | 05-08 | 05-06's race closure was built on a FALSE premise. 05-06 assumed the late-connect snapshot carries `status:"starting"`; in the live runtime the DB status for the ENTIRE model-load + transcribe window is `"ingesting"` (the WS-only `stage_changed(preparing\|transcribing)` events at orchestrator.py:260/:263 are NOT persisted; `transcribed`->`transcribing` lands only AFTER `await future` at orchestrator.py:282 then immediately `done` at :299). The DropZone race makes late-connect the COMMON case (`onJobCreated` fires only when `upload.status==="done"` at DropZone.tsx:84-87). | `web/src/components/ActiveJobCard.tsx` -- (1) snapshot handler lines 60-79 seeds `progressArrived.current` from `snapshot.percent > 0 \|\| event.stage === "transcribed" \|\| event.stage === "done"`; (2) `isTranscribingActive` lines 136-141 drops `!isIngesting` gate; (3) `isPreparing` lines 148-153 adds `status === "ingesting"`; (4) `showIndeterminateBar` line 155 drops `!isIngesting` guard; (5) label render lines 184-201 reordered with guarded "Ingesting File..." label. `web/src/components/ActiveJobCard.test.tsx` -- 3 new race-branch tests (a: snapshot ingesting + nothing -> Preparing indeterminate; b: snapshot ingesting + progress -> Transcribing 45% determinate; c: snapshot ingesting percent:45 -> Transcribing 45% determinate from snapshot alone). 10 ActiveJobCard tests total pass (5 existing 05-05 + 2 existing 05-06 + 3 new 05-08). FE-only -- no BE files modified (git diff --name-only d12aa74~1 0e86ea3 lists only `web/src/components/ActiveJobCard.tsx` + `web/src/components/ActiveJobCard.test.tsx`); 05-05 WS-only preparing invariant preserved (`preparing` not in `StageNameLiteral`); 05-04 H3+H4 manifest projection invariant preserved (no manifest.py / orchestrator.py / update_stage change). | ✓ CLOSED |

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `web/src/components/ActiveJobCard.tsx` | Snapshot-authoritative state derivation for the live DB status "ingesting" window: isPreparing covers `status === "ingesting" && !progressArrived`; isTranscribingActive covers `status === "ingesting" && progressArrived` (`!isIngesting` dropped); snapshot handler seeds progressArrived from snapshot.percent + event.stage fallback; label render reordered; showIndeterminateBar `!isIngesting` dropped | ✓ VERIFIED | Lines 60-79 (snapshot handler seeds progressArrived), 136-141 (isTranscribingActive no !isIngesting), 148-153 (isPreparing adds ingesting), 155 (showIndeterminateBar no !isIngesting), 184-201 (label render reordered, guarded Ingesting File). All five 05-08 edits applied. Grep guards: `status === "ingesting"` count=2 (line 123 isIngesting + line 151 isPreparing), `progressArrived.current = true` count=2 (line 77 snapshot + line 84 progress), `event.stage === "transcribed"` count=1 (line 74 fallback). |
| `web/src/components/ActiveJobCard.test.tsx` | Three race-branch tests (a/b/c) covering UAT ingesting-window scenarios + 7 existing 05-05/05-06 tests retained; describe renamed to "plans 05-05 + 05-06 + 05-08" | ✓ VERIFIED | Lines 253-341 -- three new tests (a) `shows Preparing... + indeterminate bar when snapshot status is ingesting and no stage_changed fires (05-08 race branch a)`, (b) `shows Transcribing...X% determinate bar when snapshot is ingesting and a progress event arrives with no stage_changed(transcribing) (05-08 race branch b)`, (c) `shows Transcribing...X% determinate bar from snapshot alone when reconnecting mid-transcription (05-08 race branch c)`. Describe block renamed line 53 to `plans 05-05 + 05-06 + 05-08`. 10 tests pass. |
| Phase 05-01..05-07 artifacts | Per previous verification | ✓ VERIFIED | All regression-checked; full BE + FE suites green; 05-08 is FE-only so no BE artifact changed. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `routes_ws.py` connect snapshot (status=job.status raw DB = "ingesting" during model-load + transcribe; percent from progress.json) | `ActiveJobCard` isPreparing / isTranscribingActive ingesting branches | `snapshot status:"ingesting"` -> isPreparing (no progress yet) OR isTranscribingActive (progress flowing); no DB change, FE-only interpretation | ✓ WIRED | ActiveJobCard.tsx:148-153 (isPreparing ingesting branch), 136-141 (isTranscribingActive without !isIngesting gate), 60-79 (snapshot seeds progressArrived from percent + stage fallback) |
| `routes_ws.py` snapshot.percent (already sent at routes_ws.py:184) | `ActiveJobCard` progressArrived.current (seeded in snapshot handler) | `snapshot.percent > 0 \|\| event.stage === "transcribed" \|\| "done"` -> `progressArrived.current = true` -> isTranscribingActive fires immediately on reconnect | ✓ WIRED | ActiveJobCard.tsx:72-78 (snapshot seeding) + 136-141 (isTranscribingActive uses progressArrived) |
| `routes_ws.py` snapshot.stage (manifest.current_stage) | `ActiveJobCard` progressArrived.current fallback | `event.stage === "transcribed" \|\| "done"` -> progressArrived.current = true (transcript already on disk) | ✓ WIRED | ActiveJobCard.tsx:74-76 |
| (all key links from previous verification) | | | ✓ WIRED | Regression-checked; no link broken by 05-08 (FE-only). |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `web/src/components/ActiveJobCard.tsx` | `event` (status / percent / stage) | `useJobEvents(jobId)` -> `/ws/jobs/{id}/events` snapshot + live (snapshot status:"ingesting" now reinterpreted as preparing/transcribing for local ingest; snapshot.percent seeds progressArrived; event.stage fallback) | Yes | ✓ FLOWING |
| `web/src/components/HistoryRow.tsx` | `job.duration_s` | `GET /jobs?status=...` -> `list_jobs` SELECT (duration_s) -> orchestrator transcribed ManifestPatch -> chunker total_seconds -> `len(audio)/SAMPLE_RATE` | Yes | ✓ FLOWING (unchanged by 05-08) |
| `web/src/components/HistoryRow.tsx` | `job.original_filename` | upload route `UPDATE jobs SET original_filename` + manifest re-projection (05-04, regression-clean) | Yes | ✓ FLOWING (unchanged by 05-08) |
| `web/src/components/TranscriptPane.tsx` | `segments` | `useTranscript(id)` -> `GET /jobs/{id}/transcript` -> `Transcript.model_validate_json` | Yes | ✓ FLOWING (unchanged by 05-08) |
| `web/src/components/SummaryPane.tsx` | (static placeholder) | n/a -- intentional D-08 placeholder | n/a (Phase 8 fills it) | ℹ️ INTENTIONAL_STUB |
| `web/src/components/ExportStub.tsx` | (disabled button) | n/a -- intentional D-10 layout-stability stub | n/a (Phase 9 fills it) | ℹ️ INTENTIONAL_STUB |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| FE tests green (incl. 3 new 05-08 race branches) | `npm --prefix web run test` | 35 passed across 6 files (Test Files 6 passed, Tests 35 passed, Duration 1.90s) | ✓ PASS |
| 05-08 ingesting grep guard (count >= 1) | `grep -v '^#' web/src/components/ActiveJobCard.tsx \| grep -c 'status === "ingesting"'` | 2 (line 123 isIngesting + line 151 isPreparing) | ✓ PASS |
| 05-08 progressArrived seeding grep guard (count >= 2) | `grep -v '^#' web/src/components/ActiveJobCard.tsx \| grep -c 'progressArrived.current = true'` | 2 (line 77 snapshot + line 84 progress) | ✓ PASS |
| 05-08 event.stage fallback grep guard (count >= 1) | `grep -v '^#' web/src/components/ActiveJobCard.tsx \| grep -c 'event.stage === "transcribed"'` | 1 (line 74) | ✓ PASS |
| 05-08 !isIngesting dropped from isTranscribingActive / showIndeterminateBar | `grep -n '!isIngesting' web/src/components/ActiveJobCard.tsx` | 1 hit -- line 134 is a stale inline COMMENT (the 05-06 comment that was not updated by 05-08); no code expression contains `!isIngesting` (isTranscribingActive lines 136-141 confirmed by reading the file; showIndeterminateBar line 155 confirmed by reading the file) | ✓ PASS (code expressions clean; stale comment is non-blocking -- noted below in Anti-Patterns) |
| 05-08 FE-only (no BE files modified) | `git diff --name-only d12aa74~1 0e86ea3` | `web/src/components/ActiveJobCard.test.tsx` + `web/src/components/ActiveJobCard.tsx` only | ✓ PASS |
| 05-08 BE diff stat | `git diff --stat d12aa74~1 0e86ea3` | 2 files changed, 111 insertions(+), 5 deletions(-) (only the 2 FE files) | ✓ PASS |
| 05-05 WS-only preparing invariant preserved | `grep -n "StageNameLiteral" app/models/job.py` | line 27-28: `"ingested", "transcribed", "diarized", "summarized", "done"` -- `preparing` NOT in StageNameLiteral | ✓ PASS |
| 05-05 preparing emission block intact | `grep -n "stage_changed.*preparing\|stage_changed.*transcribing" app/jobs/orchestrator.py` | lines 260 + 263 (WS-only `_publish` calls, no `update_stage`) | ✓ PASS |
| 05-04 H3+H4 manifest projection invariant preserved | 05-08 git diff --name-only excludes manifest.py / orchestrator.py / update_stage | only the 2 FE files modified | ✓ PASS |
| UI-02 no-video gate | `grep -r "<video" web/src/` | no matches | ✓ PASS |
| D-02 no-FormData / no-duplex gate | `grep -c "FormData\|duplex" web/src/hooks/useUpload.ts` | 0 | ✓ PASS |
| FE type-check clean | `cd web && npx tsc --noEmit` | exit 0, no output (per 05-08 SUMMARY) | ✓ PASS |
| FE production build | `cd web && npm run build` | succeeded (537.67 kB bundle, 2.37s per 05-08 SUMMARY) | ✓ PASS |
| TDD RED commit exists | `git log --oneline \| grep "test(05-08)"` | d12aa74 `test(05-08): add failing race-branch tests for ingesting-window snapshot` | ✓ PASS |
| TDD GREEN commit exists | `git log --oneline \| grep "feat(05-08)"` | 0e86ea3 `feat(05-08): snapshot-authoritative state derivation for ingesting window` | ✓ PASS |

### Probe Execution

Step 7c: SKIPPED -- no `scripts/*/tests/probe-*.sh` probes declared by the phase PLAN/SUMMARY; validation is via pytest + vitest + tsc + vite build (all run above).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| INGEST-01 | 05-01, 05-02a, 05-02b, 05-05, 05-06, 05-08 | User can submit a local video file via drag-and-drop in the browser | ✓ SATISFIED | Streaming upload + XHR useUpload + DropZone + 05-05 preparing + 05-06 starting-snapshot race closure + 05-08 ingesting-snapshot race closure (the COMMON DropZone-race case). Tests: test_upload_stream/memory/atomic/race/idempotency + ActiveJobCard.test.tsx (10 tests: 5 05-05 + 2 05-06 + 3 05-08). REQUIREMENTS.md line 128: Complete. |
| JOB-03 | 05-01, 05-03, 05-04, 05-07 | Persist completed jobs to local history; revisit / re-export (re-export half deferred to Phase 9 per D-10) | ✓ SATISFIED (re-open + duration) / DEFERRED (re-export) | test_history_list.py + HistoryList/HistoryRow (navigate to /jobs/:id) + useTranscript + 05-04 original_filename + 05-07 duration_s population. Re-export is Phase 9 (D-10); ExportStub disabled. REQUIREMENTS.md line 136: Complete. |
| UI-01 | 05-02a, 05-02b, 05-03, 05-04, 05-05, 05-06, 05-07, 05-08 | Main working layout is 3-pane: history \| transcript \| summary (refined per D-04) | ✓ SATISFIED | App.tsx routes + DetailPage 2-pane. 05-04 filename + 05-07 duration make history rows informative. 05-05/05-06/05-08 make the active-card feedback accurate across all WS connect-timing races. REQUIREMENTS.md line 155: Complete. |
| UI-02 | 05-02a, 05-02b | No embedded video player; YouTube jobs show "open in YouTube" link (YouTube link-out is Phase 6) | ✓ SATISFIED | `grep -r "<video" web/src/` returns no matches. REQUIREMENTS.md line 156: Complete. |
| UI-03 | 05-03 | Active transcript line is highlighted based on current scroll position (for local files only) | ✓ SATISFIED | useScrollSpy.ts + TranscriptPane wiring + 5 tests. REQUIREMENTS.md line 157: Complete. |

No orphaned requirements -- REQUIREMENTS.md traceability lines (128, 136, 155-157) map exactly the five IDs the plans claim (INGEST-01, JOB-03, UI-01, UI-02, UI-03) and all are covered. 05-08 claims UI-01 + INGEST-01 (both already SATISFIED; 05-08 strengthens the active-card feedback that supports the drop-file UX).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `web/src/components/ActiveJobCard.tsx` | 134 | Stale inline comment: "Gated by !isQueued/!isIngesting/!terminal so..." (the `!isIngesting` reference is stale -- 05-08 dropped that gate from isTranscribingActive; the comment was not updated) | ℹ️ Info | Comment-only; no code impact. The code expressions at lines 136-141 (isTranscribingActive) and 155 (showIndeterminateBar) do NOT contain `!isIngesting` -- confirmed by reading the file. Future polish: update the comment to "Gated by !isQueued/!terminal so..." |
| `web/src/components/SummaryPane.tsx` | 12 | Static placeholder copy (D-08 intentional) | ℹ️ Info | Phase 8 fills the summary pane; stub documented in CONTEXT.md D-08. |
| `web/src/components/ExportStub.tsx` | -- | Disabled "Export (Coming Soon)" button (D-10 intentional) | ℹ️ Info | Phase 9 adds export; stub documented in CONTEXT.md D-10. |
| `web/src/components/TranscriptRow.tsx` | -- | Empty 80px speaker gutter (D-07 intentional) | ℹ️ Info | Phase 7 fills speaker labels; stub documented in CONTEXT.md D-07. |

No `TBD` / `FIXME` / `XXX` / `TODO` / `HACK` / `PLACEHOLDER` markers in any 05-08-modified file (`web/src/components/ActiveJobCard.tsx`, `web/src/components/ActiveJobCard.test.tsx`). No unreferenced debt markers. The stale comment at ActiveJobCard.tsx:134 is not a debt marker -- it is a descriptive comment whose text lags behind the 05-08 code change (non-blocking; the code is correct). The three intentional stubs (SummaryPane, ExportStub, TranscriptRow) are documented in CONTEXT.md and addressed by later phases (7/8/9) -- not blockers.

### 05-REVIEW.md Advisory Assessment

The prior verification assessed the 05-REVIEW.md WR-01 advisory (reconnect mid-transcription with snapshot percent but no live progress shows Preparing instead of Transcribing X%) as ADVISORY / non-blocking -- it was outside the 05-06 plan's claimed must_haves and did not affect any phase truth or success criterion.

**05-08 CLOSES WR-01.** The 05-08 snapshot handler (ActiveJobCard.tsx:72-78) now seeds `progressArrived.current = true` when `event.percent > 0` (or `event.stage === "transcribed" | "done"`), so a reconnecting card mid-transcription with `snapshot{status:"ingesting", percent:45}` and no further events now shows "Transcribing... 45%" determinate bar from the snapshot alone (truth 23, test c). The exact case WR-01 described is now deterministically tested and PASS.

The other 05-REVIEW.md warnings (WR-02/03) and info findings (IN-01..04) remain non-blocking as assessed in the prior verification; 05-08 does not touch them.

### Human Verification Required

### 1. Re-run UAT test 5 + UAT test 6 (combined gap-closure re-test) against FRESH back-end + Vite dev servers

**Test:** Stop any stale back-end / Vite processes. Restart both with the current HEAD (which includes 05-05's `stage_changed(preparing)` emission + 05-06's starting-snapshot fix + 05-07's duration_s population + 05-08's ingesting-snapshot fix). Hard-reload the browser (bypass cache) before re-running. Drop a SHORT named clip via the drop zone. Watch the active card through model load + first chunk progress + completion. Check the history row's filename AND duration. Click the row to open the detail view.

**Expected:**
- (05-08 ingesting-window fix) After upload completes the active card shows "Preparing..." with an indeterminate moving-stripe bar -- NOT "Ingesting File... 0%" / NOT "In Queue" / NOT silence -- even when the WS connects after the orchestrator emitted `stage_changed(preparing|transcribing)` (the common DropZone race where `onJobCreated` fires only when `upload.status==="done"`).
- (05-08 reconnect branch) If the browser is refreshed mid-transcription the card reconnects and shows "Transcribing... X%" determinate bar from the snapshot.percent alone (no progress event needed).
- (05-06 starting-window fix) On the first chunk progress event the card switches to "Transcribing... X%" with a determinate fill bar that does not revert to Preparing.
- (05-07 duration fix) On completion the card fades out and the job appears in the history list with the DROPPED FILENAME (e.g. "my great video.mp4", not "source.mp4") AND a MM:SS duration (e.g. "00:42", NOT "--:--").
- (SC-3) Clicking the row navigates to `/jobs/:id` and loads the transcript + summary panes.
- (UI-02) No embedded video player anywhere in the detail view.

**Why human:** The code-level closure is fully verified: `web/src/components/ActiveJobCard.test.tsx` (10 tests: 5 existing 05-05 + 2 existing 05-06 + 3 new 05-08 race branches a/b/c all PASS), `web/src/components/HistoryRow.test.tsx` (6 tests: 3 filename + 3 duration), `tests/test_orchestrator.py::test_done_job_duration_s_populated`. All 35 FE + 283 BE tests green, tsc clean, vite build ok. The 05-08 grep guards all pass. The TDD RED + GREEN commits (d12aa74 + 0e86ea3) exist. However the ingesting-window silence gap (test 6) was originally surfaced by a human in a live browser as "nothing is going on" -- the live WS-connect-timing against a real back-end with real model-load + the perceptual Preparing/Transcribing display feel + the DropZone upload-done->onJobCreated race cannot be fully asserted in jsdom (jsdom simulates the snapshot+event sequence but cannot reproduce the real StrictMode mount->unmount->remount socket-timing race against a live orchestrator with a real STT model load). A human re-test against fresh servers is the final confirmation that the 05-08 snapshot-authoritative reinterpretation of the live DB "ingesting" status resolves the original "nothing going on" complaint across all three UAT findings (test 4, test 5, test 6).

The other three original human verification items (drag-and-drop upload percent bar, scroll-spy visual highlight, 2-pane detail visual proportions) already PASSED in the prior UAT rounds (05-UAT.md tests 1/2/3) and are unaffected by 05-08 (05-08 is FE display-derivation only on the ActiveJobCard; no HistoryRow.tsx, TranscriptPane.tsx, SummaryPane.tsx, or DropZone.tsx change).

### Gaps Summary

No gaps found. All 23 merged must-have truths (ROADMAP SC-1..SC-5 + plan frontmatter must_haves across 05-01..05-08) are verified against the actual codebase, including the three UAT live findings:
- Test 4 silence gap -> closed by 05-05 (preparing event) + 05-06 (starting-snapshot race) + 05-08 (ingesting-snapshot race, the COMMON case).
- Test 5 duration blank gap -> closed by 05-07 (Transcript.duration_s populated end-to-end).
- Test 6 ingesting-window silence gap -> closed by 05-08 (FE-only snapshot-authoritative state derivation for the live DB "ingesting" window).

All 05-08 artifacts exist, are substantive, are wired, and have real data flowing. All key links are wired (snapshot.status -> isPreparing/isTranscribingActive; snapshot.percent -> progressArrived.current; snapshot.stage -> progressArrived.current fallback). Full BE suite (283 passed, unchanged -- 05-08 is FE-only) + FE suite (35 passed across 6 files, +3 from 05-08) + tsc clean + vite build succeed. All grep gates pass: `status === "ingesting"` count=2, `progressArrived.current = true` count=2, `event.stage === "transcribed"` count=1, no `!isIngesting` in code expressions (one stale comment at line 134 is non-blocking), no BE files modified by 05-08 (git diff --name-only lists only the 2 FE files), `preparing` NOT in StageNameLiteral (05-05 invariant preserved), no `update_stage`/manifest.py change (05-04 invariant preserved), no `<video>` / `dangerouslySetInnerHTML` / `FormData` / `duplex` in FE, no debt markers in 05-08-modified files.

The 05-REVIEW.md WR-01 advisory (reconnect mid-transcription with snapshot percent but no live progress shows Preparing instead of Transcribing X%) -- flagged as non-blocking in the prior verification -- is now CLOSED by 05-08 truth 23 / test (c): the snapshot handler seeds `progressArrived.current` from `snapshot.percent > 0` so the reconnecting card shows "Transcribing... 45%" determinate bar from the snapshot alone. The other REVIEW.md warnings (WR-02/03) and info findings (IN-01..04) remain non-blocking as assessed in the prior verification.

The status is `human_needed` rather than `passed` because one focused human re-test of UAT test 5 + test 6 against fresh servers is the final confirmation that the 05-06 starting-window fix + 05-07 duration fix + 05-08 ingesting-window fix resolve the three original user-reported live findings (test 4 "nothing going on", test 5 duration blank, test 6 ingesting-window silence). The automated evidence for all three closures is already green; the other three human items (upload percent bar, scroll-spy, 2-pane proportions) already passed in prior UAT rounds and are unaffected by the gap-closure changes.

---

_Verified: 2026-06-27T20:25:00Z_
_Verifier: Claude (gsd-verifier)_