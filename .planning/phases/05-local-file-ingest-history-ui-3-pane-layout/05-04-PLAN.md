---
phase: 05-local-file-ingest-history-ui-3-pane-layout
plan: 04
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - migrations/0009_add_original_filename.sql
  - app/models/manifest.py
  - app/models/job.py
  - app/jobs/manifest.py
  - app/api/routes_jobs.py
  - web/src/api/types.ts
  - web/src/components/HistoryRow.tsx
  - tests/test_original_filename.py
  - web/src/components/HistoryRow.test.tsx
autonomous: true
requirements: [UI-01, JOB-03]
user_setup: []

must_haves:
  truths:
    - "The original dropped filename is visible on every completed history row"
    - "GET /jobs/{id} returns original_filename (the X-Filename the user dropped, not source.<ext>)"
    - "source_path still points at the in-job-dir source.<ext> file (D-04 unchanged); original_filename is an additive display field"
    - "A job created without an original_filename (e.g. POST /jobs without upload) has original_filename=null and HistoryRow falls back to basename(source_path)"
  artifacts:
    - path: "migrations/0009_add_original_filename.sql"
      provides: "ALTER TABLE jobs ADD COLUMN original_filename TEXT"
      contains: "original_filename"
    - path: "app/models/manifest.py"
      provides: "JobManifest.original_filename additive field"
      contains: "original_filename"
    - path: "app/models/job.py"
      provides: "JobResponse.original_filename + _row_to_response projection"
      contains: "original_filename"
    - path: "web/src/components/HistoryRow.tsx"
      provides: "Display original_filename with basename(source_path) fallback"
      contains: "original_filename"
    - path: "tests/test_original_filename.py"
      provides: "Round-trip: POST /jobs/upload X-Filename -> GET /jobs/{id} returns original_filename"
      contains: "original_filename"
  key_links:
    - from: "app/api/routes_jobs.py"
      to: "manifest.original_filename + jobs.original_filename column"
      via: "model_copy(update={...,original_filename: x_filename}) + UPDATE jobs SET original_filename"
      pattern: "original_filename"
    - from: "web/src/components/HistoryRow.tsx"
      to: "job.original_filename"
      via: "job.original_filename ?? basename(job.source_path)"
      pattern: "original_filename"
---

<objective>
Close UAT gap A (05-UAT.md test 4): history rows show `source.<ext>` instead of the dropped filename.

The back-end upload route discards the `X-Filename` header after extension validation. Persist it as an additive `original_filename` field on the manifest, the jobs table, and `JobResponse`, then render it in HistoryRow with a `basename(source_path)` fallback. `source_path` is unchanged (still `data/jobs/<id>/source.<ext>` per D-04); `original_filename` is a pure display addition (per D-04 and the gap-closure constraint).

Purpose: The user must recognize their file by name in the history list.
Output: New migration 0009, additive `original_filename` field on JobManifest + JobResponse + DB row, upload-route persistence, HistoryRow display + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-UAT.md

@app/models/manifest.py
@app/models/job.py
@app/jobs/manifest.py
@app/jobs/service.py
@app/api/routes_jobs.py
@web/src/components/HistoryRow.tsx
@web/src/api/types.ts
@migrations/0002_add_source_sha256.sql
@tests/test_upload_stream.py
@web/src/api/jobs.test.ts
@web/src/pages/DetailPage.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist original_filename across manifest + DB + JobResponse</name>
  <files>migrations/0009_add_original_filename.sql, app/models/manifest.py, app/models/job.py, app/jobs/manifest.py, app/api/routes_jobs.py, tests/test_original_filename.py</files>
  <behavior>
    - Round-trip: POST /jobs/upload with X-Filename "my great video.mp4" -> GET /jobs/{id} returns body.original_filename == "my great video.mp4".
    - Manifest on disk after upload carries original_filename == "my great video.mp4".
    - A job created via POST /jobs (no upload) has original_filename == null on GET /jobs/{id}.
    - After orchestrator run (update_stage ingested -> done), original_filename is still present on GET /jobs/{id} (re-projected by update_stage).
    - History row source_path still equals the in-job-dir source.<ext> path (D-04 unchanged).
  </behavior>
  <action>
Add migration `migrations/0009_add_original_filename.sql` containing a single `ALTER TABLE jobs ADD COLUMN original_filename TEXT;` (mirror 0002's one-column-per-file convention; the runner auto-discovers via glob and swallows the duplicate-column OperationalError on re-apply, so no db.py edit is needed).

Add `original_filename: str | None = None` to `JobManifest` in app/models/manifest.py (place it right after `source_path`). Default None keeps existing manifest JSON files loadable (Pydantic fills the default — same pattern as `diarization_enabled`).

Add `original_filename: str | None = None` to `JobResponse` in app/models/job.py (after `source_path`), and add `original_filename=row.original_filename` to `_row_to_response` (after `source_path=row.source_path`). JobResponse is `strict=True, extra="forbid"`; adding a new field with a default keeps existing constructors working.

Extend the `update_stage` UPDATE in app/jobs/manifest.py: add `"original_filename = :original_filename, "` to the SET clause (after the `source_path = :source_path, ` line) and bind `"original_filename": new_manifest.original_filename` in the params dict. This keeps the H3+H4 projection invariant (manifest -> DB on every stage transition) intact for the new column. Do NOT add original_filename to ManifestPatch — the upload route sets it directly (see below); ManifestPatch stays the user-mutable stage-patch surface.

In app/api/routes_jobs.py upload route (lines ~204-208): extend the `model_copy(update={...})` to include `"original_filename": x_filename` alongside `source_path` and `source_type`, so the on-disk manifest carries it. Then immediately after `await write_manifest(settings, manifest)` (before `await enqueue`), write the column to the DB row so an immediate GET /jobs/{id} returns it (without waiting for the orchestrator's update_stage projection):
`await session.execute(text("UPDATE jobs SET original_filename = :name WHERE id = :id"), {"name": x_filename, "id": job_id}); await session.commit()`
(`text` is already imported in routes_jobs.py; verify the import is present, add `from sqlalchemy import text` if missing.) This mirrors how source_path would ideally be projected at upload time; original_filename gets it right from the start. The later update_stage("ingested") re-projects the same value (idempotent).

Write tests/test_original_filename.py with TWO pytest async tests using the existing `client` + `tmp_data_dir` fixtures (run_worker=False, so the job stays queued):
1. `test_upload_persists_original_filename`: POST /jobs/upload with headers Idempotency-Key "orig-name-1", X-Filename "my great video.mp4", Content-Type octet-stream, content b"\x00"*1024. Assert 201, then GET /jobs/{id} returns 200 with `body["original_filename"] == "my great video.mp4"`. Then read the on-disk manifest via `read_manifest(settings, job_id)` (import from app.jobs.manifest; get settings from `app.main.app.state.settings`) and assert `manifest.original_filename == "my great video.mp4"`. Also assert `manifest.source_path` ends with `source.mp4` (D-04 unchanged). Also assert the DB-source_path column still ends with `source.mp4` via a direct SELECT (mirror test_upload_stream.py's job_dir inspection).
2. `test_create_job_without_upload_has_null_original_filename`: POST /jobs (the JSON route, not upload) with body `{"source_type":"local"}`, then GET /jobs/{id} asserts `body["original_filename"] is None`.

Run the full back-end suite to confirm no regression (especially test_upload_stream, test_upload_idempotency, test_get_jobs, test_create_job, test_reconcile, test_manifest_patch, test_stage_to_status).
  </action>
  <verify>
    <automated>python -m pytest tests/test_original_filename.py -x && python -m pytest tests/test_upload_stream.py tests/test_upload_idempotency.py tests/test_get_jobs.py tests/test_create_job.py tests/test_reconcile.py tests/test_manifest_patch.py tests/test_stage_to_status.py -x</automated>
  </verify>
  <done>POST /jobs/upload persists X-Filename as original_filename on both manifest and DB row; GET /jobs/{id} returns it; jobs created without an upload have original_filename=null; existing 05-01 upload + manifest + reconcile tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render original_filename in HistoryRow with fallback + regenerate FE types</name>
  <files>web/src/api/types.ts, web/src/components/HistoryRow.tsx, web/src/components/HistoryRow.test.tsx</files>
  <behavior>
    - HistoryRow renders job.original_filename verbatim when present.
    - HistoryRow falls back to basename(job.source_path) when original_filename is null/undefined.
    - HistoryRow shows "unknown" when both original_filename and source_path are absent.
    - The FE JobResponse type includes `original_filename?: string | null` (regenerated from the OpenAPI schema).
  </behavior>
  <action>
Regenerate the FE OpenAPI types so `JobResponse` carries the new field. Start the back-end on :8000 (`python -m uvicorn app.main:app --port 8000` from the repo root, in the background or a separate shell — migrations 0009 apply on boot), then run `npm --prefix web run gen-types` (invokes web/scripts/gen-types.sh which fetches http://localhost:8000/openapi.json -> web/src/api/types.ts via openapi-typescript). Stop the back-end afterward. Verify `web/src/api/types.ts` now contains `original_filename?: string | null;` inside the `JobResponse:` schema block (grep to confirm). If the back-end cannot be started in this environment, manually add `original_filename?: string | null;` to the JobResponse interface in web/src/api/types.ts directly below the `source_path?: string | null;` line (the file is auto-generated but a single additive field is a safe manual edit; the next gen-types run will reconcile it).

Edit web/src/components/HistoryRow.tsx: replace the `filename` derivation (currently `const filename = job.source_path ? basename(job.source_path) : "unknown"` at line 35) with `const filename = job.original_filename ?? (job.source_path ? basename(job.source_path) : "unknown")`. Keep the existing `basename` helper (it's still the fallback path). Do NOT change `formatDate` or `formatDuration`. The `<span>{filename}</span>` render at line 42 stays as-is.

Write web/src/components/HistoryRow.test.tsx (TSX — JSX is allowed; DetailPage.test.tsx uses TSX). Use vitest + @testing-library/react + react-router's MemoryRouter (HistoryRow calls useNavigate, so it must render inside a Router). Three tests:
1. `renders original_filename when present` — render `<HistoryRow job={{ id: "j1", status: "done", created_at: "2026-06-25T00:00:00+00:00", source_path: "/data/jobs/j1/source.mp4", original_filename: "vacation-final-cut.mp4", duration_s: 120 }} />` inside MemoryRouter + QueryClientProvider; assert `getByText("vacation-final-cut.mp4")` is truthy and `queryByText("source.mp4")` is null.
2. `falls back to basename(source_path) when original_filename is null` — same job shape with `original_filename: null`; assert `getByText("source.mp4")` is truthy.
3. `shows unknown when both absent` — job with `original_filename: null` and `source_path: null`; assert `getByText("unknown")` is truthy.
Note: the JobResponse type from types.ts requires a few fields; cast the job object `as JobResponse` if TypeScript complains about missing fields, or build a minimal partial. Keep the `.tsx` extension since this test uses JSX.

Run `npx --prefix web tsc --noEmit` and the full vitest suite.
  </action>
  <verify>
    <automated>npm --prefix web run build && npx --prefix web vitest run && bash -c 'grep -c "original_filename" web/src/api/types.ts | grep -qv ^0'</automated>
  </verify>
  <done>HistoryRow shows the original dropped filename when present, basename(source_path) as fallback, "unknown" when neither; FE types include original_filename; tsc + vitest + vite build all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> upload route | X-Filename header crosses here (untrusted) |
| upload route -> DB/manifest | persisted as display string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Spoofing | X-Filename header | accept | Header is a display-only string; no path traversal risk because source_path is server-generated (source.<ext>). Already validated for extension; no security impact from the name itself. |
| T-05-04-02 | Tampering | original_filename DB column | accept | Additive nullable TEXT column; no indexing, no query predicates read it; worst case is a misleading display label, not a security primitive. |
| T-05-04-03 | Information disclosure | original_filename in GET /jobs | mitigate | original_filename is returned on JobResponse like source_path; existing route authorization (single-user local app, no auth tier) applies unchanged — no new exposure surface. |
</threat_model>

<verification>
- `python -m pytest tests/test_original_filename.py -x` passes (round-trip + null case).
- Full back-end suite green (no regression on upload, reconcile, manifest, stage-to-status).
- `npx --prefix web vitest run` passes including the new HistoryRow.test.tsx.
- `npm --prefix web run build` succeeds.
- Manual spot-check (post-execution, optional): drop a named file via the UI, complete a job, confirm the history row shows the dropped name (not source.mp4).
</verification>

<success_criteria>
- Every completed history row shows the file name the user dropped (not `source.<ext>`).
- `source_path` still points at `data/jobs/<id>/source.<ext>` (D-04 unchanged).
- Jobs created without an upload render `basename(source_path)` / `unknown` as before (no regression).
- All existing tests pass.
</success_criteria>

<output>
Create `.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-04-SUMMARY.md` when done
</output>