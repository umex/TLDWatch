---
context: phase
phase: 05-local-file-ingest-history-ui-3-pane-layout
task: 1
total_tasks: 1
status: blocked
last_updated: 2026-06-27T23:08:29.985Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure this session.
> Acknowledge each one explicitly before proceeding. The execute-phase workflow will enforce
> a mandatory understanding check for the `blocking` rows in the table below.

- [ ] CONSTRAINT: jsdom-green ≠ live-green — Three successive gap closures (05-05, 05-06, 05-08) each passed every code-level check (jsdom ActiveJobCard tests, grep guards, 35 FE + 283 BE green, 23/23 must-haves goal-backward verified) yet the SAME live human UAT symptom ("nothing is going on / too quick to read / no Transcribing") recurred every time. Structural mitigation: the next gap-closure plan MUST include a live-reproducible evidence step (longer clip + browser dev-tools WS frame log + BE stage_changed/progress log for the specific job id) BEFORE writing any code, and the verifier MUST NOT mark the gap closed on jsdom+grep+unit-green alone — it must require live evidence.
- [ ] CONSTRAINT: verify runtime premises LIVE before codifying them — 05-08 was built on a FALSE premise derived from code-reading (it asserted the DB status is `"ingesting"` for the ENTIRE model-load + transcribe window). The live runtime showed the just-dropped active job in `status="starting"`. Structural mitigation: any gap-closure plan whose fix depends on a specific runtime status / event-timing MUST first capture the actual live snapshot/status/percent sequence and attach it as evidence; do not infer runtime behavior from source-reading and then encode the inference into must_haves + tests.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| jsdom-green ≠ live-green | 3 gap closures passed all jsdom+grep+unit+goal-backward checks but the live human UAT symptom recurred each time; the live WS-connect-timing + perceptual-feedback window cannot be asserted in jsdom | blocking | Next plan requires a LIVE evidence step (longer clip + browser WS frame log + BE event log) before code; verifier requires live evidence, not 23/23 code truths |
| Codifying unverified runtime premises | 05-08 asserted DB status is 'ingesting' for the whole window from reading orchestrator.py; live job was 'starting'. Tests were written against the false premise so they passed while the bug persisted | blocking | Plans depending on runtime status/event-timing must capture the actual live sequence as evidence first; premises must not be inferred from source-reading alone |
| Short warm clip hides the bug | A short clip on a warm model finishes in ~1-2s, so the active window is too small to observe feedback; this hid the bug through every re-test | advisory | Re-test with a LONGER clip (and/or after a cold model unload) so the active window is observable |

<current_state>
Phase 05 gap-closure is BLOCKED at the live UAT gate. Plan 05-08 (FE-only ActiveJobCard snapshot-authoritative for the `ingesting` window) was executed successfully at the code level — RED `d12aa74`, GREEN `0e86ea3`, docs `abc8127`, code review `f50b681`, re-verification `e853fbc` (status human_needed, 23/23 must-haves) — but the live re-test against fresh servers FAILED (`f1e84e4`): "after upload something shows up but it is too quick to read. There is no 'Transcribing ...'". 05-04 (filename) + 05-07 (duration) ARE confirmed live on new jobs (test4.mp4: duration_s=136.7, original_filename=test4.mp4); ONLY the active-card Preparing/Transcribing feedback still fails. Fresh BE + Vite are still running in the background for the follow-up diagnosis.
</current_state>

<completed_work>
- 05-08 executed: ActiveJobCard snapshot-authoritative for the ingesting window (isPreparing covers `status==='ingesting' && !progressArrived`; isTranscribingActive drops `!isIngesting`; snapshot handler seeds progressArrived from `snapshot.percent>0` + `event.stage` fallback; label render reordered). FE-only — no BE files touched. Commits d12aa74 (RED) + 0e86ea3 (GREEN) + abc8127 (SUMMARY+STATE+ROADMAP).
- 05-REVIEW.md written (status issues_found: 0 Critical, 2 Warning stale-comments, 3 Info). Commit f50b681.
- 05-VERIFICATION.md re-verified: status human_needed, 23/23 must-haves; one human_verification item (live re-test of tests 4/5/6). 05-UAT.md status → testing. Commit e853fbc.
- Fresh servers started: BE `becbwpap4` on 127.0.0.1:8000 (`python -m uvicorn app.main:app`), Vite `b0ncyirc9` on :5173. GET /health → 200.
- Live re-test of UAT test 6 FAILED; recorded in 05-UAT.md test 6 note + a new structured Gap entry with 3 hypotheses. Commit f1e84e4.
</completed_work>

<remaining_work>
- Gather LIVE evidence to distinguish H1 vs H2 vs H3 (longer clip + browser WS frame log + BE stage_changed/progress log for the specific job id).
- /gsd-plan-phase 5 --gaps → create the next gap-closure plan (e.g. 05-09) from the recorded UAT gap. The plan MUST embed the two BLOCKING CONSTRAINTS above (live-evidence-first; verify runtime premises live).
- /gsd-execute-phase 5 --gaps-only → execute the new plan.
- /gsd-verify-work 5 → re-run the live UAT (test 6 must pass against fresh servers with a longer clip) before phase 05 can be marked complete.
</remaining_work>

<decisions_made>
- 05-08 executed as FE-only Option A (reinterpret already-trusted snapshot fields) over Option B (BE-persisted transient status) to preserve the 05-04 H3+H4 + 05-05 WS-only invariants.
- Gap-closure executor run in SEQUENTIAL mode on the main tree (web/node_modules is gitignored → a fresh worktree would break vitest/tsc/build; single-plan wave, no parallelism benefit).
- Phase 05 NOT advanced to complete despite ROADMAP [x] (2026-06-26): verify-work returned human_needed and the live UAT fails; completion waits for the live re-test.
</decisions_made>

<blockers>
- Active-card Preparing/Transcribing feedback still fails live after 05-08. Needs live diagnosis (H1 timing / H2 progress-not-reaching-card / H3 snapshot status='starting' mismatch) before any more code.
</blockers>

## Required Reading (in order)
1. `.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-UAT.md` — test 6 note + the new Gap entry: the verbatim user report, live evidence (job 90289191 status='starting'), and the 3 hypotheses H1/H2/H3.
2. `.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-08-PLAN.md` — the 05-08 fix and its (false) premise about DB status='ingesting', to understand what was tried and why it didn't work live.
3. `.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-VERIFICATION.md` — status human_needed, 23/23 must-haves (code-level), the human_verification item.
4. `web/src/components/ActiveJobCard.tsx` + `web/src/pages/HistoryPage.tsx` — the card state derivation + lifecycle (card persists until onTerminal; the 'starting' branch is retained from 05-06).
5. `app/jobs/orchestrator.py` (lines ~230-300) — where DB status flips to 'ingesting' and where the WS-only stage_changed(preparing|transcribing) + progress emits fire; confirm the live timing.
6. `.planning/METHODOLOGY.md` (if it exists) — project analytical lenses.

## Critical Anti-Patterns (do NOT repeat these)
- ANTI-PATTERN: Declaring a live-runtime gap closed on jsdom + grep + unit-green alone → structural mitigation: require live evidence (WS frame log + BE event log) in the plan and in the verifier's gate.
- ANTI-PATTERN: Codifying a runtime-status premise from source-reading without live confirmation → structural mitigation: capture the actual live snapshot/status/percent sequence as evidence before writing must_haves/tests.

## Infrastructure State
- Backend: `becbwpap4` running in background — `python -m uvicorn app.main:app --host 127.0.0.1 --port 8000`, data_dir=E:\Projects\TranscriptionAndNotes\data, /health → 200. Started fresh on HEAD f1e84e4 (05-08 included; 05-08 is FE-only so BE code unchanged but process is no longer the days-old stale one).
- Frontend: `b0ncyirc9` running in background — Vite v8.1.0 on http://localhost:5173/, ready. Serves 05-08 ActiveJobCard.tsx on demand (mtime-keyed).
- Both will keep running across the pause. If they died, restart: BE `python -m uvicorn app.main:app --host 127.0.0.1 --port 8000`; FE `npm --prefix web run dev`. (Use the explicit Python path `C:\Users\dobrez\AppData\Local\Programs\Python\Python312\python.exe` if `python` is not on PATH.)
- On BE startup the worker re-claims old orphan jobs whose source files are gone (e.g. 248f5277, 301e681c) → they fail with "source file missing" and show as failed. Pre-existing, not caused by the reset.
- Untracked artifacts at repo root (NOT part of this work, leave alone): test5.mp4 (the re-test clip), out.json, data2/, data/models/, node_modules/, .planning/phases/01-.../01-VERIFICATION.md.

## Pre-Execution Critique Required
- Design artifact: none yet — the next step is diagnosis, not a new plan.
- Critique focus: before writing 05-09, confirm H1/H2/H3 with live evidence; do not let the planner repeat the "codify a premise from code-reading" anti-pattern.
- Gate: Do NOT plan another FE label/bar tweak until live WS frame + BE event logs show which hypothesis is true.

<context>
The phase has been through three code-correct gap closures that all failed the same live human test. The shared trap: the bug is in the live WS-connect timing / perceptual feedback window, which jsdom cannot reproduce, and each plan assumed a runtime status sequence it never verified live. The way out is to gather live evidence FIRST (longer clip + WS frame log + BE event log), pick the correct hypothesis, then plan. The fresh servers are already running to support this.
</context>

<next_action>
Start with: gather LIVE evidence for the active-card feedback failure. With the fresh servers still running, have the user drop a LONGER clip (cold or warm) and capture (a) the browser dev-tools WS frame log for `/ws/jobs/{id}/events`, (b) the backend log of `stage_changed(preparing|transcribing)` + progress emits for that job id, (c) the exact snapshot status/percent sequence the card receives. Use that to distinguish H1 (warm-model+short-clip timing) vs H2 (progress WS events not reaching the card live) vs H3 (snapshot status='starting'+percent:0 — 05-08's premise is false for part of the window). THEN run `/gsd-plan-phase 5 --gaps` to create the next gap-closure plan, `/gsd-execute-phase 5 --gaps-only` to execute it, and `/gsd-verify-work 5` to re-run the live UAT.
</next_action>