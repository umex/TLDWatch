---
phase: 05-local-file-ingest-history-ui-3-pane-layout
plan: 05
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - app/jobs/orchestrator.py
  - web/src/components/ActiveJobCard.tsx
  - tests/test_orchestrator.py
  - web/src/components/ActiveJobCard.test.tsx
autonomous: true
requirements: [UI-01, INGEST-01]
user_setup: []

must_haves:
  truths:
    - "While the STT model is loading the active job card shows an indeterminate 'Preparing…' state, not a stalled 0% transcribing bar"
    - "Once the first chunk progress event arrives the card switches to the determinate 'Transcribing… X%' bar"
    - "Existing stage_changed(transcribing)/progress/done/failed/cancelled handling is unchanged (additive preparing event only)"
    - "The test path (caller-provided adapter) does NOT emit a preparing event — it goes straight to transcribing, preserving existing orchestrator tests"
  artifacts:
    - path: "app/jobs/orchestrator.py"
      provides: "_publish({type:stage_changed, stage:preparing}) before _load_stt_adapter on the production path; stage_changed(transcribing) moved to AFTER adapter load"
      contains: "preparing"
    - path: "web/src/components/ActiveJobCard.tsx"
      provides: "Indeterminate Preparing… state until first progress event; determinate Transcribing…X% after"
      contains: "Preparing"
    - path: "tests/test_orchestrator.py"
      provides: "Assertion that preparing -> transcribing event ordering holds on the production path (no caller adapter)"
      contains: "preparing"
    - path: "web/src/components/ActiveJobCard.test.tsx"
      provides: "Preparing state renders until first progress event; Transcribing…X% after"
      contains: "Preparing"
  key_links:
    - from: "app/jobs/orchestrator.py"
      to: "EventBus -> WS -> ActiveJobCard"
      via: "_publish({type:stage_changed, stage:preparing}) before _load_stt_adapter"
      pattern: "stage_changed.*preparing"
    - from: "web/src/components/ActiveJobCard.tsx"
      to: "useJobEvents progress event"
      via: "progressArrived ref gates the determinate bar"
      pattern: "progressArrived|Preparing"
---

<objective>
Close UAT gap B (05-UAT.md test 4): between upload completion and the first chunk progress callback, the ActiveJobCard renders "Transcribing… 0%" with a 0% bar — the user thinks nothing is going on while the STT model JIT-loads.

Emit an additive `stage_changed(preparing)` event before `_load_stt_adapter` (production path only) and move `stage_changed(transcribing)` to AFTER the adapter loads, so the card shows an indeterminate "Preparing…" state during model load. On the FE, ActiveJobCard tracks whether a `progress` event has ever arrived and shows the determinate bar only then; until then it shows "Preparing…" (covering both the model-load window and the first-chunk wait).

Purpose: The user gets immediate feedback that transcription is working during the silent model-load + first-chunk window.
Output: Additive preparing stage event in orchestrator, indeterminate Preparing… state in ActiveJobCard, tests on both sides.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-UAT.md

@app/jobs/orchestrator.py
@app/jobs/progress.py
@web/src/components/ActiveJobCard.tsx
@web/src/api/ws.ts
@web/src/test/setup.ts
@tests/test_orchestrator.py
@tests/_stt_fake.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit additive stage_changed(preparing) before _load_stt_adapter</name>
  <files>app/jobs/orchestrator.py, tests/test_orchestrator.py</files>
  <behavior>
    - On the production path (no caller-provided adapter): preparing event fires BEFORE the STT adapter loads, transcribing event fires AFTER.
    - On the test path (caller-provided adapter): NO preparing event is emitted; transcribing fires as before (existing tests unchanged).
    - The DB stage/status do NOT change during preparing — preparing is a WS-only live event (no update_stage call).
    - Existing orchestrator tests (test_state_machine, test_restart_rejoin, test_heartbeat_during_transcribing, test_progress_snapshot_persisted) still pass.
  </behavior>
  <action>
In app/jobs/orchestrator.py, refactor the transcribing-stage block (currently lines ~236-248). Today the code is:

  if not skip_transcribed:
      _publish({"type": "stage_changed", "stage": "transcribing"})
      stt_adapter: STTAdapter
      if adapter is not None:
          stt_adapter = adapter
      else:
          stt_adapter = await _load_stt_adapter(settings)
          loaded_by_run = True

Restructure so the `stage_changed(transcribing)` emission happens AFTER the adapter resolves, and a `stage_changed(preparing)` emission happens BEFORE the (production-only) `_load_stt_adapter` call:

  if not skip_transcribed:
      stt_adapter: STTAdapter
      if adapter is not None:
          stt_adapter = adapter
      else:
          _publish({"type": "stage_changed", "stage": "preparing"})
          stt_adapter = await _load_stt_adapter(settings)
          loaded_by_run = True
      _publish({"type": "stage_changed", "stage": "transcribing"})

Rationale: the test path (adapter provided) skips the preparing event entirely — existing tests that capture events from run_job with a FakeAdapter see the same transcribing event as before. The production path now emits preparing -> (model load) -> transcribing. Do NOT call update_stage for "preparing" — it is a WS-only event; the DB stage stays at whatever the pre-transcribe stage was (e.g. "ingested", status "ingesting"). This keeps the stage_to_status mapping and the manifest-DB projection invariant untouched. The preparing stage value is NOT added to StageNameLiteral — it never reaches the DB.

Add a new test to tests/test_orchestrator.py: `test_preparing_event_emitted_before_transcribing_on_production_path`. Use the same settings + session_factory setup as test_state_machine (create_job, ensure_job_dir, write a real source.mp4, patch manifest.source_path + DB source_path). Instead of passing an adapter, pass `adapter=None` BUT monkeypatch `_load_stt_adapter` to return a FakeAdapter without doing real work (so the test does not pull faster_whisper). Add at module top or inside the test: `from app.jobs import orchestrator as orch_mod` then `monkeypatch.setattr(orch_mod, "_load_stt_adapter", lambda settings: _async_return(FakeAdapter()))` — or make the lambda an `async def` returning the FakeAdapter. Subscribe a recording queue to the EventBus BEFORE calling run_job: `q = bus.subscribe(job_id)` then collect published events with `asyncio.run(_drain(q))` (a small helper that drains the queue with a short timeout). Assert the event sequence is `[stage_changed(preparing), stage_changed(transcribing), progress..., done]` (or at minimum that the first stage_changed event has stage=="preparing" and a later stage_changed has stage=="transcribing"). Also assert run_job completes and the row is done.

Ensure the existing orchestrator tests that pass an `adapter=FakeAdapter(...)` still pass unchanged — they should NOT see a preparing event (verifies the test-path branch).

Run the full back-end suite.
  </action>
  <verify>
    <automated>python -m pytest tests/test_orchestrator.py -x && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>Production path emits preparing -> transcribing (ordered); test path (adapter provided) emits only transcribing; full back-end suite green including the existing orchestrator tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ActiveJobCard indeterminate Preparing… state until first progress</name>
  <files>web/src/components/ActiveJobCard.tsx, web/src/components/ActiveJobCard.test.tsx</files>
  <behavior>
    - When a stage_changed event with stage "preparing" arrives, the card shows "Preparing…" and an indeterminate bar (no percentage, no determinate fill width).
    - When status is "transcribing" but no progress event has arrived yet, the card STILL shows "Preparing…" (covers the first-chunk wait after the model loads).
    - Once the first progress event arrives, the card switches to "Transcribing… X%" with the determinate fill bar (existing behavior preserved).
    - The queued / ingesting / done / failed / cancelled branches are unchanged.
    - The terminal fade-out + invalidateJobs path still fires on done/failed/cancelled.
  </behavior>
  <action>
In web/src/components/ActiveJobCard.tsx:

1. Add a `progressArrived` ref: `const progressArrived = useRef(false)` (next to `invalidatedRef`). Reset it to `false` whenever `jobId` changes — add a small `useEffect(() => { progressArrived.current = false }, [jobId])` near the top, OR reset it in the existing `useJobEvents` effect's jobId-change path (the ws.ts hook already resets its own state on jobId change; mirror that here).

2. In the `useEffect([event])` switch, set `progressArrived.current = true` in the `case "progress":` branch (before/after setting percent — order does not matter; set it before the `break`). Do NOT reset progressArrived on stage_changed — a late transcribing event arriving after the first progress must not revert the UI to Preparing.

3. Compute `const isPreparing = (status === "preparing") || ((status === "transcribing") && !progressArrived.current)` (after the existing isQueued/isIngesting/isTranscribing booleans). This covers both the BE-emitted preparing stage AND the transcribing-before-first-progress window.

4. Change the `showBar` definition: `const showBar = isIngesting || isTranscribing` becomes `const showBar = isIngesting || isTranscribing` and add `const showIndeterminateBar = isPreparing && !isIngesting`. The determinate fill bar renders only when `showBar && !isPreparing` (i.e. transcribing WITH progress). When `showIndeterminateBar`, render a different fill element — a `<div className="fill indeterminate" />` with no `width` style (CSS-driven animation/stripes) — OR if the design system has no indeterminate class, render a `<div className="fill" style={{ width: "100%", opacity: 0.4 }} />` with a pulsing/animated style via the existing styles.css. Prefer reusing the existing `.progress-bar .fill` selector with a new `indeterminate` modifier class; add the class to web/src/styles.css if a matching rule is needed (a simple CSS keyframe pulse / moving stripe). If you add to styles.css, keep the change minimal (one rule + one keyframe).

5. Update the label branch: replace `{isTranscribing && (<span>Transcribing... {percent}%{etaLabel}</span>)}` with a guard: `{isTranscribing && progressArrived.current && (<span>Transcribing... {percent}%{etaLabel}</span>)}` and add `{isPreparing && (<span>Preparing…</span>)}` above it. Order matters: check isPreparing before the progressArrived-guarded transcribing label. The `isTranscribing` boolean stays defined (used for `showBar`).

6. Keep the `data-status={status}` attribute unchanged so the existing data-status selectors / tests still see the BE-reported stage. Add `data-preparing={isPreparing ? "true" : "false"}` so the new test can assert the preparing state deterministically.

Write web/src/components/ActiveJobCard.test.tsx (TSX — JSX allowed). Use vitest + @testing-library/react + @tanstack/react-query QueryClientProvider. ActiveJobCard calls `useJobEvents(jobId)` which opens a real WebSocket via the setup.ts MockWebSocket; drive events through `MockWebSocket.instances[0].__message({...})`. Wrap render in QueryClientProvider (onTerminal invalidateJobs uses useQueryClient — provide a QueryClient). Four tests:

1. `shows Preparing… on stage_changed(preparing)` — render `<ActiveJobCard jobId="job-1" />`; wait for MockWebSocket.instances.length to be 1; fire `__message({ type: "snapshot", job_id: "job-1", stage: null, percent: 0, eta: null, status: "queued" })` then `__message({ type: "stage_changed", stage: "preparing" })`; assert `getByText("Preparing…")` is truthy, `queryByText(/Transcribing/) ` is null, and `getByTestId("active-job-card").getAttribute("data-preparing") === "true"`. Assert no determinate fill width 0% bar (the fill element has no `width: 0%` or has the indeterminate class).

2. `stays Preparing… on transcribing before first progress` — after the preparing event, fire `__message({ type: "stage_changed", stage: "transcribing" })` (no progress yet); assert `getByText("Preparing…")` is still truthy and `data-preparing === "true"`.

3. `switches to Transcribing…X% on first progress` — after the transcribing event, fire `__message({ type: "progress", chunks_done: 1, chunks_total: 4, percent: 25, eta_s: 120, chunk_start_s: 0 })`; assert `getByText(/Transcribing\.\.\. 25%/)` is truthy and `data-preparing === "false"`.

4. `does not revert to Preparing… on a late stage_changed(transcribing) after progress` — after the progress event in test 3, fire another `stage_changed(transcribing)`; assert `getByText(/Transcribing/) ` is still present and `data-preparing === "false"` (progressArrived sticks).

Also assert the terminal path still works: fire `__message({ type: "done" })` and confirm the card adds the `terminal` class (existing behavior, smoke check).

Reset `MockWebSocket.instances` between tests via the setup.ts afterEach (already wired).

Run `npx --prefix web tsc --noEmit`, `npx --prefix web vitest run`, `npm --prefix web run build`.
  </action>
  <verify>
    <automated>npx --prefix web tsc --noEmit && npx --prefix web vitest run && npm --prefix web run build</automated>
  </verify>
  <done>ActiveJobCard shows "Preparing…" with an indeterminate bar during model load and until the first progress event; switches to "Transcribing… X%" determinate bar on first progress; never reverts to Preparing after progress; tsc + vitest + vite build all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator -> WS -> ActiveJobCard | new additive preparing event crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | preparing stage event (WS-only) | accept | The preparing event is a transient display-only hint; it is never written to the DB or manifest, so it cannot tamper with stage_to_status or the resume walker. A crafted preparing frame from a (local) client has no state effect — the card merely shows a label. |
| T-05-05-02 | Denial of Service | ActiveJobCard render loop | accept | progressArrived is a ref (no re-render on write); the switch runs once per event. No new polling or timer. Existing fade/invalidate path unchanged. |
| T-05-05-03 | Repudiation | stage_changed(preparing) not persisted | accept | By design — preparing is WS-only, mirroring the existing transcribing stage_changed emission (also WS-only, not persisted via update_stage until transcribed). No audit contract changes. |
</threat_model>

<verification>
- `python -m pytest tests/test_orchestrator.py -x` — preparing->transcribing ordering test passes; existing orchestrator tests pass.
- `python -m pytest tests/ -x -q` — full back-end suite green.
- `npx --prefix web vitest run` — ActiveJobCard.test.tsx passes (preparing state, first-progress switch, no-revert, terminal smoke).
- `npx --prefix web tsc --noEmit` + `npm --prefix web run build` succeed.
</verification>

<success_criteria>
- During model load the active job card shows "Preparing…" (not "Transcribing… 0%").
- After the first chunk progress event the card shows "Transcribing… X%" determinate.
- Test-path orchestrator runs (caller adapter) are unchanged — no preparing event.
- All existing tests pass.
</success_criteria>

<output>
Create `.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-05-SUMMARY.md` when done
</output>