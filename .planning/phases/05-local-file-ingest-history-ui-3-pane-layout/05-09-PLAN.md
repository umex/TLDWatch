---
phase: 05-local-file-ingest-history-ui-3-pane-layout
plan: 09
type: execute
wave: 4
gap_closure: true
mode: gap_closure
depends_on: ["05-06", "05-08"]
files_modified:
  - .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-09-EVIDENCE.md
  - web/src/components/ActiveJobCard.tsx
  - web/src/components/ActiveJobCard.test.tsx
autonomous: false
requirements: [UI-01, INGEST-01]

must_haves:
  truths:
    # The two BLOCKING CONSTRAINTS from .continue-here.md — embedded verbatim-intent
    - "CONSTRAINT (jsdom-green != live-green): the gap was closed on LIVE evidence (captured browser dev-tools WS frame log for /ws/jobs/{id}/events + BE log of stage_changed(preparing|transcribing) + progress emits for a specific job id dropped via the drop zone against FRESH servers with a LONGER clip), NOT on jsdom+grep+unit-green alone. The verifier MUST NOT mark the gap closed without the live evidence attached to 05-09-EVIDENCE.md."
    - "CONSTRAINT (verify runtime premises LIVE before codifying them): the runtime status / event-timing premise the fix depends on was captured LIVE and attached as evidence (the actual snapshot status/percent/stage sequence the card received for the specific job id) BEFORE the fix was codified in must_haves + tests. No premise was inferred from source-reading and then encoded into tests (the 05-08 anti-pattern)."
    - "After upload completes the active card shows Preparing... + indeterminate bar, then Transcribing... X% on first progress, SUSTAINED until terminal (not a sub-second flash with no Transcribing label) — confirmed by a human against FRESH back-end + Vite dev servers with a LONGER clip (cold model unload), captured in a WS frame log + screenshot/notes attached to 05-09-EVIDENCE.md or 05-UAT.md test-6 re-test note."
  artifacts:
    - path: ".planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-09-EVIDENCE.md"
      provides: "Live evidence record: (a) browser dev-tools WS frame log for /ws/jobs/{id}/events with timestamps for every snapshot + stage_changed + progress frame; (b) BE log of stage_changed(preparing|transcribing) + progress emits for the specific job id (temporary structured logging added to orchestrator.py ONLY if none exists — READ orchestrator.py first); (c) the exact snapshot status/percent/stage sequence the card received + the resulting label/bar rendered at each step; (d) the confirmed hypothesis H1|H2|H3 (or combination) with cited evidence rows; (e) post-fix live re-test WS frame log + human observation"
      contains: "H1|H2|H3"
    - path: "web/src/components/ActiveJobCard.tsx"
      provides: "Hypothesis-conditional fix applied per the evidence outcome from Task 1's checkpoint. Concrete identifiers in <action>."
    - path: "web/src/components/ActiveJobCard.test.tsx"
      provides: "jsdom regression test(s) matching the confirmed hypothesis branch. jsdom-green is NECESSARY but NOT SUFFICIENT to close the gap (live evidence is the gate)."
  key_links:
    - from: "browser dev-tools WS frame log for /ws/jobs/{id}/events"
      to: "05-09-EVIDENCE.md confirmed-hypothesis section"
      via: "captured snapshot/stage_changed/progress frames with timestamps -> cited evidence rows -> H1|H2|H3 verdict"
      pattern: "snapshot|stage_changed|progress"
    - from: "BE log of stage_changed(preparing|transcribing) + progress emits for job id"
      to: "05-09-EVIDENCE.md confirmed-hypothesis section"
      via: "orchestrator.py _publish calls (:260 preparing, :263 transcribing, _on_progress progress_cb :278) -> BE stdout/log -> cited rows"
      pattern: "stage_changed|progress"
---

<objective>
Close the LAST open gap in Phase 05: UAT test 6 (05-UAT.md, the newest `status: failed` entry at the end — "After upload completes the active card shows Preparing... + indeterminate bar, then Transcribing... X% on first progress, SUSTAINED until terminal"). 05-04 (filename) and 05-07 (duration) ARE confirmed live; ONLY the active-card Preparing/Transcribing feedback still fails.

**This is the fourth gap-closure attempt for the same live symptom.** Three prior code-correct closures (05-05, 05-06, 05-08) each passed every code-level check (jsdom ActiveJobCard tests, grep guards, 35 FE + 283 BE green, 23/23 must-haves goal-backward verified) yet the SAME live symptom recurred every time. 05-08 was built on a FALSE premise derived from code-reading (it asserted the DB status is `"ingesting"` for the ENTIRE model-load + transcribe window; live job 90289191 was in `status="starting"`). The way out — mandated by `.continue-here.md` and the gap_closure_directive — is to gather LIVE evidence FIRST (longer clip + browser WS frame log + BE event log), distinguish three unconfirmed hypotheses (H1 timing / H2 progress-not-reaching-card / H3 snapshot status='starting' mismatch), then apply the hypothesis-matching fix, then re-test live.

**TWO BLOCKING CONSTRAINTS (from `.continue-here.md` — non-negotiable):**
1. **jsdom-green != live-green.** The live WS-connect-timing + perceptual-feedback window against a real back-end with real STT model-load + the DropZone upload-done→onJobCreated race CANNOT be asserted in jsdom. The verifier MUST NOT mark the gap closed on jsdom+grep+unit-green alone — it MUST require live evidence.
2. **Verify runtime premises LIVE before codifying them.** Any fix that depends on a specific runtime status / event-timing MUST first capture the actual live snapshot/status/percent sequence and attach it as evidence; do NOT infer runtime behavior from source-reading and then encode the inference into must_haves + tests.

Purpose: Break the recurring "code-correct but live-failed" loop by making live evidence the gate, not jsdom.
Output: `05-09-EVIDENCE.md` (live evidence + confirmed hypothesis + post-fix re-test), a hypothesis-conditional fix in `ActiveJobCard.tsx` (+ matching jsdom regression test), and a live re-test result in `05-UAT.md` test 6.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/.continue-here.md
@.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-UAT.md
@.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-VERIFICATION.md
@.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-08-PLAN.md
@.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-08-SUMMARY.md

@web/src/components/ActiveJobCard.tsx
@web/src/components/ActiveJobCard.test.tsx
@web/src/pages/HistoryPage.tsx
@web/src/components/DropZone.tsx
@app/jobs/orchestrator.py
@app/api/routes_ws.py
@app/jobs/manifest.py
</context>

<tasks>

<task type="checkpoint" autonomous="false">
  <name>Task 1: Capture LIVE evidence + determine which hypothesis (H1/H2/H3) explains the persistent active-card feedback failure (NO CODE FIX in this task)</name>
  <files>.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-09-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/.continue-here.md (the TWO BLOCKING CONSTRAINTS — acknowledge each before proceeding; this task is their structural mitigation)
    - .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-UAT.md (the newest `status: failed` Gap entry at the end — the verbatim user report, the live evidence already captured for job 90289191 in status="starting", and the three hypotheses H1/H2/H3)
    - web/src/components/ActiveJobCard.tsx (current state derivation: snapshot case lines 60-79 seeds progressArrived from snapshot.percent + event.stage fallback; isQueued line 122; isIngesting line 123; isTranscribing lines 124; isTranscribingActive lines 136-141 gated by !isQueued/!isDone/!isFailed/!isCancelled but NOT !isIngesting (05-08 dropped it); isPreparing lines 148-153 covers preparing|starting|ingesting|(transcribing && !progressArrived); showIndeterminateBar line 155)
    - web/src/api/ws.ts (useJobEvents hook — WS connect/disconnect lifecycle; the card subscribes via this hook)
    - app/jobs/orchestrator.py (lines 220-300: update_stage('ingested') at :230-233 → status='ingesting'; _publish stage_changed preparing at :260 (WS-only, no update_stage); _load_stt_adapter at :261 (JIT model load, slow on cold start); _publish stage_changed transcribing at :263; _on_progress progress_cb at :278 → progress events; update_stage('transcribed') at :290-297 → status='transcribing' persisted ONLY after `await future` at :282 returns; update_stage('done') at :299. READ FIRST to see what is already logged before adding temporary logging.)
    - app/api/routes_ws.py (lines 167-188: snapshot sends status=job.status (raw DB), stage=manifest.current_stage, percent+eta from _read_progress_snapshot — confirm what the card actually receives)
    - app/jobs/manifest.py (lines 39-77: _STAGE_STATUS_MAP / stage_to_status — None→queued, ingested→ingesting, transcribed→transcribing, done→done; the DB cannot represent preparing or transcribing-during-transcription)
    - web/src/components/DropZone.tsx (onJobCreated fires only when upload.status==='done' — the upload-done→card-mount race that makes late-connect the common case)
    - web/src/pages/HistoryPage.tsx (handleJobCreated adds jobId on upload-done; card removes itself ONLY via onTerminal — confirms the card does NOT unmount during transcription)
  </read_first>
  <action>
  **Do NOT write any fix code in this task.** This task's deliverable is a live-evidence artifact that distinguishes H1 vs H2 vs H3.

  **Step 1 — Confirm fresh servers on current HEAD.** Verify the BE + Vite dev servers are running on current HEAD against the configured data_dir (E:\Projects\TranscriptionAndNotes\data). If they died, restart: BE `python -m uvicorn app.main:app --host 127.0.0.1 --port 8000` (use `C:\Users\dobrez\AppData\Local\Programs\Python\Python312\python.exe` if `python` is not on PATH); FE `npm --prefix web run dev`. Confirm `GET /health` → 200. Hard-reload the browser (bypass cache).

  **Step 2 — Pick a LONGER clip.** The short warm-clip is the WORST case for observing feedback (the 05-UAT.md note: "warm model + short clip -> starting->ingesting->done in seconds"). Use a clip long enough that the model-load + transcribe window is observable — ideally a COLD run (kill/restart BE so the STT model is NOT resident) with a clip of several minutes. If only test5.mp4 (short) is available, do a COLD run (restart BE immediately before the drop so the model must JIT-load) AND a WARM run (drop a second clip right after the first finishes). Record which run each evidence row is from.

  **Step 3 — Open browser dev-tools Network tab, filter to `/ws/jobs/{id}/events`.** Have the user drop the longer clip via the drop zone. As soon as the ActiveJobCard mounts (upload-done), note the job id from the URL or from `GET /jobs?status=starting`. Record EVERY WS frame the card receives (snapshot, stage_changed, progress, done) with timestamps — dev-tools WS frame inspector or a `window.__jobEventsLog = []; ... push(event)` probe in the console against the live `useJobEvents` stream. If a console probe is needed, add it transiently in the browser console (NOT in source) — e.g. monkey-patch `WebSocket.prototype.send`/`onmessage` to log frames.

  **Step 4 — Capture the BE event log for the same job id.** READ `app/jobs/orchestrator.py` first to see what is already logged around `_publish` (lines 260, 263) and `_on_progress` (line 278). If structured logging of `stage_changed(preparing|transcribing)` + progress emits is NOT already present, add TEMPORARY `print()` or `_log.info()` lines at orchestrator.py:260, :263, and inside `_on_progress` that emit the job_id + stage/percent + timestamp — and restart BE so they take effect BEFORE the drop. Capture the BE stdout/log lines for the specific job id.

  **Step 5 — Capture the exact snapshot status/percent/stage sequence the card received + the resulting label/bar.** For each WS frame recorded in step 3, record: frame type (snapshot | stage_changed | progress | done), `status` field, `percent` field, `stage` field, timestamp, and the label + bar the card rendered at that moment (Preparing.../indeterminate, Transcribing...X%/determinate, Done/fade). If the card flashed Preparing then Done with no Transcribing window, record that — it is H1 evidence. If progress frames were never received by the card (present in BE log but absent from the WS frame log), record that — it is H2 evidence. If the snapshot carried `status:"starting"` + `percent:0` and the card never reached `isTranscribingActive` before terminal, record that — it is H3 evidence.

  **Step 6 — Write `05-09-EVIDENCE.md`** with four sections:
    (a) **WS frame log** — table of every snapshot/stage_changed/progress/done frame the card received, with timestamps + status + percent + stage + rendered label/bar;
    (b) **BE event log** — table of every `stage_changed(preparing|transcribing)` + progress emit the BE logged for that job id, with timestamps;
    (c) **Snapshot sequence verdict** — the exact snapshot `status`/`percent`/`stage` sequence the card received and which label/bar each produced;
    (d) **Confirmed hypothesis** — H1 | H2 | H3 | combination, with cited evidence rows from (a)/(b)/(c). State explicitly whether 05-08's premise ("DB status is 'ingesting' for the ENTIRE window") was TRUE or FALSE in the live run, and which branch of `isPreparing`/`isTranscribingActive` was actually hit.

  **Step 7 — Emit `## CHECKPOINT REACHED`** at the end of `05-09-EVIDENCE.md` stating the confirmed hypothesis and hand back to the orchestrator. Do NOT proceed to Task 2 until the evidence is reviewed.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-09-EVIDENCE.md && grep -q "CHECKPOINT REACHED" .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-09-EVIDENCE.md && grep -qE "H1|H2|H3" .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-09-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - 05-09-EVIDENCE.md EXISTS in the phase directory and contains a `## CHECKPOINT REACHED` marker.
    - 05-09-EVIDENCE.md contains a captured browser dev-tools WS frame log for a specific job id with timestamps, including AT LEAST one snapshot frame and (if the run reached progress) at least one progress frame — NOT a jsdom test run, NOT a source-reading inference.
    - 05-09-EVIDENCE.md contains a captured BE event log for the SAME job id showing `stage_changed(preparing|transcribing)` and/or progress emits (or explicitly states that none were emitted/logged — which is itself H2 evidence).
    - 05-09-EVIDENCE.md states the confirmed hypothesis as H1 | H2 | H3 | a combination, with cited evidence rows (WS frame rows + BE log rows) supporting the verdict.
    - 05-09-EVIDENCE.md states explicitly whether 05-08's premise (DB status is 'ingesting' for the ENTIRE model-load + transcribe window) was TRUE or FALSE in the live run, with the actual observed snapshot `status` value(s) cited.
    - The clip used was a LONGER clip (or a COLD-model run), explicitly noted in the evidence — NOT the short warm-clip worst case.
    - NO fix code was written in this task (ActiveJobCard.tsx / orchestrator.py / routes_ws.py / manifest.py are NOT modified by Task 1 — only 05-09-EVIDENCE.md is produced, plus any TEMPORARY BE logging probe added to orchestrator.py solely for evidence capture, which must be reverted or explicitly noted for carry-over).
  </acceptance_criteria>
  <done>05-09-EVIDENCE.md exists with a WS frame log + BE event log for a specific job id from a longer/cold clip, a snapshot-sequence verdict, a stated confirmed hypothesis (H1/H2/H3/combination) with cited evidence rows, and a `## CHECKPOINT REACHED` marker. No fix code written.</done>
</task>

<task type="auto" autonomous="true">
  <name>Task 2: Apply the hypothesis-conditional fix matching Task 1's evidence verdict (FE-only if H1/H2/H3-permit; BE-touching only if H3 demands it)</name>
  <files>web/src/components/ActiveJobCard.tsx, web/src/components/ActiveJobCard.test.tsx</files>
  <read_first>
    - .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/.continue-here.md (the TWO BLOCKING CONSTRAINTS — the fix must be the EVIDENCE-MATCHING branch, not a re-codified premise)
    - .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-09-EVIDENCE.md (the confirmed hypothesis from Task 1 — this dictates which branch of the action applies)
    - web/src/components/ActiveJobCard.tsx (current state: snapshot case 60-79; progressArrived ref 52; isTranscribingActive 136-141; isPreparing 148-153; showIndeterminateBar 155; label render 184-202)
    - web/src/components/ActiveJobCard.test.tsx (existing 10 tests across the 05-05+05-06+05-08 describe block; helpers renderCard/waitForSocket/fire)
    - app/jobs/orchestrator.py (lines 220-300 — the _publish stage_changed preparing (:260) / transcribing (:263) / _on_progress progress_cb (:278) / update_stage ingested (:230-233) / transcribed (:290-297) / done (:299) — only read if a BE-touching H3 branch applies)
    - app/api/routes_ws.py (lines 167-188 — snapshot status=job.status + percent from _read_progress_snapshot; only read if a BE-touching H3 branch applies)
    - app/jobs/manifest.py (lines 39-77 — _STAGE_STATUS_MAP / stage_to_status; only read if a BE-touching H3 branch applies, since it would extend the H3+H4 invariant and requires JOB-03 in requirements)
  </read_first>
  <action>
  Apply the fix branch matching the confirmed hypothesis from `05-09-EVIDENCE.md`. The concrete identifiers per branch:

  **If H1 (TIMING/WINDOW — warm model + short clip races starting→ingesting→done in ~1-2s, snapshot already at/near terminal, Preparing flashes + Done fades before any progress event drives the Transcribing label):**
    - The fix is a MINIMUM-DISPLAY-DURATION / SUSTAINED-FEEDBACK treatment, NOT another label-derivation tweak. In `web/src/components/ActiveJobCard.tsx`:
      - Add a `mountedAtRef = useRef(Date.now())` set on mount (and on jobId change) alongside `progressArrived` (line 52).
      - In the terminal handlers (`case "done"` line 89, `case "failed"` line 94, `case "cancelled"` line 99, `case "error"` line 102): instead of `setFading(true)` immediately, defer it until `Date.now() - mountedAtRef.current >= MIN_FEEDBACK_MS` (e.g. 1500ms). If the elapsed time is less, `setTimeout` the `setFading(true)` for the remainder. This holds the Preparing/Transcribing label on-screen for fast jobs so the user can read it.
      - Introduce a post-terminal "Transcribed" visible state: when `isDone` becomes true AND `progressArrived.current` was true (i.e. the job genuinely transcribed), keep the card visible with a "Transcribed" label (or "Transcribing... 100%") for the dwell window before fading, instead of an instant Done→fade.
      - Re-test with a LONGER clip (cold) to confirm the window is genuinely observable when the model load is slow.
    - Add a jsdom regression test: render the card, fire `snapshot{status:"ingesting",percent:0}`, fire `done` immediately (< MIN_FEEDBACK_MS later), assert the card does NOT fade immediately and the Preparing/Transcribing label remains visible for the dwell window. jsdom uses fake timers — control with `vi.useFakeTimers()`.
    - Mark `autonomous: true` (FE-only). requirements stays [UI-01, INGEST-01].

  **If H2 (PROGRESS NOT REACHING THE CARD LIVE — progress WS events from orchestrator.py:278 `_on_progress` chunk callbacks are not delivered/processed by the card's `useJobEvents` handler in the short live window, so `progressArrived.current` never sticks live → `isTranscribingActive` stays false; passes in jsdom, fails live):**
    - The fix is on the WS delivery / event-processing path. In `web/src/components/ActiveJobCard.tsx` + `web/src/api/ws.ts`:
      - Add a temporary `console.log`/probe in the `useJobEvents` message handler (`web/src/api/ws.ts`) AND in the ActiveJobCard `useEffect` event switch (line 57) to confirm whether progress frames reach the handler live. Run the live drop with the probe; capture the console output.
      - If progress frames reach `useJobEvents` but NOT the ActiveJobCard effect: fix the event-shape mismatch or the subscription timing (e.g. StrictMode double-connect dropping the first progress frame; the `useJobEvents` hook's `readyState`/reconnect logic dropping early frames; the `event.type` discriminator not matching the live shape).
      - If progress frames do NOT reach `useJobEvents` at all: fix the WS subscription timing — the card may be connecting after the progress emits were broadcast and EventBus (`app/jobs/progress.py`) does not replay. The fix is either FE reconnect/retry, or a BE snapshot that already carries percent (which `routes_ws.py:179` does — confirm the snapshot.percent path works live, which would make this an H1/H3 overlap).
      - Do NOT assume jsdom's event sequence matches live — the probe is the evidence.
    - Add a jsdom regression test matching the live failure shape (e.g. progress event arrives before the effect subscribes; or StrictMode double-connect).
    - Mark `autonomous: true` (FE-only). requirements stays [UI-01, INGEST-01].

  **If H3 (SNAPSHOT STATUS MISMATCH — live snapshot during the window carries `status:"starting"` + `percent:0` (pre-'ingested') → `isPreparing` fires and the card never reaches `isTranscribingActive` before terminal; 05-08's premise that DB status is 'ingesting' for the ENTIRE window is FALSE for at least part of the window):**
    - ONLY apply this branch if Task 1's evidence confirmed the live snapshot carried `status:"starting"` (cite the WS frame row from 05-09-EVIDENCE.md). Do NOT re-codify an unverified premise (the 05-08 anti-pattern).
    - The fix makes the snapshot authoritative for the `'starting'` window the way 05-08 did for `'ingesting'`, BUT only after live confirmation. In `web/src/components/ActiveJobCard.tsx`:
      - `isPreparing` (lines 148-153) already covers `status === "starting"` (retained from 05-06) — confirm the live snapshot's `status:"starting"` actually hits this branch (if the evidence shows it does and the card still flashed, the real issue is H1 timing — apply H1's minimum-display-duration treatment instead, or in addition).
      - If the live evidence shows the snapshot carried `status:"starting"` AND the card rendered "In Queue" (not "Preparing..."), the `isQueued` definition (line 122) may still include "starting" — REMOVE `"starting"` from `isQueued` so the `isPreparing` starting branch actually wins. (Verify against the evidence: if the card showed "In Queue" with snapshot status:"starting", this is the fix.)
      - If the evidence shows the `'starting'` window is followed by a jump straight to `'done'` (no `'ingesting'` window at all for short warm clips), apply the H1 minimum-display-duration treatment on top so the Preparing label is observable.
    - Add a jsdom regression test: `snapshot{status:"starting",percent:0}` + `done` → card shows "Preparing..." (NOT "In Queue") for the dwell window before fade.
    - Mark `autonomous: true` (FE-only) UNLESS the evidence shows the BE must persist a transient status (Option B from 05-08 — extending `manifest.py` `_STAGE_STATUS_MAP` / `StageNameLiteral` + `update_stage` calls on the WS-only path). If the BE-touching Option B is required by the evidence, mark `autonomous: false`, ADD `app/jobs/manifest.py`, `app/jobs/orchestrator.py`, `app/api/routes_ws.py` to `files_modified`, and ADD `JOB-03` to the `requirements` field. Document explicitly why the FE-only approach is insufficient (citing the evidence).

  **Combined outcome (e.g. H1+H3):** apply both branches; the H1 minimum-display-duration treatment is the perceptual floor that makes any label-derivation fix (H3) observable for fast jobs. Most likely outcome per `.continue-here.md` is H1+H3 (warm short clip + snapshot status='starting' for part of the window).

  **TDD ORDER (this task is `tdd="true"`):** Write the jsdom regression test(s) for the confirmed branch FIRST, run `npx --prefix web vitest run ActiveJobCard.test.tsx` to observe RED, commit the RED state. Then apply the fix, re-run to observe GREEN, commit GREEN. The `<behavior>` block below is the test contract.

  Revert any TEMPORARY BE logging probe added in Task 1 (unless the evidence shows it should be promoted to a permanent structured log — in which case keep it and note it in 05-09-EVIDENCE.md).
  </action>
  <behavior>
    - Branch-specific behavior matching the confirmed hypothesis from 05-09-EVIDENCE.md. Examples (apply the matching one):
      - H1: render card, fire snapshot{status:"ingesting"|"starting",percent:0}, fire `done` within MIN_FEEDBACK_MS; assert the card does NOT add the `terminal` class immediately and the Preparing/Transcribing label is still in the DOM; after MIN_FEEDBACK_MS elapses (fake timers), the fade proceeds.
      - H2: render card, simulate the live failure shape (e.g. progress event arrives before the effect subscribes, or StrictMode double-connect drops the first frame); assert `progressArrived.current` sticks and `isTranscribingActive` becomes true once a progress frame is processed.
      - H3: render card, fire snapshot{status:"starting",percent:0}; assert "Preparing..." renders and "In Queue" does NOT (i.e. `"starting"` is NOT in `isQueued`).
    - All existing 10 ActiveJobCard tests (05-05 + 05-06 + 05-08) stay green.
  </behavior>
  <verify>
    <automated>npx --prefix web tsc --noEmit && npx --prefix web vitest run ActiveJobCard.test.tsx && npx --prefix web vitest run && npm --prefix web run build</automated>
  </verify>
  <acceptance_criteria>
    - The applied fix branch matches the confirmed hypothesis stated in `05-09-EVIDENCE.md` (cite the hypothesis in the commit message and in a comment in ActiveJobCard.tsx referencing the evidence row).
    - `npx --prefix web tsc --noEmit` passes.
    - `npx --prefix web vitest run ActiveJobCard.test.tsx` passes (new branch-specific regression test(s) + all 10 existing tests green).
    - `npx --prefix web vitest run` passes (full FE suite, no regression).
    - `npm --prefix web run build` passes.
    - If the BE-touching Option B was applied (H3 BE-required case): `app/jobs/manifest.py` + `app/jobs/orchestrator.py` + `app/api/routes_ws.py` changes preserve the 05-04 H3+H4 manifest projection invariant (status projected to DB on every stage transition via `stage_to_status()`); the `requirements` field includes `JOB-03`; `autonomous: false`.
    - **EXPLICITLY: jsdom-green alone does NOT close the gap.** The gap is closed only after Task 3's live re-test passes. State this in the task's commit message and in 05-09-EVIDENCE.md.
  </acceptance_criteria>
  <done>The hypothesis-matching fix is applied in ActiveJobCard.tsx (+ ws.ts / BE files only if the branch demands it), a jsdom regression test matching the confirmed hypothesis passes, all existing tests stay green, tsc + build pass, and the fix references the evidence row in 05-09-EVIDENCE.md. jsdom-green is necessary but NOT sufficient — Task 3 is the gate.</done>
</task>

<task type="checkpoint:human-verify" autonomous="false">
  <name>Task 3: Live UAT re-test of test 6 against FRESH servers with a LONGER clip (the actual gap gate)</name>
  <files>.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-09-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/.continue-here.md (the TWO BLOCKING CONSTRAINTS — this task is the live-evidence gate; jsdom-green from Task 2 is NOT sufficient)
    - .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-09-EVIDENCE.md (the confirmed hypothesis + the Task 2 fix — the re-test must confirm the fix resolves the specific failure mode the evidence identified)
    - .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-UAT.md (test 6 expected behavior + the newest `status: failed` Gap entry — the re-test must satisfy the ORIGINAL test-6 truth)
    - web/src/components/ActiveJobCard.tsx (the Task-2 fix in place)
  </read_first>
  <action>
  **Step 1 — Restart both servers on the post-fix HEAD.** Kill the BE + Vite processes and restart on current HEAD (includes the Task-2 fix). BE: `python -m uvicorn app.main:app --host 127.0.0.1 --port 8000` (use `C:\Users\dobrez\AppData\Local\Programs\Python\Python312\python.exe` if `python` is not on PATH). FE: `npm --prefix web run dev`. Confirm `GET /health` → 200. Hard-reload the browser (bypass cache).

  **Step 2 — Drop a LONGER clip via the drop zone, ideally COLD (kill/restart BE immediately before the drop so the STT model is NOT resident).** The longer/cold clip is required because the short warm clip is the worst case for observing feedback (05-UAT.md note: "warm model + short clip -> starting->ingesting->done in seconds").

  **Step 3 — Human observes the active card lifecycle.** After upload completes, the human verifies:
    (a) the active card shows "Preparing..." + indeterminate moving-stripe bar (NOT "In Queue" / NOT "Ingesting File... 0%" / NOT silence);
    (b) on first chunk progress, the card switches to "Transcribing... X%" + determinate bar that does NOT revert;
    (c) the Transcribing label + bar are SUSTAINED until terminal — NOT a sub-second flash with no Transcribing label (the exact live symptom from 05-UAT.md test 6);
    (d) on completion, the card fades and the job appears in history showing the dropped filename (05-04) + MM:SS duration (05-07).

  **Step 4 — Capture the post-fix live evidence.** Attach to `05-09-EVIDENCE.md` (or a `## Post-fix re-test` section, or a note in `05-UAT.md` test 6): the WS frame log for the post-fix job id (snapshot/stage_changed/progress/done with timestamps), a screenshot or written notes of the card in the Preparing and Transcribing states, and the human's confirmation that the Transcribing window was observable (not a flash).

  **Step 5 — Update `05-UAT.md` test 6.** If the re-test passes, flip the newest `status: failed` Gap entry to `status: resolved` with `resolved_by: "05-09 (gap-closure plan, executed — live evidence in 05-09-EVIDENCE.md)"` and add a re-test note with the post-fix evidence. If it still fails, record the new live symptom + WS frame log and do NOT mark the gap closed — hand back for further diagnosis.

  **Step 6 — Emit `## CHECKPOINT REACHED`** at the end of `05-09-EVIDENCE.md` stating whether the live re-test PASSED or FAILED, with the post-fix evidence attached.
  </action>
  <verify>
    <automated>grep -q "Post-fix re-test" .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-09-EVIDENCE.md && grep -qE "live re-test (PASSED|FAILED)" .planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-09-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - The live re-test was run against FRESH back-end + Vite dev servers on the post-fix HEAD with a LONGER clip (cold or warm-but-long), explicitly noted in 05-09-EVIDENCE.md — NOT a jsdom run, NOT a short warm clip.
    - A human observed the active card show "Preparing..." + indeterminate bar, then "Transcribing... X%" + determinate bar on first progress, SUSTAINED until terminal — NOT a sub-second flash with no Transcribing label. (This is the ORIGINAL test-6 truth.)
    - A post-fix WS frame log for the specific job id is attached to 05-09-EVIDENCE.md showing the snapshot/stage_changed/progress/done sequence with timestamps.
    - 05-09-EVIDENCE.md contains a `## Post-fix re-test` section with a `live re-test PASSED` (or `FAILED`) verdict.
    - If PASSED: 05-UAT.md test 6 newest `status: failed` Gap entry is flipped to `status: resolved` with `resolved_by: "05-09"` and a re-test note citing the evidence.
    - If FAILED: the new live symptom + WS frame log is recorded and the gap is NOT marked closed (hand back for further diagnosis).
  </acceptance_criteria>
  <done>Live re-test against fresh servers with a longer clip is recorded in 05-09-EVIDENCE.md with a human-observed PASSED/FAILED verdict + post-fix WS frame log. If PASSED, 05-UAT.md test 6 gap is flipped to resolved. This task — NOT Task 2's jsdom-green — is the gap gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS server -> browser (snapshot/stage_changed/progress frames) | server-sourced status/stage/percent cross here (already trusted by 05-02a + 05-05 + 05-06 + 05-08; routes_ws.py builds the snapshot from get_job + read_manifest + _read_progress_snapshot) |
| browser dev-tools capture -> 05-09-EVIDENCE.md | transient evidence artifact; not a runtime trust boundary |
| (H3 BE-touching branch only) manifest.py _STAGE_STATUS_MAP / orchestrator.py update_stage | extends the 05-04 H3+H4 manifest projection invariant; only touched if Task 1 evidence demands it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-01 | Tampering | WS snapshot status/percent/stage reinterpreted by the fix (H1/H2/H3 FE branches) | accept | The snapshot is already a server-sourced trusted message (routes_ws.py:180-188). FE reinterpretation of an already-trusted field changes a display label only — no DB/manifest write, no state mutation. Same trust path as 05-05/05-06/05-08. |
| T-05-09-02 | Tampering | H1 minimum-display-duration defers terminal fade | accept | Defers a UI fade by ~1.5s; no security impact. The terminal `done` WS event is still received and the history refetch still fires; only the card's unmount is delayed for perceptual feedback. |
| T-05-09-03 | Tampering | H2 fix touches ws.ts delivery / event-processing path | mitigate | If the H2 fix changes WS event-shape handling, validate the `event.type` discriminator against the live shape (snapshot/stage_changed/progress/done) before dispatching; reject unknown event types silently (default case) to prevent a crafted frame from driving a wrong state. No new trust boundary — same WS source. |
| T-05-09-04 | Tampering | H3 BE-touching Option B (only if evidence demands it): persist a transient preparing/transcribing status to DB | mitigate | Extends manifest.py _STAGE_STATUS_MAP + StageNameLiteral + update_stage calls on the WS-only path. MUST preserve the 05-04 H3+H4 invariant (status projected to DB on every stage transition via stage_to_status()). Add a migration if a new stage is introduced; add a BE test (tests/test_orchestrator.py) asserting the new stage maps correctly. requirements field adds JOB-03. |
| T-05-09-05 | Information disclosure | temporary BE logging probe added in Task 1 for evidence capture | mitigate | The probe logs job_id + stage + percent + timestamp to BE stdout — no PII (filenames are not PII for local ingest). Revert the probe in Task 2 unless the evidence shows it should be promoted to a permanent structured log (note in 05-09-EVIDENCE.md). |
| T-05-09-SC | Tampering | npm/pip installs | mitigate | No new packages introduced by this plan (FE-only branches) or, for the H3 BE branch, no new packages either (stdlib logging only). If a package were needed, slopcheck + blocking human checkpoint. |
</threat_model>

<verification>
- Task 1: 05-09-EVIDENCE.md exists with a captured WS frame log + BE event log for a specific job id from a longer/cold clip, a confirmed hypothesis (H1/H2/H3/combination) with cited evidence rows, and a `## CHECKPOINT REACHED` marker. No fix code written.
- Task 2: hypothesis-matching fix applied; jsdom regression test passes; all 10 existing ActiveJobCard tests + full FE suite green; tsc + build pass. **jsdom-green is NOT the gap gate.**
- Task 3: live re-test against fresh servers with a longer clip; human observes Preparing + indeterminate → Transcribing X% + determinate SUSTAINED until terminal; post-fix WS frame log attached; 05-UAT.md test 6 gap flipped to resolved (or new symptom recorded if still failing).
- The gap is closed ONLY when Task 3's live re-test PASSED. The verifier MUST NOT mark the gap closed on Task 1 + Task 2 alone.
</verification>

<success_criteria>
- 05-09-EVIDENCE.md captures live evidence (WS frame log + BE event log) for a specific job id from a longer/cold clip and states the confirmed hypothesis (H1/H2/H3/combination) with cited rows.
- The runtime premise the fix depends on was captured LIVE before the fix was codified (NOT inferred from source-reading — the 05-08 anti-pattern).
- The hypothesis-matching fix is applied in ActiveJobCard.tsx (+ ws.ts / BE files only if the branch demands it) with a jsdom regression test.
- All existing 283 BE + 35 FE tests stay green; new jsdom regression test(s) added.
- The 05-04 H3+H4 manifest projection invariant + the 05-05 WS-only preparing invariant are preserved (unless the H3 BE-touching Option B was explicitly required by the evidence and applied with JOB-03 in requirements).
- Live re-test of UAT test 6 against fresh servers with a longer clip PASSED: human observes Preparing + indeterminate → Transcribing X% + determinate SUSTAINED until terminal. 05-UAT.md test 6 gap flipped to resolved with the re-test note.
</success_criteria>

<output>
Create `.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-09-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-09-EVIDENCE.md` — the live evidence record (WS frame log, BE event log, snapshot sequence verdict, confirmed hypothesis, post-fix re-test). This is the primary deliverable of Task 1 and the gate for Task 3; it is the structural mitigation for the two BLOCKING CONSTRAINTS.
- `web/src/components/ActiveJobCard.tsx` — hypothesis-conditional fix (H1 minimum-display-duration / H2 WS-delivery / H3 starting-snapshot-authoritative, or a combination). Concrete identifiers cited per branch in Task 2's action.
- `web/src/components/ActiveJobCard.test.tsx` — jsdom regression test(s) matching the confirmed hypothesis branch (added to the existing `describe("ActiveJobCard preparing state (plans 05-05 + 05-06 + 05-08 + 05-09)", ...)` block).
- `web/src/api/ws.ts` — modified ONLY if H2 is confirmed (WS delivery / event-processing path fix).
- `app/jobs/orchestrator.py` — modified ONLY if (a) H3 BE-touching Option B is required by the evidence (extend `_STAGE_STATUS_MAP` / `StageNameLiteral` + `update_stage` on the WS-only path), OR (b) temporary structured logging is promoted to permanent (noted in 05-09-EVIDENCE.md). Otherwise a TEMPORARY logging probe is added in Task 1 and reverted in Task 2.
- `app/jobs/manifest.py` — modified ONLY if H3 BE-touching Option B is applied (extends `_STAGE_STATUS_MAP` / `stage_to_status`; requirements adds JOB-03).
- `app/api/routes_ws.py` — modified ONLY if H3 BE-touching Option B is applied (snapshot status would then carry the new persisted transient status).
- `.planning/phases/05-local-file-ingest-history-ui-3-pane-layout/05-UAT.md` — test 6 newest `status: failed` Gap entry flipped to `status: resolved` with `resolved_by: "05-09"` and a re-test note (only on Task 3 PASSED).
- No existing plans 05-01..05-08 are modified.
</content>
</invoke>