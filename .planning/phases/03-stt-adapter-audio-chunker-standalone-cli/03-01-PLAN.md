---
phase: 03-stt-adapter-audio-chunker-standalone-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/stt/__init__.py
  - app/models/stt/protocol.py
  - app/models/stt/adapter.py
  - pyproject.toml
  - tests/_stt_fake.py
  - tests/test_stt_adapter.py
  - tests/test_stt_boundary.py
  - tests/conftest.py
autonomous: true
requirements: [TRANS-01, INGEST-06]
user_setup: []

must_haves:
  truths:
    - "A STTAdapter Protocol exists in app.models.stt and the chunker/CLI/orchestrator depend on it, never on faster_whisper/ctranslate2 (D-06, SC-4)"
    - "FasterWhisperAdapter.transcribe() maps faster-whisper Segment{start,end,text,avg_logprob} to TranscriptSegment{start_s,end_s,text,confidence} (TRANS-01)"
    - "FasterWhisperAdapter with language=None records faster-whisper's detected language in SttTranscription.language (INGEST-06, D-07)"
    - "After load, the adapter reads self._model.model.compute_type and fails loud (RuntimeError) if it silently fell back to float32 when an int8 variant was requested (D-08)"
    - "faster_whisper and ctranslate2 are imported ONLY inside app/models/stt/adapter.py — grep -rE 'from faster_whisper|import faster_whisper|import ctranslate2' app/ matches only that file (SC-4)"
  artifacts:
    - path: "app/models/stt/protocol.py"
      provides: "STTAdapter Protocol + SttTranscription + SttSegment result types"
      contains: "class STTAdapter"
    - path: "app/models/stt/adapter.py"
      provides: "FasterWhisperAdapter — the ONLY faster_whisper/ctranslate2 import site + D-08 int8 verification"
      contains: "class FasterWhisperAdapter"
    - path: "app/models/stt/__init__.py"
      provides: "package re-exports"
      contains: "FasterWhisperAdapter"
    - path: "tests/_stt_fake.py"
      provides: "FakeAdapter implementing STTAdapter Protocol with deterministic segments + OOM-on-demand"
      contains: "class FakeAdapter"
    - path: "tests/test_stt_adapter.py"
      provides: "TRANS-01 segment mapping + INGEST-06 language detect + D-08 int8 fail-loud tests"
      contains: "test_int8_verification_fails_loud"
    - path: "tests/test_stt_boundary.py"
      provides: "SC-4 forbidden-import boundary gate"
      contains: "test_import_boundary"
  key_links:
    - from: "app/models/stt/adapter.py"
      to: "faster_whisper.WhisperModel"
      via: "lazy in-body import inside FasterWhisperAdapter.load()"
      pattern: "from faster_whisper import WhisperModel"
    - from: "app/models/stt/protocol.py"
      to: "app/models/transcript.py"
      via: "SttTranscription reuses TranscriptSegment"
      pattern: "from app.models.transcript import TranscriptSegment"
    - from: "app/models/stt/adapter.py"
      to: "ctranslate2.models.Whisper.compute_type"
      via: "self._model.model.compute_type property read after load"
      pattern: "self._model.model.compute_type"
---

<objective>
Define the STTAdapter Protocol (D-06) and implement FasterWhisperAdapter — the single faster_whisper/ctranslate2 import site in the codebase. Pin the verified-compatible pair (D-08). Implement D-08 int8 verification that fails loud on silent float16 fallback (analogue of Phase 2 Pitfall 12). This plan delivers the transcribe() + detect_language() surface the chunker (03-02) and CLI (03-03) compose into the runnable end-to-end slice.

Purpose: This is the GPU-abstraction seam — everything outside app/models/stt depends on the Protocol, never the package (SC-4). The int8 verification is the load-bearing OOM defense that keeps large-v3 at ~2 GB on the 8 GB laptop (D-04).
Output: app/models/stt/{__init__,protocol,adapter}.py, pyproject pins, Wave 0 test stubs + FakeAdapter + conftest fixtures, green tests/test_stt_adapter.py + tests/test_stt_boundary.py.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-stt-adapter-audio-chunker-standalone-cli/03-CONTEXT.md
@.planning/phases/03-stt-adapter-audio-chunker-standalone-cli/03-RESEARCH.md
@.planning/phases/03-stt-adapter-audio-chunker-standalone-cli/03-PATTERNS.md
@.planning/phases/03-stt-adapter-audio-chunker-standalone-cli/03-VALIDATION.md

@app/models/transcript.py
@app/models/backend.py
@app/models/manager.py
@app/models/diagnostics.py
@app/models/registry.py
@app/storage/atomic.py
@app/settings/service.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define STTAdapter Protocol + result types + Wave 0 test stubs + FakeAdapter + pyproject pins (interface-first)</name>
  <files>app/models/stt/__init__.py, app/models/stt/protocol.py, tests/_stt_fake.py, tests/test_stt_adapter.py, tests/test_stt_boundary.py, tests/conftest.py, pyproject.toml</files>
  <read_first>
    - app/models/transcript.py (TranscriptSegment fields: start_s, end_s, text, speaker=None, confidence=None — the output shape the adapter fills)
    - app/models/backend.py lines 162-179 (BackendProvider Protocol — the exact Protocol shape to mirror: typing.Protocol, attribute + method stubs, module docstring explaining the seam, NO upstream import at module top)
    - app/models/diagnostics.py lines 35-82 (GpuBackend, InferenceEngine, ModelCategory enums — InferenceEngine.FASTER_WHISPER is the device_for key)
    - app/models/manager.py lines 37-39 + 311-315 (the Phase 2 boundary docstring statement + lazy in-body huggingface_hub import pattern to mirror for faster_whisper)
    - tests/conftest.py lines 229-265 (mock_hf_hub_download fixture — the exact monkeypatch-the-lazy-import-seam pattern to mirror for mock_stt_adapter)
    - tests/test_manager_download.py lines 22-60 (the test-file structure pattern: module docstring listing cases, _settings helper, @pytest.mark.asyncio, fixture usage)
    - pyproject.toml (current dependencies block lines 10-26 + absence of [project.scripts] — where to add the Phase 3 pins + the transcribe entry point)
    - .planning/phases/03-stt-adapter-audio-chunker-standalone-cli/03-RESEARCH.md Pattern 1 (Protocol sketch) + Pattern 2 (int8 _ACCEPTED equivalence table) + Validation Architecture (test-seam guidance + Wave 0 file list)
  </read_first>
  <behavior>
    - test_segment_mapping: FakeAdapter.transcribe returns segments with start=1.0,end=3.0,text="hi",avg_logprob=-0.1 -> SttTranscription.segments[0] has start_s=1.0, end_s=3.0, text="hi", confidence≈math.exp(-0.1)
    - test_language_autodetect_recorded: FakeAdapter with language=None returns SttTranscription.language == "en" and language_probability == 0.99
    - test_int8_verification_fails_loud: mock WhisperModel whose .model.compute_type returns "float32" while requested="int8_float16" -> FasterWhisperAdapter.load() raises RuntimeError whose message contains "int8 verification failed"
    - test_import_boundary: subprocess grep -rE "from faster_whisper|import faster_whisper|import ctranslate2" over app/ yields exactly ["app/models/stt/adapter.py"] (this test is RED until Task 2 creates adapter.py with the imports)
  </behavior>
  <action>
Define the contract and failing tests BEFORE the implementation (TDD RED). Per D-06, protocol.py declares `class STTAdapter(Protocol)` with stubs: `load(self) -> None`, `transcribe(self, audio: str | numpy.ndarray, language: str | None = None, vad_filter: bool = True, condition_on_previous_text: bool = True) -> SttTranscription`, `detect_language(self, audio: numpy.ndarray) -> tuple[str, float]`, `unload(self) -> None`. Use `from __future__ import annotations` + `from typing import Protocol` + `import numpy as np` + `from app.models.transcript import TranscriptSegment` (mirrors backend.py lines 41-47). Declare `@dataclass class SttSegment` with `start_s: float`, `end_s: float`, `text: str`, `confidence: float | None` and `@dataclass class SttTranscription` with `segments: list[SttSegment]`, `language: str`, `language_probability: float`, `duration: float`. Module docstring explains the seam (mirrors backend.py BackendProvider docstring). protocol.py MUST NOT import faster_whisper/ctranslate2.

Create tests/_stt_fake.py with `class FakeAdapter` implementing STTAdapter Protocol: `__init__(self, segments, language="en", language_probability=0.99, duration=30.0, oom_on_call: int | None = None)`; `load()` records call; `transcribe(audio, language=None, vad_filter=True, condition_on_previous_text=True)` raises RuntimeError("CUDA failed with error out of memory") on the Nth call when oom_on_call is set, else returns SttTranscription with the configured segments (offset by 0 for the chunker to add later); `detect_language(audio)` returns (self.language, self.language_probability); `unload()` no-op. Track `call_count` and `transcribe_calls: list` for chunker tests. This fake is the reason the Protocol exists (D-06) — chunker + CLI tests depend on it, never on faster_whisper.

Add to tests/conftest.py two new fixtures mirroring mock_hf_hub_download (lines 229-265): `mock_stt_adapter(monkeypatch)` — import the real `faster_whisper` module (it becomes a real dep after the pyproject edit), build a MagicMock `WhisperModel` class whose `__init__(self, model_path, device, compute_type)` records args and sets `self.model = SimpleNamespace(compute_type=compute_type)` (so D-08 verification reads it), whose `transcribe(audio, language=None, vad_filter=True, condition_on_previous_text=True)` returns `(iter([SimpleNamespace(start=1.0,end=3.0,text="hi",avg_logprob=-0.1)]), SimpleNamespace(language="en",language_probability=0.99,duration=30.0))`, and whose `detect_language(audio, vad_filter=True)` returns ("en", 0.99, {}); `monkeypatch.setattr(faster_whisper, "WhisperModel", mock_class)`. Second fixture `fake_audio_array()` returns `numpy.zeros(16000 * 30, dtype="float32")` (30 s silence at 16 kHz) with numpy imported inside the fixture body (lazy-import discipline). Also expose a `mock_ct2_supported_compute_types(monkeypatch)` helper that patches `ctranslate2.get_supported_compute_types` to return {"int8","int8_float16","float16"} for cuda / {"int8","int8_float32","float32"} for cpu (used by the int8 test).

Create tests/test_stt_adapter.py with the four behavior cases above (module docstring lists them, mapping each to TRANS-01 / INGEST-06 / D-08). Use `@pytest.fixture` for a `_adapter(model_path, device, compute_type)` helper that builds FasterWhisperAdapter under mock_stt_adapter. int8 test sets the mock's compute_type to "float32" and asserts `pytest.raises(RuntimeError, match="int8 verification failed")`.

Create tests/test_stt_boundary.py: `test_import_boundary` runs `subprocess.run([sys.executable, "-c", "import subprocess,sys; ..."], check=True)` OR uses pathlib walk + re over app/ — collect files matching `re.compile(r"from faster_whisper|import faster_whisper|import ctranslate2")`, assert the sorted matching list equals `["app/models/stt/adapter.py"]`. This test is RED until Task 2.

Modify pyproject.toml: add a Phase 3 comment block after the Phase 2 block (lines 22-25) and add the pinned pair `"faster-whisper==1.2.1"` + `"ctranslate2==4.7.2"` (D-08 — the verified-compatible pair). Add `[project.scripts]` after the `[project]` table (before `[project.optional-dependencies]`) with `transcribe = "app.cli.transcribe:main"` (D-03 — the entry point 03-03 implements; declaring it now is harmless since the module ships in 03-03). Do NOT add nvidia-cublas-cu12 / nvidia-cuda-runtime-cu12 yet (RESEARCH Pitfall 1 / Open Q1 — deferred to the SC-5 checkpoint in 03-03).

Create app/models/stt/__init__.py re-exporting `STTAdapter`, `SttTranscription`, `SttSegment` (from .protocol), `FasterWhisperAdapter` (from .adapter — Task 2 creates it; for Task 1 import it lazily inside __getattr__ OR leave the re-export in place and accept that importing the package top triggers adapter.py module load, which is safe because adapter.py has NO top-level faster_whisper import). Use `__all__`.

Run `pytest tests/test_stt_adapter.py tests/test_stt_boundary.py -x` — expect RED (adapter.py does not exist yet, boundary test fails). Commit: `test(03-01): add STTAdapter Protocol + Wave 0 stubs + pyproject pins`.
  </action>
  <verify>
    <automated>pytest tests/test_stt_adapter.py tests/test_stt_boundary.py -x</automated>
  </verify>
  <acceptance_criteria>
    - app/models/stt/protocol.py contains `class STTAdapter(Protocol)` and does NOT contain `faster_whisper` or `ctranslate2`
    - app/models/stt/protocol.py contains `class SttTranscription` and `class SttSegment`
    - tests/_stt_fake.py contains `class FakeAdapter` with `transcribe`, `detect_language`, `load`, `unload` methods
    - tests/conftest.py contains `def mock_stt_adapter(` and `def fake_audio_array(`
    - tests/test_stt_adapter.py contains `test_segment_mapping`, `test_language_autodetect_recorded`, `test_int8_verification_fails_loud`
    - tests/test_stt_boundary.py contains `test_import_boundary`
    - pyproject.toml contains `faster-whisper==1.2.1` and `ctranslate2==4.7.2` and `[project.scripts]` and `transcribe = "app.cli.transcribe:main"`
    - pytest tests/test_stt_adapter.py tests/test_stt_boundary.py -x runs and shows the expected RED (import errors / boundary mismatch) — NOT a collection error from a missing conftest fixture
  </acceptance_criteria>
  <done>Protocol + result types + FakeAdapter + conftest fixtures + 4 failing tests + pyproject pins + [project.scripts] all in place; test suite collects and fails on adapter implementation, not on missing fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement FasterWhisperAdapter with lazy import + D-08 int8 verification (GREEN)</name>
  <files>app/models/stt/adapter.py</files>
  <read_first>
    - app/models/stt/protocol.py (Task 1 output — the contract to implement)
    - app/models/manager.py lines 37-39 (boundary docstring statement to mirror) + lines 311-315 (lazy in-body import idiom) + lines 65-133 (typed error hierarchy pattern — model for SttInt8VerificationError if introduced)
    - app/models/hf_token.py lines 41-51 (module-scope seam with lazy import inside the body, for monkeypatch.setattr — pattern if a _load_whisper_model factory is exposed)
    - app/models/transcript.py (TranscriptSegment fields to populate)
    - .planning/phases/03-stt-adapter-audio-chunker-standalone-cli/03-RESEARCH.md Pattern 2 (_ACCEPTED equivalence table) + Code Examples "WhisperModel load + int8 verification" + Pitfall 2 (int8 → int8_float16 on CUDA) + Pitfall 5 (match "out of memory" substring) + Pitfall 6 (Segment is a dataclass, use attribute access) + Pitfall 7 (segments generator is lazy, materialize with list())
  </read_first>
  <behavior>
    - test_segment_mapping: passing — FasterWhisperAdapter under mock_stt_adapter, transcribe(fake_audio_array) returns SttTranscription.segments[0] with start_s=1.0, end_s=3.0, text="hi", confidence=math.exp(-0.1) (rounded)
    - test_language_autodetect_recorded: passing — transcribe with language=None returns SttTranscription.language == "en", language_probability == 0.99
    - test_int8_verification_fails_loud: passing — mock WhisperModel whose .model.compute_type returns "float32" while requested="int8_float16" -> load() raises RuntimeError matching "int8 verification failed"
    - test_import_boundary: passing — grep matches only app/models/stt/adapter.py
  </behavior>
  <action>
Implement FasterWhisperAdapter making all four Task 1 tests GREEN (TDD GREEN). Per D-06, this is the ONLY file in app/ that imports faster_whisper or ctranslate2. Module docstring MUST contain the boundary statement: "faster_whisper and ctranslate2 are imported ONLY inside this module (the boundary check `grep -rE "from faster_whisper|import faster_whisper|import ctranslate2" app/` matches only app/models/stt/adapter.py — SC-4, mirrors the Phase 2 huggingface_hub boundary in app/models/manager.py)." Mirror manager.py lines 37-39.

Module-level: `from __future__ import annotations`, `import logging`, `import math`, `import re`, `from types import SimpleNamespace` (only if needed), `from typing import Any`. Import `STTAdapter`, `SttTranscription`, `SttSegment` from `app.models.stt.protocol`, `TranscriptSegment` from `app.models.transcript`. NO top-level `faster_whisper` / `ctranslate2` / `numpy` import (lazy inside method bodies, mirroring manager.py lines 311-315).

Define the D-08 equivalence table at module scope (per RESEARCH Pattern 2): `_ACCEPTED: dict[str, set[str]]` with keys "int8" -> {"int8","int8_float16","int8_float32"}, "int8_float16" -> {"int8_float16"}, "int8_float32" -> {"int8_float32"}, "float16" -> {"float16","int8_float16"}. Source: opennmt.net/CTranslate2/quantization.html + verified `get_supported_compute_types('cpu',0) == {'int8','int8_float32','float32'}`.

`class FasterWhisperAdapter` with `__init__(self, model_path: str, device: str, compute_type: str) -> None` storing the three resolved values (per PATTERNS: the adapter receives already-resolved values; it does NOT call current() or device_for itself — keeps it a pure transform the chunker/CLI can compose). `self._model = None` (loaded lazily).

`load(self) -> None`: lazy `from faster_whisper import WhisperModel` + `import ctranslate2` inside the body (type: ignore[import-not-found]). Call `ctranslate2.get_supported_compute_types(self._device, 0)` and log it via `_log.info("CT2 supported compute_types on %s: %s", self._device, supported)` (structured logging, mirrors manager.py lines 474-489). Construct `self._model = WhisperModel(self._model_path, device=self._device, compute_type=self._compute_type)`. D-08 int8 verification: `actual = self._model.model.compute_type` (the inner ctranslate2.models.Whisper exposes the property — verified present). If `actual not in _ACCEPTED.get(self._compute_type, {self._compute_type})`, raise `RuntimeError(f"int8 verification failed: requested compute_type={self._compute_type!r} but loaded={actual!r} supported={supported} (silent fallback — Phase 2 Pitfall 12 analogue)")`. This is the fail-loud check — accepts the documented int8→int8_float16 equivalence (Pitfall 2) so it does NOT false-positive on CUDA, but fails on a float32 fallback (Pitfall 3). Emit a structured INFO log line recording requested vs loaded compute_type (SC-2-style).

`transcribe(self, audio, language=None, vad_filter=True, condition_on_previous_text=True) -> SttTranscription`: call `self._model.transcribe(audio, language=language, vad_filter=vad_filter, condition_on_previous_text=condition_on_previous_text)` (leave chunk_length at the default 30 — Pitfall: do NOT set a non-30 value). Materialize the lazy generator: `segments = list(segments_iter)` (Pitfall 7). Map each `seg` (dataclass — Pitfall 6, use attribute access `seg.start`, `seg.end`, `seg.text`, `seg.avg_logprob`) to `SttSegment(start_s=seg.start, end_s=seg.end, text=seg.text.strip(), confidence=math.exp(seg.avg_logprob))` (RESEARCH A5 — logprob → [0,1]). Return `SttTranscription(segments=..., language=info.language, language_probability=info.language_probability, duration=info.duration)`. When `language=None`, faster-whisper auto-detects on the first 30 s (D-07) and the detected language is recorded — INGEST-06.

`detect_language(self, audio) -> tuple[str, float]`: call `self._model.detect_language(audio, vad_filter=True)` (verified signature returns (lang, probability, all_lang_probs)); return `(lang, probability)`.

`unload(self) -> None`: set `self._model = None` (idempotent, mirrors manager.py unload D-03).

Define `class SttInt8VerificationError(RuntimeError)` if a typed error is preferred (mirror manager.py lines 65-133 ModelManagerError hierarchy) — OPTIONAL; a plain RuntimeError with the "int8 verification failed" prefix satisfies the test. If introduced, raise it instead of RuntimeError.

Run `pytest tests/test_stt_adapter.py tests/test_stt_boundary.py -x` — expect GREEN. Then run the full suite `pytest` to confirm no regression (~188 existing tests). Commit: `feat(03-01): implement FasterWhisperAdapter + D-08 int8 verification`.
  </action>
  <verify>
    <automated>pytest tests/test_stt_adapter.py tests/test_stt_boundary.py -x && pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - app/models/stt/adapter.py contains `class FasterWhisperAdapter` with `load`, `transcribe`, `detect_language`, `unload` methods
    - app/models/stt/adapter.py module docstring contains the SC-4 boundary statement mentioning `grep -rE "from faster_whisper|import faster_whisper|import ctranslate2"` and `app/models/stt/adapter.py`
    - app/models/stt/adapter.py contains `from faster_whisper import WhisperModel` ONLY inside a method body (not at module top) — grep `^from faster_whisper` returns 0 matches, grep `from faster_whisper` returns matches only within indented method bodies
    - app/models/stt/adapter.py contains `self._model.model.compute_type` and `_ACCEPTED` and `int8 verification failed`
    - app/models/stt/adapter.py contains `segments = list(segments_iter)` (Pitfall 7 materialization) and `math.exp(seg.avg_logprob)` (confidence mapping)
    - app/models/stt/adapter.py does NOT call `current()` or `device_for` (the adapter is a pure transform receiving resolved values)
    - pytest tests/test_stt_adapter.py tests/test_stt_boundary.py -x exits 0
    - pytest -q (full suite) exits 0 with the existing ~188 tests still green
    - subprocess grep `from faster_whisper|import faster_whisper|import ctranslate2` over app/ returns exactly app/models/stt/adapter.py
  </acceptance_criteria>
  <done>FasterWhisperAdapter implemented with lazy import + D-08 int8 verification (fail-loud on float32 fallback, accepts int8→int8_float16 equivalence); all 4 Task 1 tests GREEN; SC-4 boundary holds; full suite green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**app/models/stt/protocol.py**
- `class STTAdapter(Protocol)` — methods: `load()`, `transcribe(audio, language=None, vad_filter=True, condition_on_previous_text=True) -> SttTranscription`, `detect_language(audio) -> tuple[str, float]`, `unload()`
- `@dataclass class SttSegment` — fields: `start_s: float`, `end_s: float`, `text: str`, `confidence: float | None`
- `@dataclass class SttTranscription` — fields: `segments: list[SttSegment]`, `language: str`, `language_probability: float`, `duration: float`

**app/models/stt/adapter.py**
- `class FasterWhisperAdapter` — `__init__(model_path, device, compute_type)`, `load()`, `transcribe(...)`, `detect_language(...)`, `unload()`
- `_ACCEPTED: dict[str, set[str]]` — D-08 compute_type equivalence table (int8 / int8_float16 / int8_float32 / float16)
- (optional) `class SttInt8VerificationError(RuntimeError)`
- Module docstring SC-4 boundary statement

**app/models/stt/__init__.py**
- `__all__` re-exporting STTAdapter, SttTranscription, SttSegment, FasterWhisperAdapter

**pyproject.toml**
- `faster-whisper==1.2.1` + `ctranslate2==4.7.2` in dependencies (D-08 pins)
- `[project.scripts]` table with `transcribe = "app.cli.transcribe:main"` (D-03 — module ships in 03-03)

**tests/_stt_fake.py**
- `class FakeAdapter` — `__init__(segments, language, language_probability, duration, oom_on_call)`, `load`, `transcribe`, `detect_language`, `unload`, `call_count`, `transcribe_calls`

**tests/conftest.py additions**
- `mock_stt_adapter` fixture (patches `faster_whisper.WhisperModel`)
- `fake_audio_array` fixture (30 s silence numpy array)
- `mock_ct2_supported_compute_types` helper

**tests/test_stt_adapter.py** — test_segment_mapping, test_language_autodetect_recorded, test_int8_verification_fails_loud
**tests/test_stt_boundary.py** — test_import_boundary
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI args → adapter.transcribe | untrusted file path + language string cross here (Phase 3 CLI; adapter receives already-resolved values) |
| on-disk audio file → PyAV/FFmpeg decoder | user-supplied local file is decoded by bundled FFmpeg (trusted local input per PROJECT.md single-user) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `<file>` / `--out` path args | accept | Local single-user CLI (PROJECT.md); Path.resolve()+exists() validation lives in the CLI (03-03), not the adapter. The adapter receives a resolved model_path and a numpy array/path from the chunker — no untrusted path handling in this plan. |
| T-03-02 | Tampering | malicious audio → PyAV/FFmpeg parser | accept | PyAV bundles a fixed FFmpeg build in the wheel (D-01); faster-whisper's decode_audio wraps av.error.InvalidDataError and skips invalid frames. Input is user-supplied local files (trusted). |
| T-03-SC | Tampering | pip install faster-whisper==1.2.1 + ctranslate2==4.7.2 | mitigate | Both flagged OK in RESEARCH.md Package Legitimacy Audit (flagship, millions/wk, SYSTRAN/OpenNMT repos). Verified installed + importing on this machine. nvidia-*-cu12 packages are [ASSUMED] — NOT added in this plan; deferred to the SC-5 checkpoint in 03-03 which gates any CUDA-runtime-lib pip install on a blocking-human verify. |

No high-severity threats. No V2/V3/V4/V6 categories apply (local single-user, no auth, no sessions, no crypto, no network beyond the Phase 2 huggingface_hub download already audited).
</threat_model>

<verification>
- pytest tests/test_stt_adapter.py tests/test_stt_boundary.py -x (per-task, ~5s)
- pytest -q (full suite, ~15s — ~188 existing + new, no regressions)
- Manual boundary grep: `grep -rE "from faster_whisper|import faster_whisper|import ctranslate2" app/` returns exactly app/models/stt/adapter.py
- D-08 int8 verification: the int8 test proves the fail-loud path fires on a float32 fallback; the _ACCEPTED table proves the int8→int8_float16 CUDA equivalence is accepted (no false positive)
</verification>

<success_criteria>
- STTAdapter Protocol + SttTranscription/SttSegment types defined in app/models/stt/protocol.py (D-06)
- FasterWhisperAdapter implemented in app/models/stt/adapter.py — the ONLY faster_whisper/ctranslate2 import site (SC-4)
- D-08 int8 verification fails loud on silent float32 fallback, accepts the int8→int8_float16 CUDA equivalence
- faster-whisper==1.2.1 + ctranslate2==4.7.2 pinned in pyproject.toml; [project.scripts] transcribe entry declared
- TRANS-01 (segment mapping) + INGEST-06 (language auto-detect recorded) covered by green tests
- Wave 0 test stubs + FakeAdapter + conftest fixtures in place for 03-02 and 03-03 to reuse
- Full pytest suite green
</success_criteria>

<output>
Create `.planning/phases/03-stt-adapter-audio-chunker-standalone-cli/03-01-SUMMARY.md` when done
</output>