---
phase: 03-stt-adapter-audio-chunker-standalone-cli
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - app/cli/__init__.py
  - app/cli/transcribe.py
  - pyproject.toml
  - tests/test_cli_transcribe.py
autonomous: false
requirements: [TRANS-01, INGEST-06]
user_setup:
  - service: nvidia-cuda-runtime
    why: "CT2 win_amd64 wheel bundles cuDNN 9 but NOT cublas64_12.dll / cudart12.dll; the laptop's CUDA runtime libs must come from a system CUDA 12.x toolkit OR these pip packages (RESEARCH Pitfall 1 / Open Q1). Required ONLY for SC-5's laptop CUDA half; the desktop CPU path is unaffected."
    env_vars: []
    dashboard_config:
      - task: "On the laptop, run: python -c \"import ctranslate2; print(ctranslate2.get_supported_compute_types('cuda',0))\". If int8/int8_float16 are present, a system CUDA toolkit already provides the libs — no pip install needed. If only float32 appears (or cublas64_12.dll not found), run: pip install nvidia-cublas-cu12 nvidia-cuda-runtime-cu12."
        location: "laptop terminal (the CLI's CUDA target machine)"

must_haves:
  truths:
    - "A `transcribe` console_scripts entry point exists (declared in this plan's pyproject.toml edit, NOT in 03-01 — per Codex HIGH: declaring it before app.cli.transcribe exists breaks editable installs) and runs `transcribe <file>` to produce a transcript.json (D-03, SC-1)"
    - "The CLI resolves device from persisted settings.backend via device_for(backend, InferenceEngine.FASTER_WHISPER) when --device is auto (the default) (SC-5 — no per-machine flags); --device auto is a VALID argparse choice (Codex HIGH: 'auto' is in the choices list alongside cuda/cpu/rocm)"
    - "The CLI bootstraps settings AND the model manager before any use: _bootstrap_settings() calls load_settings_from_disk() then configure() BEFORE current(); if the model manager is not configured, configure_manager(ModelManager(settings)) is called BEFORE get_manager() (Codex HIGH: precise bootstrap ordering)"
    - "Default compute_type is int8_float16 on CUDA, int8 on CPU (D-04); --compute-type overrides"
    - "--language forces a language and skips auto-detect; omitting --language auto-detects (D-07, INGEST-06)"
    - "The CLI writes Transcript JSON via atomic_write_json to --out (default <input>.transcript.json — the accepted interpretation of SC-1's 'writes transcript.json' per Codex MEDIUM) (TRANS-01, D-03, Phase 1 D-04 atomic writes)"
    - "The CLI validates <file> exists and --out's parent dir exists before transcribing (V5 input validation, T-03-02); writability is not pre-checked (cross-platform unreliable — Codex MEDIUM: atomic_write_json reports failures clearly instead)"
    - "The CLI never imports faster_whisper or ctranslate2 (SC-4) — it depends on the STTAdapter Protocol + transcribe_file + FasterWhisperAdapter factory"
    - "The CLI calls adapter.unload() in a finally block after transcription/write (Codex suggestion: VRAM cleanup on error); raw RuntimeError messages are preserved to stderr (Codex MEDIUM: do not mask int8/CUDA errors as a generic failure)"
    - "SC-5 laptop half: a human verifies on the CUDA laptop that ctranslate2.get_supported_compute_types('cuda',0) shows int8/int8_float16 and `transcribe <small-file>` produces a transcript.json (BLOCKER Open Q1 checkpoint)"
    - "SC-5 desktop half: a human verifies on the CPU desktop that `transcribe <small-file>` completes on CPU fallback with no code changes"
  artifacts:
    - path: "app/cli/transcribe.py"
      provides: "argparse CLI + main() entry point + settings/model-manager bootstrap + device/compute_type resolution + atomic write + stdout summary + path validation + finally-block unload"
      contains: "def main"
    - path: "app/cli/__init__.py"
      provides: "package init"
      contains: ""
    - path: "pyproject.toml"
      provides: "[project.scripts] transcribe entry point (declared here, NOT in 03-01 — per Codex HIGH)"
      contains: "transcribe ="
    - path: "tests/test_cli_transcribe.py"
      provides: "SC-5 device resolution (--device auto exercised), D-04 default compute_type per device, --language force skips detect, path validation, bootstrap ordering, model-manager configured before use, adapter unload on error"
      contains: "test_device_resolution_from_settings"
  key_links:
    - from: "app/cli/transcribe.py"
      to: "app/models/stt/chunker.py"
      via: "transcribe_file(adapter, file, language=...) — the CLI is a thin caller of the Protocol, never imports faster_whisper"
      pattern: "from app.models.stt.chunker import transcribe_file"
    - from: "app/cli/transcribe.py"
      to: "app/models/backend.py"
      via: "device_for(settings.backend, InferenceEngine.FASTER_WHISPER) for --device auto"
      pattern: "device_for\\("
    - from: "app/cli/transcribe.py"
      to: "app/settings/service.py"
      via: "load_settings_from_disk() + configure() bootstrap BEFORE current()"
      pattern: "load_settings_from_disk"
    - from: "app/cli/transcribe.py"
      to: "app/models/manager.py"
      via: "configure_manager(ModelManager(settings)) BEFORE get_manager() when the manager is not configured"
      pattern: "configure_manager"
    - from: "app/cli/transcribe.py"
      to: "app/storage/atomic.py"
      via: "asyncio.run(atomic_write_json(out_path, transcript.model_dump()))"
      pattern: "atomic_write_json"
---

<objective>
Implement the standalone `transcribe` CLI that ties the STT adapter (03-01) and the audio chunker (03-02) into the runnable end-to-end slice: `transcribe <file>` → transcript.json. Resolve device + compute_type from persisted settings via the Phase 2 device_for seam (SC-5 — same command on laptop CUDA and desktop CPU with no code changes). Bootstrap settings AND the model manager precisely without the FastAPI lifespan (the PATTERNS CLI settings-bootstrap gap, tightened per Codex HIGH): _bootstrap_settings() runs load_settings_from_disk → configure before current(); configure_manager runs before get_manager() when the manager is not configured. Declare the `transcribe` console_scripts entry point here (moved out of 03-01 per Codex HIGH — declaring it before app.cli.transcribe exists breaks editable installs). Add `auto` to the --device argparse choices (Codex HIGH: `--device auto` was previously rejected). Then gate SC-5's "runs on both machines" half on a blocking human-verify checkpoint that confirms the laptop's CUDA runtime libs are findable by CTranslate2 (BLOCKER Open Q1) and that the CLI actually runs end-to-end on each machine.

Purpose: This is the GPU-abstraction proof (SC-5) and the Phase 3 vertical slice completing the file → adapter → transcript.json path. The checkpoint closes RESEARCH Open Q1 — the single biggest threat to the silent-no-install laptop promise.
Output: app/cli/transcribe.py, app/cli/__init__.py, pyproject.toml ([project.scripts] transcribe entry), tests/test_cli_transcribe.py (green), full suite green, SC-5 human-verify checkpoint resolved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-stt-adapter-audio-chunker-standalone-cli/03-CONTEXT.md
@.planning/phases/03-stt-adapter-audio-chunker-standalone-cli/03-RESEARCH.md
@.planning/phases/03-stt-adapter-audio-chunker-standalone-cli/03-PATTERNS.md
@.planning/phases/03-stt-adapter-audio-chunker-standalone-cli/03-VALIDATION.md

@app/models/stt/protocol.py
@app/models/stt/adapter.py
@app/models/stt/chunker.py
@app/models/transcript.py
@app/models/backend.py
@app/models/diagnostics.py
@app/models/registry.py
@app/models/manager.py
@app/settings/service.py
@app/storage/atomic.py
@app/storage/fs.py
@pyproject.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 CLI test stubs — device resolution (--device auto), default compute_type per device, --language force skips detect, path validation, bootstrap ordering, model-manager configured before use, adapter unload on error (RED)</name>
  <files>tests/test_cli_transcribe.py</files>
  <read_first>
    - tests/test_settings_phase2.py (closest existing "exercise a settings-reading entry point" test — the monkeypatch.current + client pattern to mirror)
    - tests/_stt_fake.py (FakeAdapter — the test double for transcribe_file mocking)
    - app/settings/service.py lines 86-147 (load_settings_from_disk signature + configure + current — the bootstrap chain the CLI must call)
    - app/models/manager.py lines 241-280 (ensure_downloaded) + lines 554-567 (the configure_manager-if-not-configured / get_manager pattern the CLI must mirror precisely — Codex HIGH)
    - app/models/backend.py lines 436-447 (device_for(backend, InferenceEngine.FASTER_WHISPER) — the SC-5 seam)
    - app/models/diagnostics.py lines 35-82 (GpuBackend.CUDA/CPU, InferenceEngine.FASTER_WHISPER, QualityPreset)
    - .planning/phases/03-stt-adapter-audio-chunker-standalone-cli/03-VALIDATION.md rows for 03-03 (the test commands)
    - .planning/phases/03-stt-adapter-audio-chunker-standalone-cli/03-RESEARCH.md Code Examples "CLI entry point" (the argparse sketch to implement in Task 2)
  </read_first>
  <behavior>
    - test_device_resolution_from_settings_cuda: monkeypatch app.cli.transcribe.current to return Settings(backend=GpuBackend.CUDA); monkeypatch app.cli.transcribe.transcribe_file to capture kwargs; run main(["audio.wav", "--device", "auto"]); assert transcribe_file was called with device="cuda" (SC-5 — device resolves from settings, no per-machine flag). Confirms `--device auto` is ACCEPTED by argparse (Codex HIGH: 'auto' must be in choices).
    - test_device_resolution_from_settings_cpu: same with Settings(backend=GpuBackend.CPU); assert device="cpu"
    - test_device_auto_is_valid_choice: run `main(["audio.wav", "--device", "auto"])` under the cuda-settings mock; assert it does NOT raise SystemExit(2) from argparse rejecting 'auto' (regression guard for the Codex HIGH fix — 'auto' must be in the argparse choices).
    - test_default_compute_type_per_device: parameterized — device="cuda" -> compute_type="int8_float16"; device="cpu" -> compute_type="int8" (D-04). Assert transcribe_file received the right compute_type when --compute-type is omitted.
    - test_compute_type_override: --compute-type int8_float32 on cuda -> transcribe_file called with compute_type="int8_float32" (D-04 escape hatch wins over the default)
    - test_language_force_skips_detect: --language en -> transcribe_file called with language="en" (D-07 force, skips auto-detect). Omitting --language -> language=None (auto-detect path).
    - test_default_out_path: --out omitted -> output path is <input>.transcript.json (Path("audio.wav").with_suffix(".transcript.json")) — the accepted interpretation of SC-1 (Codex MEDIUM: filename is <stem>.transcript.json, not literally "transcript.json")
    - test_atomic_write_called: monkeypatch atomic_write_json to capture (path, payload); assert it was called once with the resolved out_path and transcript.model_dump()
    - test_stdout_summary: capsys; main prints a line containing "language=", "segments=", and the out path
    - test_missing_file_errors: main(["nonexistent.wav"]) exits non-zero with a clear error mentioning the missing file (V5 path validation, T-03-02). The parent-dir-writability check is NOT asserted (Codex MEDIUM: writability is not pre-checked; atomic_write_json reports failures instead).
    - test_cli_does_not_import_faster_whisper: import app.cli.transcribe; assert "faster_whisper" not in dir(app.cli.transcribe) and the module source has no top-level faster_whisper/ctranslate2 import (SC-4)
    - test_bootstrap_settings_runs_before_current: monkeypatch app.cli.transcribe.load_settings_from_disk, app.cli.transcribe.configure, and app.cli.transcribe.current to record calls (a list-backed recorder appending the function name on each call); mock transcribe_file + atomic_write_json so main runs the happy path. Run main(["audio.wav"]). Assert the recorded call order is ["load_settings_from_disk", "configure", "current"] - i.e. _bootstrap_settings() fires load_settings_from_disk + configure BEFORE current() returns (W2 - the PATTERNS CLI settings-bootstrap gap is behaviorally guarded, not just source-textually ordered). test_device_resolution_from_settings_cuda / _cpu (which monkeypatch current directly and bypass the bootstrap path) do NOT cover this ordering - this test does.
    - test_cli_configures_model_manager_when_unconfigured: monkeypatch app.cli.transcribe.get_manager to raise RuntimeError("not configured") on the first call (simulating an unconfigured manager, as in a standalone CLI with no FastAPI lifespan) and to return a MagicMock manager on the second call (after configure_manager); monkeypatch app.cli.transcribe.configure_manager to record its call; mock ensure_downloaded + transcribe_file + atomic_write_json so main runs the happy path. Run main(["audio.wav"]). Assert configure_manager was called BEFORE the successful get_manager() call (Codex HIGH: precise model-manager bootstrap ordering — configure_manager runs before get_manager() when the manager is not configured).
    - test_adapter_unload_on_error: monkeypatch app.cli.transcribe.transcribe_file to raise RuntimeError("boom"); build the CLI's adapter-build path to return a MagicMock adapter with an `unload` recorder; run main(["audio.wav"]); assert adapter.unload() was called (Codex suggestion: unload in a finally block so VRAM is released even on transcription/write errors) AND the RuntimeError's raw message ("boom") is preserved to stderr (Codex MEDIUM: do not mask the original error as a generic failure).
  </behavior>
  <action>
Create tests/test_cli_transcribe.py with the behavior cases above (TDD RED). Module docstring lists the cases and maps each to SC-5 / D-04 / D-07 / TRANS-01 / T-03-02 / Codex HIGH (--device auto, bootstrap+manager ordering) / Codex MEDIUM (path validation, error preservation). Use `from __future__ import annotations`, `import sys`, `from pathlib import Path`, `import pytest`. Import `from app.cli.transcribe import main` (RED — module does not exist), `from app.models.diagnostics import GpuBackend, InferenceEngine`, `from app.models.settings import Settings`.

Pattern for invoking main: `monkeypatch.setattr(sys, "argv", ["transcribe", str(file), ...])` then call `main()` and assert the return code (0 on success, non-zero on arg/path errors). Mock `transcribe_file` at `app.cli.transcribe.transcribe_file` to return a `Transcript(schema_version=1, job_id="test", language="en", segments=[TranscriptSegment(start_s=0.0, end_s=1.0, text="hi")])` — avoids running the real pipeline. Mock `atomic_write_json` at `app.cli.transcribe.atomic_write_json` (async) to capture the payload without touching disk. Mock `current` at `app.cli.transcribe.current` (or `load_settings_from_disk` + `configure`) to return a controlled Settings per case. Mock `get_manager` / `configure_manager` / `ensure_downloaded` at `app.cli.transcribe.{get_manager,configure_manager}` for the manager-bootstrap and happy-path tests (the adapter build path must not hit the real model manager).

For the path-validation test: use a `tmp_path` file that does NOT exist; assert `main(["nonexistent.wav"])` raises SystemExit (non-zero) OR returns non-zero with a stderr/stdout message containing "not found" / "does not exist". Do NOT assert a writability check on --out's parent (Codex MEDIUM: writability is cross-platform-unreliable; the CLI checks parent exists and lets atomic_write_json report write failures).

For test_cli_does_not_import_faster_whisper: `import inspect, app.cli.transcribe as m; src = inspect.getsource(m); assert "import faster_whisper" not in src and "import ctranslate2" not in src and "from faster_whisper" not in src`.

For test_adapter_unload_on_error: the CLI must call adapter.unload() in a `finally` block so VRAM is released even when transcribe_file or atomic_write_json raises; the raw exception message is printed to stderr (not swallowed/replaced with a generic message).

Run `pytest tests/test_cli_transcribe.py -x` — expect RED (ImportError on app.cli.transcribe). Commit: `test(03-03): add CLI Wave 0 stubs (--device auto, bootstrap+manager ordering, unload on error)`.
  </action>
  <verify>
    <automated>pytest tests/test_cli_transcribe.py -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_cli_transcribe.py contains test_device_resolution_from_settings_cuda, test_device_resolution_from_settings_cpu, test_device_auto_is_valid_choice, test_default_compute_type_per_device, test_compute_type_override, test_language_force_skips_detect, test_default_out_path, test_atomic_write_called, test_stdout_summary, test_missing_file_errors, test_cli_does_not_import_faster_whisper, test_bootstrap_settings_runs_before_current, test_cli_configures_model_manager_when_unconfigured, test_adapter_unload_on_error
    - `pytest tests/test_cli_transcribe.py::test_bootstrap_settings_runs_before_current -x` is a concrete per-test command asserting the load_settings_from_disk -> configure -> current call order via monkeypatch call recording (W2)
    - `pytest tests/test_cli_transcribe.py::test_cli_configures_model_manager_when_unconfigured -x` asserts configure_manager runs before get_manager() when the manager is unconfigured (Codex HIGH)
    - `pytest tests/test_cli_transcribe.py::test_device_auto_is_valid_choice -x` asserts `--device auto` is accepted (Codex HIGH)
    - `pytest tests/test_cli_transcribe.py::test_adapter_unload_on_error -x` asserts unload() runs in a finally block and the raw error message is preserved (Codex MEDIUM + suggestion)
    - tests/test_cli_transcribe.py imports `from app.cli.transcribe import main`
    - pytest tests/test_cli_transcribe.py -x runs and shows the expected RED (ImportError on app.cli.transcribe) — NOT a collection error from a missing fixture
  </acceptance_criteria>
  <done>Fourteen failing CLI tests in place covering SC-5 device resolution (cuda + cpu + --device auto as a valid choice), D-04 default compute_type + override, D-07 --language force vs auto-detect, default --out path, atomic_write call, stdout summary, V5 path validation (no writability check), SC-4 (no faster_whisper import), the W2 bootstrap-order test, the Codex HIGH model-manager-configured-before-use test, and the Codex unload-on-error / raw-message-preservation test; suite collects and fails on the missing CLI module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement app/cli/transcribe.py + declare [project.scripts] transcribe — argparse, settings+manager bootstrap, device/compute_type resolution, atomic write, finally-block unload, path validation (GREEN)</name>
  <files>app/cli/__init__.py, app/cli/transcribe.py, pyproject.toml</files>
  <read_first>
    - tests/test_cli_transcribe.py (Task 1 output — the contract: main() signature, mocked seams at app.cli.transcribe.{transcribe_file, atomic_write_json, current, load_settings_from_disk, configure, get_manager, configure_manager}, behavior expectations including --device auto, bootstrap ordering, manager-configured-before-use, finally-block unload)
    - app/settings/service.py lines 86-147 (load_settings_from_disk(path=None) -> (Settings, pending), configure(settings), current() — the bootstrap chain; current() raises RuntimeError("settings not configured (lifespan not installed)") if configure was never called — the PATTERNS gap the CLI MUST close)
    - app/storage/fs.py lines 50-60 (bootstrap_settings_path() — the default path load_settings_from_disk reads when path=None)
    - app/models/backend.py lines 436-447 (device_for(backend, InferenceEngine.FASTER_WHISPER) — returns "cuda" or "cpu")
    - app/models/diagnostics.py (GpuBackend, InferenceEngine, QualityPreset enums)
    - app/models/registry.py lines 91-128 (REGISTRY + get_spec — resolve "{preset}.stt" -> ModelSpec)
    - app/models/manager.py lines 241-280 (ensure_downloaded async signature) + lines 554-567 (the configure_manager-if-not-configured / get_manager pattern — mirror this PRECISELY per Codex HIGH)
    - app/storage/atomic.py lines 61-64 (atomic_write_json(path, payload) — async, pretty-printed)
    - app/models/transcript.py (Transcript.job_id is REQUIRED — the CLI must supply it; per PATTERNS the CLI does NOT create a data/jobs/<id>/ dir, so job_id is a logical label = the input filename stem)
    - pyproject.toml (where 03-01 added faster-whisper + ctranslate2 pins; this task adds [project.scripts] transcribe = "app.cli.transcribe:main" here, NOT in 03-01 — per Codex HIGH)
    - .planning/phases/03-stt-adapter-audio-chunker-standalone-cli/03-RESEARCH.md Code Examples "CLI entry point" (the argparse sketch) + Open Q1 (the SC-5 checkpoint)
    - .planning/phases/03-stt-adapter-audio-chunker-standalone-cli/03-PATTERNS.md `app/cli/transcribe.py` section (the settings-bootstrap gap decision + atomic write pattern + imports pattern + argparse shape)
  </read_first>
  <behavior>
    - All fourteen Task 1 tests pass (GREEN) - including test_bootstrap_settings_runs_before_current (call recorder asserts load_settings_from_disk + configure fire before current() returns), test_cli_configures_model_manager_when_unconfigured (configure_manager runs before get_manager() when unconfigured), test_device_auto_is_valid_choice (--device auto accepted), and test_adapter_unload_on_error (unload in finally + raw error preserved)
    - Full suite green (~188 + new)
  </behavior>
  <action>
Implement app/cli/transcribe.py making all fourteen Task 1 tests GREEN (TDD GREEN). Per D-03, this plan declares the `transcribe` console_scripts entry point in pyproject.toml (03-01 deferred it to here per Codex HIGH — declaring it before app.cli.transcribe exists breaks editable installs / smoke tests in Waves 1-2). Per SC-4, the CLI NEVER imports faster_whisper or ctranslate2 — it depends on the Protocol + transcribe_file + FasterWhisperAdapter factory.

Module-level: `from __future__ import annotations`, `import argparse`, `import asyncio`, `import sys`, `from pathlib import Path`. Project imports: `from app.models.backend import device_for`, `from app.models.diagnostics import GpuBackend, InferenceEngine, ModelCategory, QualityPreset`, `from app.models.stt import FasterWhisperAdapter`, `from app.models.stt.chunker import transcribe_file`, `from app.models.transcript import Transcript`, `from app.models.registry import get_spec, get_category`, `from app.models.manager import get_manager, configure_manager, ModelManager`, `from app.settings.service import current, configure, load_settings_from_disk`, `from app.storage.atomic import atomic_write_json`. NO faster_whisper/ctranslate2 import anywhere in this file (SC-4 — test_cli_does_not_import_faster_whisper enforces it).

`def _default_compute_type(device: str) -> str` (D-04): return "int8_float16" if device == "cuda" else "int8" (CPU and ROCm→CPU both use int8; CTranslate2 has no ROCm path per D-05).

`def _bootstrap_settings() -> None` (the PATTERNS CLI settings-bootstrap gap, tightened per Codex HIGH): the CLI is a standalone entry point with NO FastAPI lifespan, so `current()` would raise RuntimeError("settings not configured..."). Call `settings, _pending = load_settings_from_disk()` (reads from `bootstrap_settings_path()` when path=None — app/storage/fs.py line 50), then `configure(settings)`. Wrap in try/except FileNotFoundError — if the bootstrap file does not exist, surface a clear stderr message ("settings.json not found; run the back-end once to bootstrap it") and exit non-zero. This MUST run BEFORE any `current()` call. (test_bootstrap_settings_runs_before_current records the call order via monkeypatched load_settings_from_disk / configure / current; _bootstrap_settings() must ACTUALLY call load_settings_from_disk() then configure() — a behavioral guard, not a source-textual one (W2).)

`def _get_or_configure_manager(settings) -> ModelManager` (Codex HIGH: precise model-manager bootstrap — mirror manager.py lines 554-567): try `manager = get_manager()`; if that raises (manager not configured, as in a standalone CLI with no lifespan), call `configure_manager(ModelManager(settings))` then `manager = get_manager()`. Return the manager. This guarantees configure_manager runs BEFORE a successful get_manager() when the manager was unconfigured (test_cli_configures_model_manager_when_unconfigured asserts this ordering).

`def main() -> int`:
1. Build `argparse.ArgumentParser(prog="transcribe")` per D-03: positional `file` ( metavar `<file>`); `--preset` choices `["small","balanced","large"]` default `"balanced"`; `--device` choices `["auto","cuda","cpu","rocm"]` default `"auto"` (Codex HIGH: `auto` IS in the choices list so `--device auto` is accepted); `--language` default `None`; `--compute-type` choices `["int8","int8_float16","float16","int8_float32"]` default `None`; `--out` default `None`; `--verbose` action `store_true`. Parse args.
2. V5 path validation (T-03-02): resolve `file_path = Path(args.file).resolve()`; if not `file_path.exists()`, print `f"error: input file not found: {file_path}"` to stderr and return 2. Compute `out_path = Path(args.out).resolve() if args.out else file_path.with_suffix(".transcript.json")` (NOTE: with_suffix replaces .mp4 -> .transcript.json, satisfying test_default_out_path for "audio.wav" -> "audio.transcript.json" — this is the accepted interpretation of SC-1's "writes transcript.json" per Codex MEDIUM: filename is <stem>.transcript.json, not a literal "transcript.json"); check `out_path.parent.exists()` (NOT writability — Codex MEDIUM: writability checks are cross-platform-unreliable; if the parent does not exist, print an error and return 2; let atomic_write_json report any write failure clearly with its raw message).
3. `_bootstrap_settings()` (the PATTERNS gap fix — before current()).
4. `settings = current()`. Resolve device: `device = args.device if args.device != "auto" else device_for(settings.backend, InferenceEngine.FASTER_WHISPER)` (SC-5 — no per-machine flags; device_for returns "cuda" on CUDA, "cpu" on CPU). Resolve compute_type: `compute_type = args.compute_type or _default_compute_type(device)` (D-04).
5. Build the adapter: resolve the STT spec via `spec = get_spec(f"{args.preset}.stt")` + `category = get_category(f"{args.preset}.stt")` (registry.py). Get the model manager via `manager = _get_or_configure_manager(settings)` (Codex HIGH: configure_manager runs before get_manager() when unconfigured). Download the model if needed: `model_path = asyncio.run(manager.ensure_downloaded(spec, category))` (02-04 thread-offloaded, classic-resume). Build `adapter = FasterWhisperAdapter(model_path=str(model_path), device=device, compute_type=compute_type)`; `adapter.load()` (D-08 int8 verification runs here — fail loud on silent float16 fallback).
6. `try:` `transcript = transcribe_file(adapter, str(file_path), language=args.language, job_id=file_path.stem)` (D-07 — language=None auto-detects; --language forces and the chunker/adapter skips detect). transcript.job_id uses the filename stem (the CLI does NOT create a data/jobs/<id>/ dir per D-03). `asyncio.run(atomic_write_json(out_path, transcript.model_dump()))` (Phase 1 D-04 atomic writes). Print the stdout summary: `print(f"language={transcript.language} segments={len(transcript.segments)} -> {out_path}")` (D-03 one-line summary). Return 0.
   `except RuntimeError as exc:` print the RAW exception message to stderr (Codex MEDIUM: do NOT mask int8-verification or CUDA-DLL errors as a generic "transcription failed" — preserve `str(exc)` so the user sees e.g. "int8 verification failed: requested compute_type='int8_float16' but loaded='float32'..." or the cuBLAS/cudart missing-DLL message); return 1 (do NOT crash with a traceback).
   `finally:` `adapter.unload()` (Codex suggestion: VRAM cleanup on error — unload runs even when transcribe_file or atomic_write_json raised). Guard the finally with `if 'adapter' in locals() and adapter is not None:` so a failed adapter build does not double-raise.
   If `--verbose`, set the logging level to INFO before step 5 (the chunker already logs per-chunk progress via _log).

Create app/cli/__init__.py (empty package init, mirrors app/jobs/__init__.py minimal convention).

Declare the `[project.scripts]` entry point in pyproject.toml (Codex HIGH: moved here from 03-01): add `[project.scripts]` after the `[project]` table (before `[project.optional-dependencies]`) with `transcribe = "app.cli.transcribe:main"`. 03-01 added ONLY the faster-whisper + ctranslate2 dependency pins; the entry point ships here alongside the module it references.

Run `pytest tests/test_cli_transcribe.py -x` — expect GREEN. Run `pytest -q` — expect full suite green. Then `pip install -e .` (picks up the new [project.scripts] entry) and `transcribe --help` as a smoke check. Commit: `feat(03-03): implement transcribe CLI + settings+manager bootstrap + device resolution + atomic write + declare entry point`.
  </action>
  <verify>
    <automated>pytest tests/test_cli_transcribe.py -x && pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - app/cli/transcribe.py contains `def main(` and `def _default_compute_type(` and `def _bootstrap_settings(` and `def _get_or_configure_manager(`
    - app/cli/transcribe.py contains `device_for(settings.backend, InferenceEngine.FASTER_WHISPER)` (SC-5 device resolution)
    - app/cli/transcribe.py contains `load_settings_from_disk(` and `configure(` BEFORE any `current(` call (PATTERNS gap fix)
    - app/cli/transcribe.py contains `configure_manager(` before `get_manager()` (Codex HIGH: precise manager bootstrap) and `asyncio.run(atomic_write_json(` and `transcript.model_dump()`
    - app/cli/transcribe.py contains `argparse.ArgumentParser(prog="transcribe")` with `--device` choices INCLUDING `"auto"` (Codex HIGH) and all seven args (file, --preset, --device, --language, --compute-type, --out, --verbose)
    - app/cli/transcribe.py contains the V5 path validation (Path.resolve + exists check; returns 2 on missing file; checks parent EXISTS, not writability)
    - app/cli/transcribe.py contains a `finally:` block calling `adapter.unload()` (Codex suggestion) and the `except RuntimeError` branch prints `str(exc)` to stderr (Codex MEDIUM: raw message preserved)
    - app/cli/transcribe.py does NOT contain `import faster_whisper`, `from faster_whisper`, or `import ctranslate2` (SC-4 — test_cli_does_not_import_faster_whisper enforces)
    - app/cli/transcribe.py contains `int8_float16` (cuda default) and `int8` (cpu default) in _default_compute_type (D-04)
    - app/cli/__init__.py exists
    - pyproject.toml contains `[project.scripts]` with `transcribe = "app.cli.transcribe:main"` (declared HERE per Codex HIGH, NOT in 03-01)
    - pytest tests/test_cli_transcribe.py -x exits 0 (all fourteen tests GREEN, including test_bootstrap_settings_runs_before_current, test_cli_configures_model_manager_when_unconfigured, test_device_auto_is_valid_choice, test_adapter_unload_on_error)
    - `pytest tests/test_cli_transcribe.py::test_bootstrap_settings_runs_before_current -x` exits 0 (W2 behavioral guard: call recorder shows [load_settings_from_disk, configure, current] order)
    - `pytest tests/test_cli_transcribe.py::test_cli_configures_model_manager_when_unconfigured -x` exits 0 (Codex HIGH: configure_manager before get_manager when unconfigured)
    - `pytest tests/test_cli_transcribe.py::test_device_auto_is_valid_choice -x` exits 0 (Codex HIGH: --device auto accepted)
    - `pytest tests/test_cli_transcribe.py::test_adapter_unload_on_error -x` exits 0 (finally-block unload + raw message preserved)
    - pytest -q (full suite) exits 0
    - `pip install -e .` succeeds and `transcribe --help` prints the argparse help (the [project.scripts] entry point resolves now that app.cli.transcribe exists)
    - subprocess grep `from faster_whisper|import faster_whisper|import ctranslate2` over app/ still returns exactly app/models/stt/adapter.py (SC-4 boundary holds with the CLI added)
  </acceptance_criteria>
  <done>transcribe CLI implemented: argparse with all D-03 args (including --device auto as a valid choice — Codex HIGH), settings+model-manager bootstrap via load_settings_from_disk+configure before current() and configure_manager before get_manager() (Codex HIGH), SC-5 device resolution from settings.backend via device_for, D-04 default compute_type per device + override, D-07 --language force vs auto-detect, atomic_write_json output, stdout summary, finally-block adapter.unload() (Codex suggestion), raw-error-message preservation (Codex MEDIUM), V5 path validation (parent exists, not writability — Codex MEDIUM), no faster_whisper import (SC-4); [project.scripts] transcribe declared HERE in pyproject (Codex HIGH); all fourteen CLI tests GREEN; full suite green; `transcribe --help` resolves after `pip install -e .`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SC-5 human verify — CLI runs end-to-end on the CUDA laptop AND the CPU desktop (BLOCKER Open Q1)</name>
  <files></files>
  <read_first>
    - .planning/phases/03-stt-adapter-audio-chunker-standalone-cli/03-RESEARCH.md Pitfall 1 + Open Q1 + Assumptions A1/A6 (the CUDA-runtime-lib gap and the checkpoint rationale)
    - .planning/phases/03-stt-adapter-audio-chunker-standalone-cli/03-VALIDATION.md Manual-Only Verifications table (the two SC-5 halves and the exact laptop/desktop commands)
    - app/cli/transcribe.py (Task 2 output — the entry point being verified)
    - pyproject.toml (the [project.scripts] transcribe entry + the pins)
  </read_first>
  <action>
This is the BLOCKER Open Q1 gate from RESEARCH.md and the SC-5 "runs on both machines" success criterion. The automated suite mocks faster_whisper (no real GPU, no real model download); only a human can verify the real end-to-end path on each machine. This checkpoint is NOT auto-advanceable (workflow.auto_advance is ignored for blocking-human gates). It also closes the Codex HIGH CUDA-runtime concern: if the laptop needs nvidia-*-cu12 pip packages, the human installs them during this checkpoint and reports them as a MANDATORY follow-up to add to pyproject.toml (so future installs are silent — not a silent-first-run risk).

On the CUDA LAPTOP (SC-5 laptop half + Open Q1):
1. `pip install -e .` (picks up faster-whisper==1.2.1 + ctranslate2==4.7.2 + the [project.scripts] transcribe entry from pyproject).
2. Run `python -c "import ctranslate2; print(ctranslate2.get_supported_compute_types('cuda',0))"`.
   - If the output includes `int8` and/or `int8_float16` (NOT just `float32`): the CUDA runtime libs (cublas64_12.dll / cudart12.dll) are findable by CT2 — proceed to step 3. Note whether a system CUDA 12.x toolkit provided them OR the `nvidia-cublas-cu12` + `nvidia-cuda-runtime-cu12` pip packages were needed.
   - If the output is only `float32`, OR you see `RuntimeError: Library cublas64_12.dll is not found`: run `pip install nvidia-cublas-cu12 nvidia-cuda-runtime-cu12` and re-run the command. If `int8`/`int8_float16` now appear, proceed. If they still do not appear, STOP and report — SC-5's laptop CUDA half cannot pass without a system CUDA 12.x toolkit; the desktop CPU half (below) still works.
3. Ensure the STT model is downloaded (first run pulls it via the 02-04 thread-offloaded path): the CLI calls `manager.ensure_downloaded` automatically.
4. Run `transcribe <small-audio-file> --out out.json` (use any short local audio/video file — a 1-3 min MP4 or WAV). Expected: stdout summary `language=<code> segments=<N> -> out.json`, and `out.json` contains a valid `Transcript` JSON with `schema_version=1`, `job_id`, `language`, and a non-empty `segments` array with `start_s`/`end_s`/`text`. Confirm `compute_type` was `int8_float16` (check the adapter's INFO log if --verbose). If a RuntimeError is printed to stderr, paste the RAW message (Codex MEDIUM: the CLI preserves the original error, e.g. an int8-verification failure or a CUDA-DLL-missing message).

On the CPU DESKTOP (SC-5 desktop half):
5. `pip install -e .` on the desktop.
6. Run `transcribe <small-audio-file> --out out.json` with NO --device flag (defaults to auto → settings.backend → CPU via device_for). Expected: completes on CPU fallback with no code changes (SC-5), same output shape, `compute_type=int8`.

Report results: for each machine, paste the stdout summary line and confirm `out.json` has segments. If the laptop required the `nvidia-*-cu12` pip packages, report that as a MANDATORY follow-up to add them to pyproject.toml (do NOT add it during this checkpoint without confirmation — the packages were [ASSUMED] in the RESEARCH Package Legitimacy Audit and adding them is a separate decision, but the Codex HIGH CUDA-runtime concern means the follow-up is tracked, not dropped: record it in 03-03-SUMMARY.md as an open follow-up so the next planning round picks it up).
  </action>
  <verify>
    <automated>python -c "import ctranslate2; print(sorted(ctranslate2.get_supported_compute_types('cuda',0)))"</automated>
  </verify>
  <acceptance_criteria>
    - LAPTOP: ctranslate2.get_supported_compute_types('cuda',0) output includes 'int8' or 'int8_float16' (NOT only 'float32'); OR a clear report that nvidia-cublas-cu12 + nvidia-cuda-runtime-cu12 were pip-installed to make it so AND recorded as a mandatory follow-up to add to pyproject (Codex HIGH: not a silent-first-run risk)
    - LAPTOP: `transcribe <small-file> --out out.json` exits 0 and out.json is valid Transcript JSON with a non-empty segments array and a detected language
    - DESKTOP: `transcribe <small-file> --out out.json` exits 0 with no --device flag (auto → CPU) and out.json is valid Transcript JSON
    - Same CLI command on both machines with no code changes and no per-machine flags (SC-5)
    - If the laptop CUDA path could not be made to work, report it as a known SC-5 partial (CPU desktop half passes; laptop CUDA half blocked on CUDA runtime libs) — do NOT silently pass; record the nvidia-*-cu12 follow-up in 03-03-SUMMARY.md regardless
  </acceptance_criteria>
  <done>SC-5 verified on both machines: the CLI runs end-to-end and writes a valid transcript.json on the CUDA laptop (with int8_float16 verified) AND the CPU desktop (with int8), same command, no code changes. Open Q1 closed. If the laptop CUDA path is blocked, that is reported explicitly as a partial pass AND the nvidia-*-cu12 follow-up is recorded in 03-03-SUMMARY.md (Codex HIGH: not dropped).</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**app/cli/__init__.py** — empty package init

**app/cli/transcribe.py**
- `def main() -> int` — the `transcribe` console_scripts entry point (D-03)
- `def _default_compute_type(device: str) -> str` — D-04 default (int8_float16 cuda / int8 cpu)
- `def _bootstrap_settings() -> None` — CLI-side settings bootstrap (load_settings_from_disk + configure before current — closes the PATTERNS gap)
- `def _get_or_configure_manager(settings) -> ModelManager` — precise model-manager bootstrap: configure_manager before get_manager() when unconfigured (Codex HIGH)
- argparse args: positional `file`, `--preset {small,balanced,large}` (default balanced), `--device {auto,cuda,cpu,rocm}` (default auto — Codex HIGH: 'auto' is a valid choice), `--language <code>` (default None), `--compute-type {int8,int8_float16,float16,int8_float32}` (default None), `--out <path>` (default <input>.transcript.json), `--verbose`
- finally-block `adapter.unload()` (Codex suggestion: VRAM cleanup on error); raw RuntimeError message preserved to stderr (Codex MEDIUM)

**pyproject.toml** (this plan, NOT 03-01 — Codex HIGH)
- `[project.scripts]` table with `transcribe = "app.cli.transcribe:main"` (declared here because app.cli.transcribe ships here; 03-01 added only the dependency pins)

**tests/test_cli_transcribe.py**
- test_device_resolution_from_settings_cuda, test_device_resolution_from_settings_cpu, test_device_auto_is_valid_choice, test_default_compute_type_per_device, test_compute_type_override, test_language_force_skips_detect, test_default_out_path, test_atomic_write_called, test_stdout_summary, test_missing_file_errors, test_cli_does_not_import_faster_whisper, test_bootstrap_settings_runs_before_current (W2), test_cli_configures_model_manager_when_unconfigured (Codex HIGH), test_adapter_unload_on_error (Codex suggestion + MEDIUM)

**SC-5 verification record** (in 03-03-SUMMARY.md): laptop CUDA result + desktop CPU result + Open Q1 resolution (system CUDA toolkit vs nvidia-*-cu12 pip packages vs blocked) + the mandatory nvidia-*-cu12 follow-up if the laptop needed the pip packages (Codex HIGH: tracked, not dropped)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user argv → CLI argparse | untrusted positional `<file>` path + flag values cross here |
| `<file>` / `--out` filesystem paths | local single-user filesystem; the CLI reads the input and writes the output |
| pip install (laptop CUDA runtime libs) | nvidia-cublas-cu12 / nvidia-cuda-runtime-cu12 are [ASSUMED] in the RESEARCH audit — the SC-5 checkpoint gates any install on a blocking-human verify |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering | `<file>` / `--out` path args | mitigate | V5 input validation: `Path(args.file).resolve()` + `exists()` check before decode (returns exit code 2 with a clear stderr message on missing file); `--out` parent-dir EXISTS check (writability is NOT pre-checked — Codex MEDIUM: cross-platform unreliable; atomic_write_json reports write failures with its raw message). argparse choices validate `--preset`/`--device`/`--compute-type` (and `--device auto` is now valid — Codex HIGH). Local single-user so no sandboxing needed beyond these checks (PROJECT.md). |
| T-03-05 | Information Disclosure | transcript stdout summary / raw error to stderr | accept | The one-line stdout summary (language, segment count, out path) and any raw RuntimeError message are the user's own transcript metadata / their own error on their own machine — no disclosure boundary. Preserving raw errors (Codex MEDIUM) aids debugging without crossing a trust boundary. |
| T-03-06 | Tampering | laptop pip install nvidia-cublas-cu12 + nvidia-cuda-runtime-cu12 | mitigate | These packages were [ASSUMED] in RESEARCH.md Package Legitimacy Audit (not pip-viewed this session). They are NVIDIA-official packages from pypi.nvidia.com (millions/wk downloads). The SC-5 checkpoint (Task 3) is a blocking-human gate that runs `ctranslate2.get_supported_compute_types('cuda',0)` BEFORE any install decision: if a system CUDA toolkit already provides the libs, NO pip install happens; if the pip packages are needed, the human confirms and the install is reported as a MANDATORY follow-up to add to pyproject (Codex HIGH: tracked in 03-03-SUMMARY.md, not dropped). The packages are NOT added to pyproject in this plan. |
| T-03-07 | Denial of Service | adapter VRAM not released on CLI error | mitigate | finally-block `adapter.unload()` runs even when transcribe_file or atomic_write_json raises (Codex suggestion) — VRAM is returned to the allocator rather than leaking across failed runs. |
| T-03-SC | Tampering | (no new pip installs in this plan's code; the checkpoint may surface a follow-up install) | mitigate | T-03-06 covers the checkpoint-gated install. faster-whisper + ctranslate2 were approved in 03-01's audit. |

No high-severity threats. No V2/V3/V4/V6 categories apply (local single-user, no auth, no sessions, no crypto).
</threat_model>

<verification>
- pytest tests/test_cli_transcribe.py -x (per-task, ~5s)
- pytest -q (full suite, ~15s — no regressions)
- `pip install -e .` + `transcribe --help` (smoke check that the [project.scripts] entry resolves — Codex HIGH: declared here, not in 03-01)
- SC-4 boundary re-check: `grep -rE "from faster_whisper|import faster_whisper|import ctranslate2" app/` still returns exactly app/models/stt/adapter.py (the CLI added no new import sites)
- SC-5 human-verify checkpoint (Task 3) — the only verification for "runs on both machines"
</verification>

<success_criteria>
- `transcribe` console_scripts entry point declared in pyproject (HERE, not 03-01 — Codex HIGH) and runs `transcribe <file>` to produce a valid transcript.json (SC-1, D-03)
- `--device auto` is a valid argparse choice and resolves device from persisted settings.backend via device_for with no per-machine flags (SC-5, Codex HIGH)
- Settings bootstrapped via load_settings_from_disk + configure before current() AND model manager configured via configure_manager before get_manager() when unconfigured (PATTERNS gap + Codex HIGH)
- Default compute_type int8_float16 (CUDA) / int8 (CPU); --compute-type overrides (D-04)
- --language forces + skips detect; omitted auto-detects (D-07, INGEST-06)
- Atomic_write_json writes Transcript JSON; default out is <input>.transcript.json (TRANS-01, Phase 1 D-04; SC-1 accepted interpretation — Codex MEDIUM)
- V5 path validation on <file> + --out parent exists (writability not pre-checked — Codex MEDIUM); raw errors preserved to stderr (Codex MEDIUM); adapter.unload() in finally (Codex suggestion)
- CLI never imports faster_whisper/ctranslate2 (SC-4)
- SC-5 human-verify: CLI runs end-to-end on the CUDA laptop (int8_float16 verified) and the CPU desktop (int8), same command, no code changes; Open Q1 closed; nvidia-*-cu12 follow-up tracked if needed (Codex HIGH)
- Full pytest suite green
</success_criteria>

<output>
Create `.planning/phases/03-stt-adapter-audio-chunker-standalone-cli/03-03-SUMMARY.md` when done, including the SC-5 verification record (laptop CUDA result, desktop CPU result, Open Q1 resolution, and the mandatory nvidia-*-cu12 follow-up if the laptop needed the pip packages). After execution, surface the phase for `/gsd-review` so the D-09 cross-AI review can run (D-09 is a downstream orchestrator concern, NOT a Phase 3 implementation deliverable - no task is added for it).
</output>